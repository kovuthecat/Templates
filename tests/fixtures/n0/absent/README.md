# Fixture « absent »

Dossier sans `.claude/n0.json`, intentionnellement — teste le cas « configuration absente » de
`plugin/bin/n0.mjs` (code 2, message de C1 mot pour mot). Ce fichier existe seulement parce que git
ne suit pas les dossiers vides.

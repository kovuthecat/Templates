# P2 · S5 — Skill /choisir-mecanisme + archive du rapport   (rédigé par Fable)

> **Modèle : Sonnet · effort : high · Vague : 1 (parallèle : oui)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-22 · Branche : —

## Lire (commun à la session)

- Le rapport source : `C:\Users\kovu\Downloads\rapport fonctionnalités claude code et documentation anthropic.md`
  (≈3000 lignes — le lire par sections ciblées : §64-65, §67-78, §85, Annexes A/E ; ne PAS tout
  charger d'un bloc)
- `plans/P2/index.md` §D-P2-8

## Hors périmètre

- Ne pas recopier les descriptions de fonctionnalités du rapport (partie périssable). Ne pas
  modifier WORKFLOW.md ni CLAUDE-BASE.md. Pas de commit.

---

## T12 — Archiver le rapport

### Objectif
Le rapport complet, daté, disponible dans le repo pour référence — jamais chargé par défaut.

### Décision clé
D-P2-8. Archive verbatim, seule addition : un en-tête d'avertissement.

### Lire / Modifier
Créer : `docs/references/claude-code-capabilities-2026-08.md` (copie du fichier de Downloads).

### Étapes
1. Copier le fichier tel quel (commande de copie, pas de retranscription).
2. Ajouter EN TÊTE un bloc de 4 lignes : source (rapport ChatGPT, 2026-08-22) · statut (archive
   figée, la doc officielle prime) · point d'entrée vivant : <https://code.claude.com/docs/llms.txt> ·
   usage : consulté via la skill `/choisir-mecanisme`, ne pas charger en session courante.

### Validation
- **N0 auto** : le fichier archivé a le même nombre de lignes que la source + l'en-tête.
- **N1** : — · **N2** : —

### Si bloqué
Fichier source absent de Downloads → STOP, demander à Thibault où il est.

### Message de commit (appliqué en fin de plan)
`docs(references): archive rapport capacités Claude Code 2026-08`

---

## T13 — Écrire `.claude/skills/choisir-mecanisme/SKILL.md`

### Objectif
La partie durable du rapport en ≤ 150 lignes : choisir le bon mécanisme Claude Code (règle,
skill, hook, agent, plugin, …) et auditer un projet — sans payer ce contenu en session courante.

### Décision clé
D-P2-8. Contenu = arbres de décision + checklists + URLs, PAS de descriptions de fonctionnalités
(périssables — pour ça : agent `claude-code-guide` ou `lecteur-doc` sur la doc vivante). La skill
cite ses limites : datée 2026-08, la doc officielle prime.

### Lire / Modifier
Créer : `.claude/skills/choisir-mecanisme/SKILL.md`.

### Étapes
1. Frontmatter : description = « Choisir le bon mécanisme Claude Code (règle, rule scopée, skill,
   hook, subagent, MCP, plugin, /goal, scheduling) et auditer la config .claude/ d'un projet.
   À dérouler quand on hésite entre plusieurs mécanismes, avant de créer de l'infrastructure
   custom, ou pour un audit périodique de workflow. »
2. §Arbre de choix (distiller rapport §64 + §85) : déterministe ? → code/script · connaissance
   permanente ? → CLAUDE.md/rule scopée · procédure réutilisable ? → skill · événement garanti ? →
   hook · exploration isolée ? → subagent · intégration externe ? → MCP · endpoint vérifiable ? →
   /goal · répétition temporaire ? → /loop · automatisation durable ? → Routine.
3. §Hiérarchie de complexité (rapport §65, 12 niveaux) + la règle : le mécanisme LE PLUS SIMPLE
   qui résout de façon fiable.
4. §Critères rapides (3-4 puces chacun) : quand créer une skill projet (multi-étapes + répété ≥2×
   + spécialisé — et coût : chaque skill paie sa description à toutes les sessions) · quand un
   hook plutôt qu'une instruction · quand un agent plutôt que faire soi-même · quand PAS de MCP.
5. §Audit express d'un projet (distiller phases 67-78 en ~15 lignes) : inventaire `.claude/` →
   classer chaque instruction (globale/scopée/procédure/état temporaire) → duplications →
   « always/never/toujours » convertibles en hook/validateur → candidats skill/agent → gaspillages
   de quota (rediscovery, gros modèle sur tâche simple, contexte obèse) → classement A/B/C/D.
6. §Vérifier avant de construire : la checklist de fraîcheur (llms.txt → page dédiée → feature
   availability → changelog) + le tableau d'URLs de l'annexe A du rapport (garder ~15 entrées les
   plus utiles) + renvoi : question pointue → agent `claude-code-guide` ; archive complète →
   `docs/references/claude-code-capabilities-2026-08.md`.
7. Plafond : 150 lignes. Si ça déborde, couper dans les critères, jamais dans l'arbre de choix.

### Validation
- **N0 auto** : ≤ 150 lignes ; zéro description de fonctionnalité (relecture : chaque section est
  un arbre, une checklist ou une table d'URLs) ; frontmatter valide.
- **N1** : — · **N2** : —

### Si bloqué
Un contenu semble ni arbre ni checklist mais indispensable → le résumer en ≤ 2 lignes avec pointeur
vers l'archive, consigner le cas.

### Message de commit (appliqué en fin de plan)
`feat(skills): /choisir-mecanisme — distillation du rapport capacités`

---

## Fin de session
Dérouler `/fin-de-tache` (mode vague parallèle).

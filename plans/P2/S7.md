# P2 · S7 — Docs de référence, plafonds, settings projet   (rédigé par Fable)

> **Modèle : Sonnet · effort : medium · Vague : 3 (parallèle : oui)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-22 · Branche : —

## Lire (commun à la session)

- `plans/P2/index.md` §Décisions (toutes)
- `WORKFLOW.md`, `CLAUDE-BASE.md` (état post-S1), `project-settings.json`, `plafonds.json`

## Hors périmètre

- Ne pas toucher les SKILL.md (S6) ni README/CHANGELOG/MIGRATION/DECISIONS (S8). CLAUDE-BASE est
  chargé à CHAQUE session de CHAQUE projet : chaque ligne ajoutée doit en mériter le coût. Pas de
  commit.

---

## T17 — WORKFLOW.md : effort, checkpointing, agents, enchaînement, hooks

### Objectif
Mettre la source unique du workflow au niveau des décisions P2.

### Décision clé
L'échelle d'effort affirmée au §3 (« pas de niveau max ») est probablement périmée — VÉRIFIER
avant de corriger, jamais de mémoire.

### Lire / Modifier
Modifier : `WORKFLOW.md`.

### Étapes
1. **§3 Effort** : vérifier l'échelle réelle sur
   <https://code.claude.com/docs/en/model-config> (WebFetch ou agent `lecteur-doc`). Si `max`
   existe : corriger l'échelle, ajouter 1 ligne de positionnement (`max` = au-delà de `xhigh`,
   mêmes réserves). Sinon : ne rien changer, consigner la vérification datée en commentaire.
2. **§4b Commits** : ajouter ~2 lignes : le filet intra-plan n'est PAS git (commits différés) mais
   le **checkpointing** de Claude Code — `/rewind` restaure code et/ou conversation ; committer
   « par sécurité » en cours de plan reste interdit.
3. **§5 Subagents** : renommer « Déléguer au lieu de faire » ; présenter les 4 agents du plugin
   (une ligne chacun : nom → quand) + renvoi table de délégation dans CLAUDE-BASE. Garder le
   principe existant (la conclusion remonte, pas les traces).
4. **Nouveau §5b — Enchaîner les sessions** (~8 lignes) : jamais deux sessions dans une même
   conversation ; Desktop → pastille posée par `/fin-de-tache` ; vagues sans N1 → orchestrateur
   `claude -p` (renvoi `/nouveau-plan` §Vagues autonomes) ; sessions `Desktop` (N1) → toujours
   lancement manuel.
5. **§7 Hooks** : tableau → 4 hooks (ajout `posttooluse-format.mjs` : « formate via prettier si
   configuré, silencieux sinon ») ; noter que le câblage vit dans `hooks.json` du plugin
   (`${CLAUDE_PLUGIN_ROOT}`), le settings projet n'en porte plus ; plafond VALIDATION → 60 dans
   la table des plafonds.
6. **§8 Anti-patterns** : ajouter « Faire soi-même ce qu'un agent mécanique rend en 10 lignes
   (build verbeux, exploration, doc externe) » et « Enchaîner deux sessions dans une même
   conversation ».

### Validation
- **N0 auto** : cohérence interne (aucun renvoi cassé §↔§) ; l'échelle d'effort ne contredit plus
  la doc vérifiée.
- **N1** : — · **N2** : —

### Si bloqué
La doc model-config est ambiguë sur l'échelle → consigner la citation exacte, ne pas trancher seul.

### Message de commit (appliqué en fin de plan)
`docs(workflow): agents, enchaînement, checkpointing, 4e hook, effort vérifié`

---

## T18 — CLAUDE-BASE.md : table de délégation, tests, enchaînement

### Objectif
Les règles communes intègrent la délégation systématique et la règle tests — en restant courtes.

### Décision clé
D-P2-4. Budget strict : +12 lignes MAXIMUM net sur le fichier (il est payé partout, toujours).

### Lire / Modifier
Modifier : `CLAUDE-BASE.md`.

### Étapes
1. §Avant de coder — remplacer la mention Explore par la **table de délégation** (4 lignes) :
   localiser quelque chose (> 1 fichier) → `explorateur` · build/typecheck/tests → `verificateur-n0`
   (jamais ces commandes en direct dans la conversation principale) · résumé diff/historique →
   `resumeur-git` · doc externe → `lecteur-doc`.
2. §Validation N0 — reformuler : « `build` + `typecheck` + **les tests du périmètre touché — en
   créer si la tâche introduit de la logique testable** ; un `—` se justifie dans le plan » (la
   parenthèse « si logique pure » saute).
3. §Une session = un `S<k>.md` — ajouter : « Session suivante d'un plan = nouvelle conversation,
   jamais la même (pastille ou commande du bandeau — `WORKFLOW.md` §5b). »
4. Mention `.claude/rules/` (1 ligne, §Règles générales) : « Instruction utile seulement sur un
   sous-ensemble de fichiers → `.claude/rules/` du projet (règle scopée), pas CLAUDE.md
   (cf. `CONVENTIONS.md`). »

### Validation
- **N0 auto** : delta net ≤ +12 lignes (`git diff --stat`) ; aucune règle dupliquée avec WORKFLOW.
- **N1** : — · **N2** : —

### Si bloqué
Impossible de tenir +12 lignes → couper dans l'existant (chercher les redites avec WORKFLOW),
consigner ce qui a été coupé.

### Message de commit (appliqué en fin de plan)
`docs(claude-base): délégation aux agents, tests systématiques, enchaînement`

---

## T19 — plafonds.json + templates VALIDATION/TASKS/CLAUDE/CONVENTIONS

### Objectif
Aligner plafonds et templates copiés sur D-P2-2, D-P2-6 et la règle `.claude/rules/`.

### Lire / Modifier
Modifier : `.claude/hooks/plafonds.json`, `VALIDATION.md`, `TASKS.md`, `CLAUDE.md`,
`CONVENTIONS.md`.

### Étapes
1. `plafonds.json` : `VALIDATION.md` 120 → 60.
2. `VALIDATION.md` (template) : en-tête réécrit — « Items N2 (jugement humain) EN ATTENTE
   uniquement. Item tranché = ligne supprimée (git est l'archive). Plafond 60. »
3. `TASKS.md` (template) : ligne convention effort — aligner sur l'échelle vérifiée en T17
   (coordination : si T17 n'est pas terminée, lancer la vérification model-config soi-même,
   c'est 1 fetch).
4. `CLAUDE.md` (template) : supprimer la ligne d'import `@...CLAUDE-BASE.md`, remplacée par le
   commentaire « Règles communes injectées à chaque session par le plugin `workflow` (hook
   SessionStart) — ne pas les recopier ici. »
5. `CONVENTIONS.md` : ajouter une courte règle « Règles scopées `.claude/rules/` » — quand une
   convention ne vaut que pour un chemin (migrations Supabase, données HE, tests…), fichier
   `.claude/rules/<domaine>.md` dans le projet plutôt qu'une ligne CLAUDE.md ; vérifier la syntaxe
   de scoping dans la doc memory avant d'écrire l'exemple
   (<https://code.claude.com/docs/en/memory>).

### Validation
- **N0 auto** : JSON parse ; grep `CLAUDE-BASE` dans `CLAUDE.md` template → plus d'import `@`.
- **N1** : — · **N2** : —

### Si bloqué
La syntaxe `.claude/rules/` (frontmatter/globs) diffère de l'attendu → écrire la règle sans
exemple de syntaxe + pointeur doc, consigner.

### Message de commit (appliqué en fin de plan)
`feat(templates): VALIDATION 60, CLAUDE.md sans import, règles scopées documentées`

---

## T20 — project-settings.json : plugin + permissions

### Objectif
Le settings copié dans chaque projet active le plugin et embarque l'allowlist (prérequis du mode
autonome et de l'orchestrateur headless).

### Décision clé
D-P2-3. Vérifier la clé exacte d'activation dans la doc plugins (`enabledPlugins`,
format `workflow@templates`) et la syntaxe des règles de permission
(<https://code.claude.com/docs/en/permissions>) — ne pas improviser.

### Lire / Modifier
Modifier : `project-settings.json`.

### Étapes
1. Supprimer le bloc `hooks` (voyage désormais dans le plugin). Garder `effortLevel`.
2. Ajouter `enabledPlugins` : `workflow@templates`.
3. Ajouter `permissions.allow` (indicatif, ajuster à la syntaxe vérifiée) : `npm run build/test/
   lint/typecheck` (formes `:*`), `npx vitest`, `git status/diff/log/show`, `node --check`.
   `permissions.deny` : lecture `.env*`, `**/*.pem`, `**/secrets/**`.
4. Mettre à jour le `_comment` : rôle du fichier, renvoi MIGRATION §5 pour les projets existants,
   et note « allowlist = prérequis des vagues headless ; l'étendre via la skill
   `fewer-permission-prompts` après quelques sessions réelles ».

### Validation
- **N0 auto** : JSON parse ; aucune règle `allow` couvrant commit/push (interdits en session par
  le workflow) ; les deny couvrent `.env`.
- **N1** : — · **N2** : —

### Si bloqué
Doute sur une règle de permission (trop large ?) → la retirer, l'allowlist s'étend a posteriori,
jamais par précaution inverse.

### Message de commit (appliqué en fin de plan)
`feat(settings): activation plugin workflow + allowlist permissions`

---

## Fin de session
Dérouler `/fin-de-tache` (mode vague parallèle).

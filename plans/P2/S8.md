# P2 · S8 — README, CHANGELOG, MIGRATION, décisions   (rédigé par Fable)

> **Modèle : Sonnet · effort : medium · Vague : 4 (parallèle : non)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-22 · Branche : —

## Lire (commun à la session)

- `plans/P2/index.md` (tout — cette session documente l'état final du plan)
- `README.md`, `MIGRATION.md` actuels ; l'état réel des livrables S1-S7 (survol des diffs :
  `git status` + lecture des fichiers créés/modifiés — via l'agent `resumeur-git` si disponible)

## Hors périmètre

- Ne rien corriger dans les livrables S1-S7 (incohérence constatée → STOP, signaler). Pas de commit.

---

## T21 — README.md : le repo est un plugin

### Objectif
Le README raconte le nouveau fonctionnement : plugin + marketplace, `/nouveau-projet`, DESIGN_SPEC.

### Décision clé
D-P2-1 à D-P2-7. Le README reste un README : orientation + séquences, le détail vit dans les
skills et WORKFLOW.md.

### Lire / Modifier
Modifier : `README.md`.

### Étapes
1. §distribution (remplace §« Référencés — ne jamais copier » pour la partie mécanique) : le repo
   est la marketplace `templates` (GitHub privé) exposant le plugin `workflow` (skills, hooks,
   agents) ; activation par projet via `enabledPlugins` dans `.claude/settings.json` ; les projets
   non migrés (chemins absolus) fonctionnent encore — MIGRATION §5.
2. §Copiés dans le projet : ajouter `DESIGN_SPEC.md` (si UI) ; retirer la mention jonction skills
   (obsolète après S9).
3. §Séquence de création : réécrite en 3 lignes — « dans le repo vide : dérouler `/nouveau-projet`
   (interview + instanciation) ; puis ARCHITECTURE avec Opus ; si UI : DESIGN_SPEC → maquette
   Claude Design → exports `design/maquettes/` ; puis `/nouveau-plan`. » Supprimer le pas-à-pas
   manuel absorbé par la skill (le stub AGENTS.md vit désormais dans la skill).
4. §Skills du workflow : table à 6 (+ `nouveau-projet`, `choisir-mecanisme`).
5. §Coût & fiabilité : ajouter une ligne délégation (« les tâches mécaniques passent par les
   agents du plugin — la conclusion remonte, pas les traces ») et une ligne plafond VALIDATION 60.

### Validation
- **N0 auto** : plus aucune mention de jonction NTFS ni d'import `@CLAUDE-BASE` ; tout fichier
  cité existe réellement dans le repo.
- **N1** : — · **N2** : relecture Thibault (le README est son point d'entrée).

### Si bloqué
Un livrable S1-S7 contredit le README à écrire → STOP, signaler (ne pas choisir seul qui a raison).

### Message de commit (appliqué en fin de plan)
`docs(readme): distribution par plugin, /nouveau-projet, DESIGN_SPEC`

---

## T22 — CHANGELOG + MIGRATION §5

### Objectif
Une ligne datée par évolution ; une procédure de migration plugin pour les projets existants.

### Décision clé
D-P2-2 : la migration DOIT retirer l'import `@CLAUDE-BASE` du CLAUDE.md projet (sinon règles en
double : import + injection hook).

### Lire / Modifier
Modifier : `CHANGELOG.md`, `MIGRATION.md`.

### Étapes
1. CHANGELOG : entrées datées 2026-08-22 — plugin workflow@templates · agents mécaniques ·
   /nouveau-projet · /choisir-mecanisme · DESIGN_SPEC.md · VALIDATION 60 · permissions allowlist ·
   enchaînement de sessions · règle tests.
2. MIGRATION : nouvelle « Étape 5 — Passage au plugin » (projets déjà migrés P1) : (a) settings :
   remplacer le bloc hooks par `enabledPlugins` + permissions (recopier depuis
   `project-settings.json` à jour) ; (b) CLAUDE.md : supprimer la ligne d'import `@...CLAUDE-BASE.md` ;
   (c) supprimer une éventuelle copie locale des skills du workflow ; (d) si UI : créer
   `DESIGN_SPEC.md` en y déplaçant les sections écrans/maquette de l'ARCHITECTURE.md du projet ;
   (e) vérification : nouvelle session → les règles communes apparaissent UNE fois, hooks actifs.
3. MIGRATION : rappeler en tête d'étape que le prompt standard de migration au fil de l'eau reste
   valable et couvre maintenant l'étape 5.

### Validation
- **N0 auto** : l'étape 5 est exécutable sans lire le plan P2 (auto-suffisante).
- **N1** : — · **N2** : —

### Si bloqué
Un cas de projet existant ne rentre pas dans (a)-(e) → l'ajouter à l'annexe Cas particuliers,
comme en P1.

### Message de commit (appliqué en fin de plan)
`docs(migration): étape 5 — passage au plugin workflow`

---

## T23 — Registre DECISIONS + fichiers de décision

### Objectif
Les 8 décisions P2 au registre, le détail dans `docs/decisions/`.

### Lire / Modifier
Modifier : `DECISIONS.md`. Créer : `docs/decisions/2026-08-22-plugin-workflow.md` (D-P2-1 à 3),
`docs/decisions/2026-08-22-agents-mecaniques.md` (D-P2-4 et 5),
`docs/decisions/2026-08-22-design-spec-validation.md` (D-P2-6 à 8).

### Étapes
1. Une ligne par décision au registre (verdict seul, renvoi au fichier).
2. Chaque fichier : contexte (2-3 lignes), décision, alternatives écartées (l'essentiel de la
   conversation du 2026-08-22 : scission VALIDATION écartée, fichier questionnaire vierge écarté
   au profit de la skill, hook anti-exploration reporté après mesure), conséquences.
3. Respect du plafond du registre (150).

### Validation
- **N0 auto** : registre ≤ 150 lignes ; chaque renvoi pointe un fichier existant.
- **N1** : — · **N2** : —

### Si bloqué
Registre déjà proche du plafond → dérouler `/purge-contexte` d'abord.

### Message de commit (appliqué en fin de plan)
`docs(decisions): D-P2-1 à D-P2-8`

---

## Fin de session
Dérouler `/fin-de-tache` (mode solo).

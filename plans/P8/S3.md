# P8 · S3 — Le cycle de plan sans double lecture   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 1 (parallèle : oui)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-22 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md` (toujours en premier), puis :
- `docs/decisions/2026-09-22-flous-du-workflow.md` — « Corrections sans choix » 1, 3, 5, 6, 7, 8, 10.
- `plugin/skills/nouveau-plan/SKILL.md` — Étape 0 (l. ~37-45), Étape 1 point 5 (l. ~95-102),
  Étape 1bis (bloc `critique-plan`, l. ~124-133), Étape 3 → 4, Étape 4b (`verificateur-plan`, l. ~298-305).
- `plugin/skills/nouveau-plan/references/squelette-index.md` (colonne « Message de commit »),
  `references/squelette-session.md` (fin de fichier).
- `plugin/skills/fin-de-tache/SKILL.md` l. ~15-25 (points 2-3 : N1, Bilan).
- `plugin/skills/verif-visuelle/SKILL.md` — Mode B (l. ~90-105), pour la forme de la consigne N1.
- `plugin/skills/reprendre/SKILL.md` l. ~50-60 ; `plugin/skills/fin-de-tache/references/fin-de-plan.md` point 1.
- Référence de repli à imiter : `plugin/skills/orchestrer-plan/SKILL.md` l. 118-120 (trois crans).

## Hors périmètre
`orchestrer-plan`, `reprendre-echec`, `EXECUTANT.md` (S2) ; `nouveau-projet`, `migrer-projet` (S4) ;
le script. `verif-visuelle` : lu, pas modifié.

---

## T6 — `/nouveau-plan` : sonde, replis, dépôt source, messages de vague

### Objectif
Corrections 1 (partie `nouveau-plan`), 3, 5 et 10 de la décision.

### Pourquoi
Quatre endroits où un exécutant littéral bloque ou improvise : un agent retiré prescrit pour
« sonder », un agent appelé sans repli, un premier geste impossible dans le dépôt source, une
colonne d'index à remplir avant que son contenu existe.

### Décision clé
`docs/decisions/2026-09-22-flous-du-workflow.md`, « Corrections sans choix ».

### Lire / Modifier
Modifier : `plugin/skills/nouveau-plan/SKILL.md`, `plugin/skills/nouveau-plan/references/squelette-index.md`.

### Étapes
1. Étape 1 point 5, « sonder » : « une commande ou un script jetable via `verificateur-n0` » → « une
   commande ou un script jetable exécuté au premier plan (`n0.mjs --seulement <nom>` s'il s'agit
   d'une commande N0) ».
2. Étape 1bis, bloc `critique-plan` : ajouter sous le bloc le repli à trois crans, forme de
   `orchestrer-plan/SKILL.md:118-120` — nom nu → `workflow:critique-plan` → `general-purpose`,
   `model: "opus"`, prompt « Lis `.claude/agents/critique-plan.md` (ou `plugin/agents/…` en dépôt
   source) et tiens ce rôle… ». Même repli, en une phrase, pour `verificateur-plan` à l'Étape 4b.
3. Étape 0, premier geste : ajouter le cas « dépôt source du workflow (pas de
   `.claude/workflow/manifest.json`, `plugin/` présent) → `claude plugin update workflow@templates`
   à la place de `/maj-workflow` » — une ligne.
4. Étape 3 → 4 : écrire que la colonne « Message de commit » des vagues parallèles se remplit **après**
   l'Étape 4 (retour sur l'index, un message par tâche, repris du `S<k>.md`) ; accorder le
   commentaire de `squelette-index.md` (l. 25-30).

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre `PASS` (dont `tester-renvois`).
- **Tests** : — (texte).
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : `grep -n "verificateur-n0" plugin/skills/nouveau-plan/` vide ; et chaque
  `subagent_type:` de `nouveau-plan/SKILL.md` a son repli écrit à moins de cinq lignes.

### Si bloqué
Aucun choix propre à cette tâche.

### Message de commit (appliqué par l'orchestrateur, vague parallèle)
`fix(nouveau-plan): sonde par n0, replis d'agent, dépôt source, messages de vague`
`Plan: P8/S3/T6`

---

## T7 — `/fin-de-tache` : bilan unique, N1 hors navigateur

### Objectif
Corrections 6 et 7 de la décision.

### Pourquoi
« Bilan dans le `S<k>.md` » placé dans la liste « après chaque tâche » se lit « un bloc par tâche »
ou « un bloc réécrit » ; « un défaut N1 se corrige maintenant » est impossible hors navigateur
in-app, où `/verif-visuelle` ne constate rien lui-même.

### Décision clé
Un seul bloc `## Bilan de session`, réécrit à chaque tâche (forme des bilans de P6/S8, P7/S4).

### Lire / Modifier
Modifier : `plugin/skills/fin-de-tache/SKILL.md`, `plugin/skills/nouveau-plan/references/squelette-session.md`.

### Étapes
1. `fin-de-tache` point 3 : « un seul bloc `## Bilan de session` en fin de `S<k>.md`, créé à la
   première tâche, **réécrit** (pas empilé) aux suivantes ».
2. `squelette-session.md` : ajouter le bloc `## Bilan de session` (vide, une ligne de consigne) avant
   `## Fin de session`.
3. `fin-de-tache` point 2 (N1) : « avec navigateur in-app, un défaut N1 se corrige maintenant ; sans
   (mode checklist de `/verif-visuelle`), il se consigne `N1 S<k> : à faire — <écran>` au bilan et au
   verdict, et ne bloque pas la tâche ».

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre `PASS`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : `—`

### Si bloqué
Aucun choix propre à cette tâche.

### Message de commit (appliqué par l'orchestrateur, vague parallèle)
`fix(fin-de-tache): bilan unique et N1 hors navigateur`
`Plan: P8/S3/T7`

---

## T8 — `/reprendre` : un diff non commité renvoie à la session

### Objectif
Correction 8 de la décision.

### Pourquoi
Le renvoi actuel mène à la clôture de plan, dont le point 1 suppose tout commité — l'inverse de la
situation qui déclenche la ligne : risque de clore un plan dont une session n'a pas fini.

### Lire / Modifier
Modifier : `plugin/skills/reprendre/SKILL.md`.

### Étapes
1. Ligne « Diff non commité → `/fin-de-tache` §Fin de plan » (~l. 55) → « Diff non commité →
   `/fin-de-tache` de la session interrompue (repère : le `S<k>.md` dont la zone contient les
   fichiers) ; la fin de plan seulement une fois toutes les sessions commitées ».

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre `PASS`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : `—`

### Si bloqué
Aucun choix propre à cette tâche.

### Message de commit (appliqué par l'orchestrateur, vague parallèle)
`fix(reprendre): diff non commité renvoie à la session`
`Plan: P8/S3/T8`

---

## Bilan de session
Fichiers modifiés (T6+T7+T8, un seul commit à faire par l'orchestrateur en fin de vague, sous
`.claude/wave.lock`) :
- `plugin/skills/nouveau-plan/SKILL.md` — sonde via `n0.mjs` (pas `verificateur-n0`) ; repli à
  trois crans pour `critique-plan` et `verificateur-plan` ; premier geste Étape 0 couvre le dépôt
  source (`claude plugin update workflow@templates`) ; « Message de commit » de l'index se remplit
  après l'Étape 4.
- `plugin/skills/nouveau-plan/references/squelette-index.md` — commentaire de la colonne « Message
  de commit » accordé à ce moment de remplissage.
- `plugin/skills/nouveau-plan/references/squelette-session.md` — bloc `## Bilan de session` ajouté
  avant `## Fin de session`.
- `plugin/skills/fin-de-tache/SKILL.md` — point 3 : un seul bloc `## Bilan de session`, réécrit (pas
  empilé) ; point 2 : N1 sans navigateur in-app se consigne `N1 S<k> : à faire — <écran>` sans
  bloquer.
- `plugin/skills/reprendre/SKILL.md` — diff non commité renvoie à `/fin-de-tache` de la session
  interrompue, pas à la fin de plan.

N0 : `node plugin/bin/n0.mjs` → quatre `PASS` (tester-hooks, tester-scripts, tester-renvois,
publier-dry-run). N1 : `—` (texte). N2 à faire : `—`. Anti-raccourci T6 vérifié :
`grep -n "verificateur-n0" plugin/skills/nouveau-plan/` vide ; chaque `subagent_type:` de
`nouveau-plan/SKILL.md` a son repli à moins de cinq lignes.

Prochaine action : rien côté session — sous `.claude/wave.lock`, l'orchestrateur committe et pousse
en fin de vague (repère `Plan: P8/S3/T6`, `Plan: P8/S3/T7`, `Plan: P8/S3/T8`), guidé par « Zone
modifiée » de l'`index.md`.

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : oui).

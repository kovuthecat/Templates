# P8 · S4 — Amorçage et migration disent le vrai   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 1 (parallèle : oui)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-22 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md` (toujours en premier), puis :
- `docs/decisions/2026-09-22-flous-du-workflow.md` — « Corrections sans choix » 1, 2, 9, 11.
- `plugin/templates/project-settings.json` — clés de `hooks` (l. ~18-76) : la vérité sur le nombre.
- `plugin/skills/migrer-projet/SKILL.md` l. ~130-140 et ~240-255.
- `plugin/skills/nouveau-projet/SKILL.md` — Phase A (questions Q1-Q14), Phase C point 1 (l. ~74-80),
  l. ~95-100 (correspondance questions ↔ sections).
- `plugin/templates/PROJECT_BRIEF.md` (titres de sections), `plugin/templates/ARCHITECTURE.md` (titres).
- `plugin/skills/maj-workflow/SKILL.md` l. ~15-20 (les « quatre frontières »).
- `plugin/templates/CLAUDE.md` l. ~5-15 ; `plugin/bin/publier.mjs` l. ~18-25.

## Hors périmètre
`maj-workflow` (lu, pas modifié : c'est `nouveau-projet` qui se met en accord avec lui). Les skills
de S2 et S3. Tout code de `publier.mjs` hors commentaire.

---

## T9 — Cinq hooks, et plus d'agent retiré dans les gabarits

### Objectif
Corrections 1 (partie gabarit) et 2 : l'attendu de `/migrer-projet` et chaque décompte de hooks
égalent le nombre de clés de `hooks` du gabarit (5) ; `plugin/templates/CLAUDE.md` ne cite plus
`verificateur-n0`.

### Pourquoi
Une migration correctement câblée sort `hooks: 5` et échoue la vérification littérale `hooks: 4` ;
un exécutant strict la déclare non finie ou retire un hook pour tomber juste.

### Lire / Modifier
Modifier : `plugin/skills/migrer-projet/SKILL.md`, `plugin/skills/nouveau-projet/SKILL.md` (l. ~80
seulement, le décompte), `plugin/templates/CLAUDE.md`, `plugin/bin/publier.mjs` (commentaire l. 22).

### Étapes
1. Compter les clés de `hooks` dans `plugin/templates/project-settings.json` (attendu 5 :
   SessionStart, PreToolUse, PostToolUse, PostModelSwitch, Stop). Si le compte diffère de 5, prendre
   le compte réel partout ci-dessous et le signaler au bilan.
2. `migrer-projet` l. ~136, ~245, ~250 : « 4 hooks » / `hooks: 4` → le compte réel.
3. `nouveau-projet` l. ~80 : « câble les 4 hooks » → le compte réel.
4. `publier.mjs` l. ~22 (commentaire) : « rejoue les quatre hooks » → le compte réel
   (`tests/tester-hooks.mjs` les rejoue tous).
5. `plugin/templates/CLAUDE.md` l. ~11 : la phrase qui cite « l'agent `verificateur-n0` (mesure A7) »
   → le script `n0.mjs` au premier plan (le fond de la mesure A7 reste : la sortie de build ne
   remplit pas le contexte).

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre `PASS` (dont `publier-dry-run`).
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : `grep -rnE "4 hooks|hooks: 4|quatre hooks" plugin/` vide **et** l'attendu de
  `migrer-projet` égal au nombre de clés de `hooks` du gabarit — un grep vide seul passerait si
  l'attendu avait été retiré au lieu d'être corrigé.

### Si bloqué
Aucun choix propre à cette tâche.

### Message de commit (appliqué par l'orchestrateur, vague parallèle)
`fix(migrer-projet): cinq hooks attendus`
`Plan: P8/S4/T9`

---

## T10 — `/nouveau-projet` : manifeste existant, routage des réponses

### Objectif
Corrections 9 et 11 : sur un projet dont le manifeste existe, `/nouveau-projet` déroule
`/maj-workflow` ; les réponses Q2, Q7 et Q9 ont une destination écrite.

### Pourquoi
`/maj-workflow` annonce que `/nouveau-projet` l'appelle ; le texte de `/nouveau-projet` dit « ne rien
refaire » — un projet repris après un échec partiel garde un workflow périmé sans le savoir. Et trois
réponses d'interview n'ont aucune section cible : deux exécutants les rangent à deux endroits.

### Décision clé
Q2 (utilisateurs et contexte d'usage) → section « Usage prévu » de `PROJECT_BRIEF.md` ; Q7
(plateformes cibles) et Q9 (données) → `ARCHITECTURE.md`, instancié en Phase D. Si « Usage prévu »
n'existe pas sous ce nom dans le gabarit, prendre la section du brief qui décrit l'usage et le
signaler au bilan.

### Lire / Modifier
Modifier : `plugin/skills/nouveau-projet/SKILL.md`.

### Étapes
1. Phase C point 1 : « si le manifeste existe, le workflow est déjà là : ne rien refaire » → « si le
   manifeste existe : dérouler `/maj-workflow` (contrôle de version, C4), puis continuer ».
2. Correspondance questions ↔ sections (l. ~98) : écrire la destination de Q2, Q7, Q9 comme ci-dessus.

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre `PASS`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : `—`

### Si bloqué
Aucun choix propre à cette tâche.

### Message de commit (appliqué par l'orchestrateur, vague parallèle)
`fix(nouveau-projet): maj-workflow sur manifeste existant, routage des réponses`
`Plan: P8/S4/T10`

---

## Bilan de session
- **Fichiers modifiés** : `plugin/skills/migrer-projet/SKILL.md` (5 hooks, ×2 lignes),
  `plugin/skills/nouveau-projet/SKILL.md` (5 hooks + manifeste existant → `/maj-workflow` + routage
  Q2/Q7/Q9), `plugin/templates/CLAUDE.md` (renvoi `verificateur-n0` → script `n0.mjs`),
  `plugin/bin/publier.mjs` (commentaire, « cinq hooks »).
- **Résumé** : T9 — le compte réel de clés `hooks` dans `plugin/templates/project-settings.json`
  est bien 5 (SessionStart, PreToolUse, PostToolUse, PostModelSwitch, Stop) ; toutes les occurrences
  « 4 hooks » corrigées en « 5 », et la citation morte de l'agent `verificateur-n0` remplacée par le
  script `n0.mjs` au premier plan. T10 — manifeste existant : `/nouveau-projet` déroule désormais
  `/maj-workflow` au lieu de « ne rien refaire » ; Q2 → section « Usage prévu » du brief, Q7 et Q9 →
  `ARCHITECTURE.md` (Phase D), comme décidé au plan.
- **N0** : `node plugin/bin/n0.mjs` → 4 `PASS` (`tester-hooks`, `tester-scripts`, `tester-renvois`,
  `publier-dry-run`), rejoué après T9 et après T10.
- **N1** : — (aucune UI touchée).
- **N2 à faire** : aucun.
- **Prochaine action** : néant pour cette session — collecte de la vague 1 par l'orchestrateur.

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : oui). `.claude/wave.lock` présent : commits de tâche déjà
posés (T9, T10), aucun push — l'orchestrateur committe et pousse le rangement en fin de vague.

Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- plugin/bin/prochaine-action.mjs:~264 — `cheminsDeZone` ne gère qu'un seul groupe `{...}` par
  segment (regex `/^(.*)\{([^{}]*)\}(.*)$/`, `.*` gourmand) : un segment à deux groupes d'accolades
  (`a/{x,y}/{c,d}`) ne serait pas développé correctement. Non couvert par D1 (qui ne décrit qu'un
  groupe), donc pas un bloquant au regard du plan — juste une limite à garder en tête si une future
  zone en use plusieurs.

## Notes de couverture
- T1 (D2, appel d'agent) et T2 (D1, arrêt sur arbre sale) relus en entier sur le diff
  `cee954f^..084a49b` (`plugin/bin/prochaine-action.mjs`, `tests/tester-scripts.mjs`,
  `tests/fixtures/plans/moteur-arbre-sale/index.md`).
- Objectif/Validation de chaque tâche confrontés au diff : les deux champs `agent`/`avertissement`
  sont bien exclusifs, la table modèle→effort est reprise fidèlement, le croisement arbre sale suit
  les huit règles D1 (backticks seuls, accolades développées, dossier en préfixe, `*`/accolades non
  appariées → illisible, fichiers du plan ignorés sauf nommés, `-z` avec renommage, échec `git` →
  question).
- Anti-raccourci des deux tâches vérifié dans le code : la fixture `moteur-efforts-melanges` écrit
  bien `Haiku`/`Sonnet` capitalisés (pas déjà en minuscules) ; les 9 cas D1 passent par un vrai
  dépôt git jetable (`initDepot`/`initEtCommitTout`) et de vrais fichiers, jamais une sortie `git`
  simulée à la main.
- `node plugin/bin/n0.mjs` rejoué : PASS 4/4 (`tester-hooks`, `tester-scripts`, `tester-renvois`,
  `publier-dry-run`) — confirme le bilan de session.
- Aucun commit `Correctif localisé :` / `Amendement :` dans la plage (`git log --format=%B
  cee954f^..084a49b`) ; aucune ligne `Écarts au plan :` dans `S1.md`.
- Incident signalé par la session (`docs/workflow/incidents/2026-09-22-commit-sous-verrou-non-bloque.md`,
  récidive du 2026-09-17 : commit accepté sous `.claude/wave.lock`) : lu, cohérent avec les preuves
  citées (`EXECUTANT.md` § Fin de tâche contredisant `WORKFLOW.md` §4b sur ce que le verrou exempte).
  C'est un défaut d'infra du plugin (hook `pretooluse-git.mjs`), pas un défaut du livrable de S1 ;
  rien à corriger ici, hors périmètre de cette revue par session.
- Décisions D1/D2 relues dans `docs/decisions/2026-09-22-flous-du-workflow.md` : l'implémentation
  correspond aux arbitrages et à leurs « revers » assumés.

# P3 · S1 — Domicilier les règles dans `WORKFLOW.md`   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 1 (parallèle : non)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-25 · Branche : main
- Décision appliquée : `docs/decisions/2026-08-25-cadrage-voie-unique-orchestration.md` (option C).
  La lire en entier avant de commencer : elle contient le tableau des faits mesurés et la
  définition exacte des deux cas d'exception headless.

## Lire (commun à la session)

- `docs/decisions/2026-08-25-cadrage-voie-unique-orchestration.md` — en entier.
- `plugin/WORKFLOW.md` — en entier (262 lignes) : c'est le fichier à réviser.
- `plugin/CLAUDE-BASE.md` — en entier (83 lignes).

## Hors périmètre

- Ne touche à AUCUNE skill : S2 et S3 s'en chargent, après cette session.
- Ne modifie ni les hooks ni `templates/project-settings.json`.
- Ne réécris pas §1-§3 ni §6-§7 de `WORKFLOW.md` au-delà des renvois nécessaires : le chantier
  porte sur §4a, §4b, §5b.

---

## T1 — `WORKFLOW.md` : trois domiciles, et l'option C dans §5b

### Objectif

Faire de §4a (statuts), §4b (commits) et §5b (sessions & voies d'orchestration) les domiciles
**uniques** de leurs règles, et réécrire §5b selon l'option C.

### Décision clé

Cause racine des trois bugs du 2026-08-24 : la même règle énoncée à 3-4 endroits, corrigée à un
seul. Le remède n'est pas « mieux synchroniser » mais **un seul énoncé** — partout ailleurs, un
renvoi d'une ligne. Et §5b devient : voie normale = **sous-agent** pour toute session orchestrée
(outil `Agent`, arrière-plan, environnement hérité, verdict = commits) ; `Env. = headless` est une
exception à **déclarer et justifier** dans l'index, légitime dans exactement deux cas — effort
`high`/`xhigh` à appliquer réellement (l'outil `Agent` ne règle pas l'effort), ou vague à lancer
sans garder la fenêtre ouverte (`claude -p` détaché survit, un sous-agent non).

### Lire / Modifier

- Modifier : `plugin/WORKFLOW.md` (§4a, §4b, §5b ; en-têtes des trois sections marqués comme
  domiciles — une ligne du type « Domicile de cette règle : les autres fichiers renvoient ici »).
- Modifier : la table des hooks (§7) si elle mentionne encore une voie par son ancien nom.

### Étapes

1. §4a : vérifier qu'il est complet et autoporteur (statut = `index.md` seul ; qui coche = le
   verrou décide). Y rapatrier ce qui manque depuis `fin-de-tache` SANS copier sa prose.
2. §4b : idem pour les commits (par session, staging explicite, repère `Plan:`, push groupé,
   exception `wave.lock`). Supprimer toute redite avec §5b.
3. §5b : réécrire selon la Décision clé. Structure imposée : la voie normale en premier, le tableau
   des deux cas d'exception headless ensuite, le repli pastille (hors Desktop) en dernier —
   trois blocs courts, pas de narratif.
4. Relire l'ensemble : aucune règle de §4a/4b/5b ne doit être énoncée deux fois dans le fichier.

### Validation

- **N0 auto (bloque le commit)** : `grep -c "sous-agent" plugin/WORKFLOW.md` ≥ 3 ;
  `grep -n "pastille" plugin/WORKFLOW.md` ne montre la pastille QUE comme repli ;
  aucune occurrence de « verdict lu dans l'index ».
- **Tests** : — (document ; la validation est le grep ci-dessus + relecture)
- **N1 visuel auto** : —
- **N2 humain** : —

### Si bloqué

Si une règle semble avoir DEUX domiciles légitimes (ex. : le repère `Plan:` — commit §4b ou
verdict §5b ?) → trancher §4b (le repère appartient au commit ; §5b renvoie), et le noter dans le
bilan. Si un cas ne rentre pas dans les deux exceptions headless → STOP, ne pas inventer un
troisième cas : signaler.

### Message de commit (appliqué par la session, à la fin de T1)

`refactor(workflow): §4a/4b/5b domiciles uniques, §5b passe à l'option C`

Dernière ligne du commit : `Plan: P3/S1/T1`

---

## T2 — `CLAUDE-BASE.md` : pointer, ne plus décrire

### Objectif

Aligner les 2-3 phrases de `CLAUDE-BASE.md` qui décrivent sessions/commits sur le rôle de pointeur
pur — le fichier est injecté à chaque session, chaque mot compte.

### Décision clé

`CLAUDE-BASE.md` ne doit contenir AUCUNE règle dont le domicile est `WORKFLOW.md` — seulement le
renvoi. Sa longueur ne doit pas augmenter (83 lignes aujourd'hui : plafond).

### Lire / Modifier

- Modifier : `plugin/CLAUDE-BASE.md` (section « Avant de coder » fin, et la phrase
  « Session suivante d'un plan = … §5b »).

### Étapes

1. Vérifier chaque phrase mentionnant commit, session suivante, ou voie d'orchestration : si elle
   reformule §4b/§5b, la réduire au renvoi.
2. `wc -l` avant/après : ≤ 83.

### Validation

- **N0 auto (bloque le commit)** : `wc -l plugin/CLAUDE-BASE.md` ≤ 83 ; aucune occurrence de
  `spawn_task` ni de description de voie.
- **Tests** : —
- **N1 visuel auto** : — · **N2 humain** : —

### Si bloqué

Rien à signaler de spécifique : la tâche est bornée à des réductions.

### Message de commit (appliqué par la session, à la fin de T2)

`refactor(workflow): CLAUDE-BASE pointe vers les domiciles au lieu de reformuler`

Dernière ligne du commit : `Plan: P3/S1/T2`

---

## Fin de session

Dérouler `/fin-de-tache` (mode parallèle : non).

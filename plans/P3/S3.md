# P3 · S3 — Satellites : renvois, colonne Env., renommage   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui, avec S2 — zones disjointes)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-08-25 · Branche : main
- Quatre tâches : trois de renvois (T5-T7), une de renommage (T8).
- **Dépend de S1** (les domiciles §4a/4b/5b existent). Indépendante de S2 : ne PAS lire ni
  attendre `executer-vague` — zones disjointes, c'est ce qui rend la vague parallèle.

## Lire (commun à la session)

- `plugin/WORKFLOW.md` §4a, §4b, §5b — tels que S1 les a laissés.
- Les quatre skills à modifier, en entier chacune au moment de sa tâche.

## Hors périmètre

- `executer-vague` : S2 s'en charge en parallèle. Ne pas l'ouvrir.
- Aucune règle nouvelle : cette session REMPLACE des reformulations par des renvois, elle
  n'invente rien.
- Les descriptions frontmatter : déjà raccourcies en 0.15.0, ne pas y revenir.

---

## T5 — `fin-de-tache` : renvois + numérotation continue

### Objectif

Remplacer chaque reformulation de §4a/4b/5b par un renvoi d'une ligne, et réparer la numérotation
en escalier (1-8, puis deux branches 9-13, puis 14-17) héritée des éditions successives.

### Décision clé

La skill garde sa VALEUR PROPRE : l'ordre des opérations, le gabarit de commit, le tableau
« verrou → qui coche », l'enchaînement (pastille de session suivante / de collecte). Ce qui part :
les justifications déjà écrites dans les domiciles. Numérotation cible : continue par section, ou
listes à puces là où l'ordre n'importe pas — au choix de l'exécutant, mais cohérente.

### Lire / Modifier

- Modifier : `plugin/skills/fin-de-tache/SKILL.md` (~154 lignes, cible ≤ 120).

### Étapes

1. Passer chaque paragraphe : reformulation d'un domicile → renvoi (`cf. WORKFLOW.md §4b`).
2. Renuméroter d'un bout à l'autre. Vérifier les renvois internes (« les commits du point N »).
3. La section Enchaînement : vérifier sa cohérence avec l'option C (sous-agent orchestré = pas
   d'enchaînement à faire ; manuel = pastille) — S1 a fixé §5b, s'y référer.

### Validation

- **N0 auto (bloque le commit)** : numérotation strictement croissante par section (contrôle
  visuel + grep `^[0-9]*\.` trié) ; `wc -l` ≤ 120 ; aucun renvoi interne cassé.
- **Tests** : — · **N1** : — · **N2** : —

### Si bloqué

Un passage qui semble porter une règle absente des domiciles → STOP, le signaler : c'est un trou
de S1, pas une licence pour reformuler ici.

### Message de commit (appliqué par la session, à la fin de T5)

`refactor(workflow): fin-de-tache renvoie aux domiciles, numerotation continue`

Dernière ligne du commit : `Plan: P3/S3/T5`

---

## T6 — `nouveau-plan` : la colonne Env. s'inverse

### Objectif

Mettre le gabarit de plan au régime de l'option C : `Env. = —` signifie sous-agent (défaut) ;
`Env. = headless` est l'exception, déclarée avec sa justification dans le bandeau du `S<k>.md`.

### Décision clé

Un plan écrit après P3 ne doit plus JAMAIS marquer `Desktop` : le navigateur est hérité par tout
sous-agent, la colonne ne discrimine plus ça. Les deux seules valeurs : `—` (sous-agent) et
`headless` (+ justification : « effort high appliqué » ou « vague fenêtre fermée »). Le squelette
de bandeau gagne une ligne conditionnelle pour le cas headless (commande `claude -p` complète).

### Lire / Modifier

- Modifier : `plugin/skills/nouveau-plan/SKILL.md` (~184 lignes) : Étape 2 (règle de
  parallélisation inchangée), Étape 3 (légende de la colonne Env., exemple du tableau), Étape 4
  (squelette de bandeau), et le paragraphe « Vagues autonomes » s'il reformule §5b → renvoi.

### Étapes

1. Légende Env. : réécrire selon la Décision clé.
2. Exemple de tableau : une session `headless` justifiée, une session `—`.
3. Squelette de bandeau : la ligne Environnement propose les deux formes.
4. Chasser les reformulations de §5b restantes → renvois.

### Validation

- **N0 auto (bloque le commit)** : `grep -c "Desktop" plugin/skills/nouveau-plan/SKILL.md` = 0
  (hors éventuel renvoi au repli pastille, qui doit citer §5b) ; l'exemple de tableau contient
  `headless`.
- **Tests** : — · **N1** : — · **N2** : —

### Si bloqué

Ne pas retoucher les plans EXISTANTS des projets (P2 de MYO reste tel quel) : la nouvelle légende
vaut pour les plans futurs. Si l'ambiguïté gêne, une ligne « legacy : Desktop se lit comme — » dans
la légende suffit.

### Message de commit (appliqué par la session, à la fin de T6)

`refactor(workflow): nouveau-plan — colonne Env. inversee (option C)`

Dernière ligne du commit : `Plan: P3/S3/T6`

---

## T7 — `reprendre` et `reprendre-echec` : vocabulaire des voies et verdicts

### Objectif

Aligner les deux skills de reprise sur l'option C : plus de mention d'une voie Desktop normale,
verdict = commits, PANNE réduite.

### Décision clé

Ces skills consomment les concepts, elles ne les définissent pas. Chercher : mentions de
`spawn_task`/pastille comme voie normale, du verdict JSON comme juge, de la table des symptômes de
`reprendre` (la ligne wave.lock et la ligne diff non commité doivent coller à §4b post-S1).

### Lire / Modifier

- Modifier : `plugin/skills/reprendre/SKILL.md` (~87 lignes) et
  `plugin/skills/reprendre-echec/SKILL.md` (~108 lignes). Modifications ponctuelles, pas de
  réécriture.

### Étapes

1. Grep `pastille|spawn_task|Desktop|PANNE|verdict` dans les deux fichiers ; juger chaque
   occurrence contre §4b/§5b post-S1.
2. Corriger les seules lignes fausses ou reformulantes. Longueur stable.

### Validation

- **N0 auto (bloque le commit)** : chaque occurrence restante des mots grep-és est soit exacte
  sous l'option C, soit un renvoi.
- **Tests** : — · **N1** : — · **N2** : —

### Si bloqué

Une incohérence plus profonde (ex. : un symptôme de la table de `reprendre` sans remède valide
sous l'option C) → STOP et signaler, ne pas réécrire la table.

### Message de commit (appliqué par la session, à la fin de T7)

`refactor(workflow): skills de reprise alignees sur l'option C`

Dernière ligne du commit : `Plan: P3/S3/T7`

---

## T8 — Renommer les références à la skill d'orchestration

### Objectif

Remplacer les 14 occurrences de `executer-vague` par `orchestrer-plan` partout où la skill est
**citée**, sans toucher au dossier lui-même.

### Décision clé

S2 crée `skills/orchestrer-plan/` (le contenu) ; cette tâche met à jour les **références**. Aucune
des deux ne supprime `skills/executer-vague/` — c'est la vague 3, une fois les deux commitées.
Pendant l'intervalle, les deux noms coexistent : c'est voulu, et sans effet (une skill de plus dans
la liste, le temps d'une vague).

### Lire / Modifier

- Modifier : `plugin/hooks/pretooluse-git.mjs` (2 occurrences, dans des **messages d'erreur** —
  vérifier que le texte reste juste, pas seulement le nom).
- Les autres références vivent dans les fichiers déjà traités par S1 (`WORKFLOW.md`,
  `CLAUDE-BASE.md`) et par les tâches T5-T7 de cette session : les corriger **au passage** dans
  chacune, plutôt que d'y repasser ici.

### Étapes

1. `grep -rn "executer-vague" plugin/` — inventaire.
2. Corriger le hook (le seul fichier hors périmètre des autres tâches).
3. Vérifier que S1 et T5-T7 ont bien traité les leurs ; s'il en reste dans un fichier de cette
   session, le corriger. **Ne pas toucher** aux fichiers de S2 (conflit de vague).

### Validation

- **N0 auto (bloque le commit)** : `grep -rn "executer-vague" plugin/ --exclude-dir=executer-vague`
  ne renvoie plus rien ; `node --check plugin/hooks/pretooluse-git.mjs` vert ;
  `skills/executer-vague/` toujours présent (sa suppression est vague 3).
- **Tests** : — · **N1** : — · **N2** : —

### Si bloqué

Une occurrence dans un fichier appartenant à S2 → **ne pas la toucher** et le signaler : S2 tourne
en parallèle, l'éditer créerait le conflit que les zones disjointes évitent.

### Message de commit (appliqué par la session, à la fin de T8)

`refactor(workflow): references executer-vague -> orchestrer-plan`

Dernière ligne du commit : `Plan: P3/S3/T8`

---

## Fin de session

Dérouler `/fin-de-tache` (mode parallèle : oui).

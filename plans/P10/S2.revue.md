Bloquant : 1
Couverture : complète

## Bloquants
- plugin/skills/orchestrer-plan/SKILL.md:41-49 — la « Table des actions » (le dispatch que
  l'orchestrateur suit — SKILL.md dit explicitement « l'action rendue s'exécute, ne se recalcule
  pas », et « Action absente de cette table : correctif localisé … sinon FAIL ») n'a pas été mise à
  jour pour les deux actions ajoutées par cette session : `cloturer` (T6, prochaine-action.mjs ligne
  ~907) et `relancer-interrompue` (T5, ligne ~633). `nouveau-plan/references/squelette-index.md` et
  `orchestrer-plan/references/remediation.md` documentent bien ces actions côté script, mais
  personne ne dit à l'orchestrateur quel geste faire quand le script les rend. — un orchestrateur
  qui reçoit `cloturer` (plan stampé Workflow sans `Clos :`, cas couvert par le test « moteur : tout
  coché … → cloturer ») ou `relancer-interrompue` (session coupée par quota) la traitera comme une
  action inconnue de la table : correctif localisé si possible, sinon `FAIL` prémisse — alors que
  T5/T6 avaient précisément pour objectif de faire agir l'orchestrateur sur ces cas (incidents ebm-msp
  et Chords cités au Pourquoi de S2).

## Backlog
- aucun

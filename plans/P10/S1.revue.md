Bloquant : 0
Couverture : complète — T1, T2 et T3 comparées au diff des trois commits (02de238, 8348e57,
b97cc4e) contre `plans/P10/S1.md` ; N0 rejoué (`node plugin/bin/n0.mjs`) → 4/4 PASS
(`tester-hooks`, `tester-scripts`, `tester-renvois`, `publier-dry-run`).

## T1 — Racine du dépôt (02de238)
`racineDepot()` (`lib.mjs`) suit exactement la logique de l'étape 2-3 : hors worktree lié,
`--show-toplevel` ; worktree lié classique, `dirname(--git-common-dir)` ; worktree lié d'un gitdir
déplacé, `null`. `vagueParallele` traduit `null` en refus par défaut (étape 4) ; `racineIntrouvable`
ajoutée et utilisée par `pretooluse-git.mjs` pour le message dédié. Les cinq appelants
(`revuesManquantes`, `sessionsOuvertes`, `derniereVersionPubliee`, `postmodelswitch-journal`,
`sessionstart-contexte`) tolèrent `null` sans exception. `prochaine-action.mjs` porte la même copie
(étape 5), `waveLock` forcé à `true` sur racine introuvable. Précision non prévue au plan mais
correcte : « pas un dépôt du tout » garde le repli sur `cwd` (comportement historique), distinct du
cas ambigu refusé — sans quoi des tests `prochaine-action` sans `git init` régressaient ; repéré et
corrigé par la session elle-même avant commit. Tests (étape 6) : les quatre cas (a)-(d) et le cas
`pousser` sont présents dans `tester-hooks.mjs`/`tester-scripts.mjs`, verrou vérifié sous
`<arbre>/.claude/wave.lock` (anti-raccourci respecté).

## T2 — Cache C4, `git -C`, revues `low`, plafond `.echec.md` (8348e57)
Cache C4 recalculé via `git --git-path` (étape 1). `pretooluse-git.mjs` accepte les options
globales (`-C`, `-c`) avant la sous-commande sur les trois refus, via un groupe `OPTIONS_GLOBALES`
partagé (étape 2). `revuesManquantes` exempte les sessions `low` en réutilisant une lecture de table
commune avec `sessionsOuvertes` (`toutesLesSessions`, refactor propre, étape 3). `depassements()`
résout le motif `plans/P<n>/S<k>.echec.md` via `resoudreMotif()` contre les fichiers réels (étape 4).
Les quatre cas et l'anti-raccourci (cache réellement écrit et relu en gitfile) sont couverts par les
tests ajoutés.

## T3 — Collecteur, version chargée, renvois (b97cc4e)
`lireIncident()` découpe chaque puce sur `·` puis teste chaque segment `Champ : valeur`, compatible
avec l'ancien gabarit (étape 1). `sessionstart-contexte.mjs` compare, dépôt source seulement (pas de
manifeste vendoré + `plugin/.claude-plugin/plugin.json` présent), la version chargée (`ICI`, jamais
`process.env.CLAUDE_PLUGIN_ROOT`) à la version source, muet si un fichier manque (étape 2). Les trois
renvois sont corrigés comme prescrit : `stop-contexte.mjs` → Étape 2, `lib.mjs` → « `/fin-de-tache`,
Relecture », `hooks.json` `_comment` reformulé pour le mode vendoré (étape 3). Le bilan de session
signale en outre un troisième écart réel trouvé en vérifiant (torrent-uploader,
`2026-09-17-navigate-localhost-refuse.md`) laissé ⚠, cohérent avec l'anti-raccourci (pas d'assouplissement
de la règle).

## Bloquants
- aucun

## Backlog
- aucun

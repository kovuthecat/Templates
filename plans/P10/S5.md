# P10 · S5 — Clarté des skills de plan   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 5 (parallèle : non)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-24 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md` (toujours en premier), puis :
- `docs/revues/2026-09-24-revue-finale-workflow.md` — sections « Cohérence globale » et « Clarté
  des consignes ».
- `plugin/skills/orchestrer-plan/SKILL.md` : ses titres d'étapes (`grep -n "^## " …`) et sa table
  d'actions. C'est la cible de tous les renvois que tu corriges.
- `tests/tester-renvois.mjs` (en-tête et assertions).
- Les `SKILL.md` de `nouveau-plan`, `cadrer`, `reprendre-echec`, `revue-de-conception` et
  `migrer-projet`, entiers, au moment de les modifier.

## Hors périmètre
- `WORKFLOW.md` (S4 l'a aligné, S6 y ajoute le glossaire).
- `orchestrer-plan/` et `fin-de-tache/`.
- Le code de `prochaine-action.mjs`.

---

## T13 — Renvois morts, seuil unique, « gate » et Plan Mode

### Objectif
Plus aucune skill de plan ne renvoie à une étape qui n'existe plus, et un test l'empêche de
revenir. Le seuil « encore un plan ou `/cadrer` » n'a qu'un domicile. « gate » garde son seul sens
(refuser sans demander).

### Pourquoi
Ce sont des instructions exécutables qui pointent dans le vide : « 5c », « 5d », « Étape 6 »,
restes de la voie headless. Trois compteurs différents décident d'arrêter une série de plans, et
aucun n'a arrêté les cinq plans MYO du 2026-09-14.

### Lire / Modifier
Modifier : les cinq `SKILL.md` ci-dessus, `plugin/skills/nouveau-plan/references/*`,
`plugin/skills/cadrer/references/*` si un renvoi y traîne, `tests/tester-renvois.mjs`.

### Étapes
1. Renvois vers `/orchestrer-plan` :

   | Renvoi mort | Remplacé par |
   | --- | --- |
   | « 5c » | l'action `verifier-premisse` ou `reprendre`, selon le contexte |
   | « 5d » | `enqueter` |
   | « Étape 6 » | « Étape 3 (question) » |
   | « Étape 5 » | « Étape 2 » |
   | « son Étape 3 » dans `nouveau-plan` (l. ~207, 221) | la bonne étape, d'après ses titres réels |

   Occurrences connues :
   - `nouveau-plan` l. ~207, 221, 234 ;
   - `reprendre-echec` l. ~25, 35, 48, 85, 91, 193, 213, 283.
   
   Les trouver toutes par `grep -rn "5c\|5d\|Étape [4-9]" plugin/skills/`.
2. Restes de la voie headless dans `reprendre-echec` l. ~15 (`claude --resume`,
   `.claude/vague/S<k>.session`) : les supprimer.
3. `tester-renvois.mjs`, **assertion 7** : aucun fichier de `plugin/**` ne cite `/orchestrer-plan`
   suivi, dans la même phrase, de `5c`, `5d` ou d'un numéro d'`Étape` supérieur au nombre de
   titres `## Étape` de `orchestrer-plan/SKILL.md`. Mettre à jour l'en-tête du test.
4. **Seuil unique**, domicile `nouveau-plan` Étape 0 :
   - « **zone** = les chemins de la colonne Zone modifiée » ;
   - « un plan a été **tué sur une prémisse** si un de ses `.echec.md` a porté
     `Nature : prémisse`, **y compris supprimé** :
     `git log --all -p --diff-filter=AD -- 'plans/*/*.echec.md' | grep -n 'Nature :.*pr[ée]misse'` » ;
   - « deuxième plan tué sur la même zone → `/cadrer` » ;
   - la règle de la troisième vague de remédiation reste.
   
   `cadrer` l. ~134 et `revue-de-conception` l. ~201 renvoient à ce domicile, sans reformuler.
5. « gate » au sens « attendre le oui » : `migrer-projet` l. ~114 et `revue-de-conception` l. ~130
   deviennent « Question » (titre et texte).
6. **Plan Mode** :
   - `cadrer` l. ~18 et ~180 : « Plan Mode pour l'investigation ; en sortir **avant l'Étape 5**,
     qui écrit seulement `docs/decisions/…`, la ligne de `DECISIONS.md` et le brief si la ligne
     `Brief :` l'exige » ;
   - même principe pour `revue-de-conception` l. ~235, qui ne doit plus interdire ce que ses
     dernières étapes écrivent.
7. `revue-de-conception`, contradictions internes :
   - le frontmatter interdit `Bash`, alors que l. ~83 lance `n0.mjs`. Retirer la ligne N0 : la
     revue ne corrige pas de code, elle constate ;
   - classe A « à faire maintenant » (l. ~172), sortie `TASKS.md` (l. ~199), mais la l. ~242
     interdit d'écrire `TASKS.md`. Trancher dans le texte : la revue **écrit** ses constats de
     classe A dans `TASKS.md`, et l'interdit porte sur les fichiers de code.
8. `cadrer`, issue « Preuve à faire » : nommer l'emplacement du protocole, à savoir une section
   `## Protocole de preuve` dans le `docs/decisions/<date>-<slug>.md` de la décision, là où
   `/nouveau-plan` le relira.

### Validation
- **N0 auto** : `node plugin/bin/n0.mjs` → vert, assertion 7 comprise.
- **Tests** : assertion 7 ; elle doit échouer si on réintroduit un « 5c » (essai à la main, noté
  au bilan, puis retiré).
- **Anti-raccourci** : une assertion 7 qui ne cherche que la chaîne « 5c ». « Étape 6 » était le
  renvoi mort le plus fréquent.

### Message de commit (appliqué par la session elle-même)
`fix(skills): renvois vers /orchestrer-plan, seuil unique avant un nouveau plan, gate et Plan Mode`
Dernière ligne du commit, obligatoire : `Plan: P10/S5/T13`

---

## T14 — Lecture ouverte, descriptions, polarité de prémisse, `migrer-projet`, allègement

### Objectif
Les points restants de clarté des skills de plan, et les passages longs déplacés en `references/`.

### Lire / Modifier
Modifier : les cinq `SKILL.md`, de nouvelles `references/` dans leurs dossiers,
`tests/tester-scripts.mjs` (chemin du gabarit `.echec.md` s'il déménage).

### Étapes
1. Lecture ouverte (C5) :
   - `nouveau-plan` l. ~288 (« Lire est restrictif… que ces fichiers ») devient « Lire est un
     point de départ : la lecture reste ouverte, seule l'écriture est bornée » ;
   - `reprendre-echec` l. ~201 (« ni le code ») : aligner.
2. Descriptions :
   - `nouveau-plan` : retirer « ou quand le scope est flou », puisque le corps renvoie ce cas à
     `/cadrer` ;
   - `reprendre-echec` : « déroulée dans un sous-agent lancé par `/orchestrer-plan`, jamais par
     l'orchestrateur lui-même ».
3. `nouveau-plan` l. ~44 : `claude plugin update workflow@templates --scope local`.
4. Polarité de prémisse, `reprendre-echec` l. ~145-150 : « Écris l'affirmation que **tu** as
   constatée (ex. « le PDF a 32 titres »). `REFUTEE` = ton constat est faux ; `CONFIRMEE` = ton
   constat est vrai, donc le plan est faux. » Même polarité que `verificateur-premisse`.
5. `migrer-projet` :
   - « point 5 » (l. ~63) devient « point 8 » : c'est lui qui crée `n0.json` ;
   - l. ~234-262 : l'ordre D1 à D8 est écrit explicitement. D1-D4, puis commit, puis D6-D8 ;
     D5 est consigné au rapport. Remplacer « quatre premiers… le cinquième ».
6. Allègement sans perte : déplacer en `references/`, avec un renvoi d'une ligne à l'endroit
   exact où le passage servait :
   - `reprendre-echec` : le gabarit (l. ~99-195) en `references/gabarit-echec.md`, et le mode
     enquête (l. ~52-95) en `references/mode-enquete.md` ;
   - `migrer-projet` : les voies 1 et 2 (B1 et B2) en `references/voies-migration.md` ;
   - `revue-de-conception` : le gabarit du rapport (l. ~203-231) en `references/gabarit-rapport.md`.
   
   Les interdits et les premiers gestes **restent** dans le corps. Si le gabarit `.echec.md` a
   déménagé, mettre à jour le test (c) de S2 dans `tester-scripts.mjs` pour qu'il lise le nouveau
   chemin.

### Validation
- **N0 auto** : `node plugin/bin/n0.mjs` → vert. `tester-renvois` vérifie que chaque nouvelle
  annexe a un appelant et qu'aucune annexe n'en cite une autre.
- **Anti-raccourci** : un déplacement en annexe qui emporte un interdit ou un premier geste. Relire
  chaque corps de skill allégé et vérifier que ses STOP y sont encore.

### Message de commit
`docs(skills): lecture ouverte, descriptions, polarité de prémisse, annexes des skills longues`
Dernière ligne : `Plan: P10/S5/T14`

---

## Bilan de session
<à compléter>

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle : oui/non » du bandeau).

# P10 · S7 — Ménage et publication 0.46.0   (rédigé par Opus)

> **Modèle : Sonnet · effort : low · Vague : 7 (parallèle : non)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-24 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md` (toujours en premier), puis :
- `TASKS.md` (entier), `docs/revues/2026-09-24-revue-finale-workflow.md`, section « Ménage du
  dépôt source ».
- `plans/P6/S1.md` (bilan, l. ~94) et `docs/analyses/2026-09-17-sondes-p6.md`.
- `plans/P7/index.md`, `plans/P8/index.md` et `plans/P8/S5.revue.md`, `plans/P9/index.md` et
  `plans/P9/S3.revue.md`.
- `CHANGELOG.md` (entrée 0.45.0) et `plans/P9/S4.md` (forme d'une tâche de publication).

## Hors périmètre
- Tout fichier sous `plugin/` autre que `plugin.json` et `MIGRATION.md`.
- `DECISIONS.md` : déjà nettoyé au commit du plan.

---

## T17 — Ménage des plans et de `TASKS.md`

### Objectif
`TASKS.md` ne porte plus que ce qui reste à faire. Les plans P6 à P9 ont leurs statuts justes, et
les bloquants de revue oubliés sont versés.

### Pourquoi
La revue a trouvé :
- trois bloquants P6 déjà corrigés et toujours listés ;
- la liste de tâches d'un plan (P7) déjà exécuté ;
- deux bloquants de revue (P8/S5, P9/S3) jamais versés, parce que la clôture n'existait pas en
  mode orchestré.

### Lire / Modifier
Modifier : `TASKS.md`, `plans/P6/S1.md`, `plans/P7/index.md`, `plans/P8/index.md`,
`plans/P9/index.md`, et `.revue.md` de P8 et P9 (suppression après versement).

### Étapes
1. `TASKS.md` :
   - retirer les bloquants P6/S3, P6/S4 et P6/S7 (corrigés, preuves dans la revue du 2026-09-24,
     « Ménage ») ;
   - P6/S1 : écrire le « Bilan de session » de `plans/P6/S1.md` (l. ~94) à partir de
     `docs/analyses/2026-09-17-sondes-p6.md`, en résumé de 10 lignes au plus, avec un renvoi
     vers ce fichier. Puis retirer le bloquant.
2. P7 : S5 est commitée (`git log --grep "Plan: P7/S5"`). La passer à `[x]` dans `plans/P7/index.md`
   et ajouter `Clos : 2026-09-24`. Les revues de S2 à S4 n'ont jamais eu lieu : l'écrire en une
   ligne sous l'objectif (« revues S2-S4 non faites, plan publié en 0.40.0 »), sans les lancer.
3. P8 : verser le bloquant de `plans/P8/S5.revue.md` dans `TASKS.md`, en une ligne avec son
   chemin. Supprimer les `.revue.md` de P8 dans un commit, puis ajouter `Clos : 2026-09-24` à son
   index.
4. P9 : verser le bloquant de `plans/P9/S3.revue.md` dans `TASKS.md` et passer S3 de `[x]!` à `[x]`.
   Ne **pas** clore P9, puisque S5 reste à lancer. Ne pas supprimer ses revues : elles le seront à
   sa clôture.
5. `TASKS.md` reste sous 60 lignes.

### Validation
- **N0 auto** : `node plugin/bin/n0.mjs` → vert.
- **Contrôle** :
  - `node plugin/bin/prochaine-action.mjs P8` → `fini` ;
  - `node plugin/bin/prochaine-action.mjs P7` → `fini` ;
  - `node plugin/bin/prochaine-action.mjs P9` → la vague de S5 (validation ou lancement), jamais
    `relire`.
- **Anti-raccourci** : supprimer une revue avant d'en avoir versé le bloquant. Le versement vient
  d'abord, dans le même commit ou avant.

### Si bloqué
Aucun critère d'arrêt en jeu.

### Message de commit (appliqué par la session elle-même)
`chore(plans): ménage de TASKS.md, bloquants de P8 et P9 versés, P7 et P8 clos`
Dernière ligne du commit, obligatoire : `Plan: P10/S7/T17`

---

## T18 — Publier 0.46.0

### Objectif
La version 0.46.0 (S5 et S6) est publiée et installée sur ce poste.

### Lire / Modifier
Modifier : `plugin/.claude-plugin/plugin.json`, `CHANGELOG.md`, `plugin/MIGRATION.md` (seulement si
le fichier tient une section par version).

### Étapes
1. `plugin.json` : `"version": "0.46.0"`.
2. `CHANGELOG.md` : une entrée datée, sur le modèle de 0.45.0. Ce que ça change pour un projet :
   - renvois corrigés, avec un test qui les garde ;
   - seuil unique avant un nouveau plan ;
   - « gate » en un seul sens, et un glossaire ;
   - exemples de prémisse qui concluent ;
   - contrôle 12 de `verificateur-plan` ;
   - skills longues allégées en annexes.
3. N0, commit, puis au premier plan :
   - `node plugin/bin/publier.mjs` ;
   - `claude plugin update workflow@templates --scope local` ;
   - `claude plugin list` → **0.46.0**, `✔ enabled`.

### Validation
- **N0 auto** : `node plugin/bin/n0.mjs` → quatre `PASS`.
- **Anti-raccourci** : le bilan porte la sortie du `publier.mjs` réel et `claude plugin list` en
  0.46.0. Un `--dry-run` ne suffit pas.

### Si bloqué
Critère 1 (irréversible) : publication seulement si N0 est vert.

### Message de commit
`chore(plugin): 0.46.0 — consignes clarifiées, renvois gardés par test`
Dernière ligne : `Plan: P10/S7/T18`

---

## Bilan de session
<à compléter>

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle : oui/non » du bandeau).

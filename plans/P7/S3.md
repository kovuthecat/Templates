# P7 · S3 — Le lancement compose le `subagent_type`   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : non)**
> **Environnement : indifférent** (`WORKFLOW.md` §6, N1)
> **Régime : fixé**
> **Latitude : défaut** — moyens libres ; objectif, contrats publics et zone du plan fixes.
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `EXECUTANT.md`.

- Date : 2026-09-18 · Branche : —

## Lire (commun à la session — point de départ, pas une clôture)
`plugin/EXECUTANT.md` (toujours en premier), puis :
- `plugin/skills/orchestrer-plan/SKILL.md` — en entier ; c'est le fichier principal de la session.
- `plugin/skills/orchestrer-plan/references/remediation.md` — blocs `reprendre` (~`:54`) et
  `enqueter` (~`:84`).
- `plugin/bin/prochaine-action.mjs`, sections `lancer` et `relire` — le contrat que S2 vient de
  changer ; tu le consommes, tu ne le modifies pas.
- `docs/decisions/2026-09-18-effort-porte-par-l-agent-de-session.md`.

## Hors périmètre
`plugin/bin/prochaine-action.mjs` (S2, déjà fait), `plugin/agents/` (S1, déjà fait),
`plugin/WORKFLOW.md` (S4). Trois règles qui valent pour toute la session :
- **La phrase-invariant reste au mot près** dans chaque bloc `Agent({ … })` que tu touches :
  `Lis ${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md en entier avant ton premier geste : il porte les invariants de lancement.`
  `tests/tester-renvois.mjs` la vérifie littéralement et `publier.mjs` refuse de publier sans elle.
- **`isolation: "worktree"` et `subagent_type: "fork"` restent interdits** (`SKILL.md:92`).
- **Ne jamais écrire un `subagent_type` littéral** comme `"session-high"` dans un bloc de lancement :
  la valeur se compose depuis l'index. Un littéral figerait l'effort de toutes les sessions.

---

## T5 — Composer le `subagent_type` de session, avec repli annoncé

### Objectif
Dans le bloc `Agent({ … })` de l'Étape 1 (`SKILL.md:96-113`), remplacer
`subagent_type: "claude"` par un nom composé depuis l'effort de la session, avec un repli à trois
crans si ce nom ne résout pas.

### Pourquoi
C'est le geste qui applique enfin l'effort écrit dans l'index — tout le reste du plan n'existe que
pour le rendre possible. Le repli est ce qui empêche ce gain de coûter la robustesse : aujourd'hui
`"claude"` résout partout, et ce plan ne doit pas transformer une dégradation silencieuse en blocage
total.

### Décision clé
`docs/decisions/2026-09-18-effort-porte-par-l-agent-de-session.md`, plus l'arbitrage du mainteneur du
2026-09-18 : **replier en annonçant**, pas bloquer. Le nom d'un agent de plugin ne résout pas partout —
il est nu quand le plugin est vendoré (`.claude/agents/`), préfixé `workflow:` quand il est installé
en marketplace, et absent dans un bac à sable sans installation. Les trois crans, dans l'ordre :

1. `session-<effort>` ;
2. `workflow:session-<effort>` ;
3. `claude` — **et l'orchestrateur annonce alors**, en une ligne au bandeau de vague et dans son
   rapport, que la vague tourne à l'effort ambiant de la conversation, pas à celui de l'index. C'est
   exactement le message que portait `regler-effort` ; il ne disparaît pas, il devient conditionnel.

### Référence
`SKILL.md:141-143` : le repli déjà en place pour `relecteur-session` (`Agent type '…' not found` →
repli nommé → mention au rapport si le repli échoue aussi). Même forme, même registre.

### Lire / Modifier
**Modifier** : `plugin/skills/orchestrer-plan/SKILL.md` — bloc `Agent({ … })` de l'Étape 1, et le
Préflight (3) autour de `:60-62`.

### Étapes

1. Remplacer `subagent_type: "claude"` par `subagent_type: "session-<effort lu dans l'index>"`, en
   gardant `model: <modèle lu dans l'index>` et tout le prompt **inchangés** — phrase-invariant
   comprise.
2. Écrire les trois crans du repli sous le bloc, sur la forme de `:141-143`, avec le texte exact de
   l'annonce du cran 3.
3. Préflight (3) (`:60-62`) : il ne prévoit aujourd'hui que « revues annoncées absentes ». Y ajouter
   la présence des agents `session-<effort>` pour les efforts effectivement demandés par la vague à
   lancer, de sorte que le repli soit connu **avant** de lancer, pas découvert au premier échec.
4. Relire le bloc modifié : la phrase-invariant est-elle intacte, au mot près ?

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre commandes vertes ; `tester-renvois`
  contrôle la phrase-invariant et la résolution des agents cités.
- **Tests** : aucun test créé ici — la couverture du lancement est le cas de cohérence « valeurs
  acceptées ↔ agents présents » ajouté en S2/T4. Le motif de `tester-renvois.mjs:234` ne reconnaît
  qu'un `subagent_type` littéral et ignorera le nom composé : c'est attendu, pas à contourner.
- **N1 visuel auto** : — · **N2 humain** : —
- **Anti-raccourci** : écrire le repli comme une phrase de prose (« si l'agent n'existe pas, utiliser
  `claude` ») sans le message d'annonce du cran 3. N0 serait vert, et le workflow retrouverait
  exactement le mensonge silencieux que ce plan supprime, mais sans même le message de
  `regler-effort` pour le signaler.

### Si bloqué
Aucun des quatre critères d'arrêt.

### Message de commit (appliqué par la session elle-même)
`feat(orchestration): lancer chaque session sur l'agent de son effort`
Dernière ligne du commit, obligatoire : `Plan: P7/S3/T5`

---

## T6 — Transmettre l'effort au relecteur, et retirer les traces de `regler-effort`

### Objectif
Passer l'effort de chaque session au bloc `relecteur-session` (`SKILL.md:132-139`), et retirer de la
skill l'en-tête (`:8-9`) et la ligne de table (`:37`) de l'action `regler-effort`, que S2 a supprimée
du moteur.

### Pourquoi
L'exemption « une session `low` n'est pas relue » (C7) repose sur un effort que le parent est censé
donner au relecteur (`relecteur-session.md:20-22`) — et que le bloc de lancement ne lui passe pas
aujourd'hui. S2 a rendu l'action `relire` porteuse de l'effort ; il reste à le lui transmettre.

### Décision clé
S2 a mis l'exemption `low` **dans le moteur** : une session `low` ne figure plus dans les sessions que
`relire` rend. Le relecteur ne recevra donc plus de session `low` du tout — l'effort que tu lui passes
sert à ce qu'il puisse le redire et le vérifier, pas à ce qu'il arbitre seul. Ne réécris pas
`relecteur-session.md` : il est hors de ta zone, et son texte reste juste.

### Lire / Modifier
**Modifier** : `plugin/skills/orchestrer-plan/SKILL.md` — bloc `Agent({ … })` de l'Étape 2,
en-tête `:8-9`, ligne de table `:37`.

### Étapes

1. Bloc `relecteur-session` : passer, pour chaque session de la liste, son effort tel que l'action
   `relire` le rend maintenant (`sessions: [{session, effort}]`). Garder la phrase-invariant intacte
   et le repli `general-purpose` existant (`:141-143`).
2. Retirer la ligne de table de l'action `regler-effort` (`:37`).
3. Réécrire l'en-tête `:8-9` : il explique aujourd'hui qu'il n'existe pas d'agents `session-<effort>`
   et qu'une session hérite de l'effort de la conversation. Les deux affirmations deviennent fausses.
   Le remplacer par une phrase disant que l'effort vient de l'index via l'agent de session, et que le
   modèle de l'orchestrateur (Sonnet, jamais Haiku) reste, lui, une contrainte inchangée.
4. Chercher dans la skill toute autre mention de `regler-effort` ou de l'héritage d'effort
   (`grep -n "regler-effort\|hérit" plugin/skills/orchestrer-plan/SKILL.md`) et la traiter.

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre commandes vertes.
- **Tests** : — (le comportement testé vit dans le moteur, couvert en S2/T3).
- **N1 visuel auto** : — · **N2 humain** : —
- **Anti-raccourci** : retirer la ligne de table `:37` sans toucher à l'en-tête `:8-9`. Aucun test ne
  lit cette prose ; la skill continuerait d'annoncer à chaque orchestration une règle que le code ne
  suit plus, et c'est précisément le genre d'écart que ce plan corrige.

### Si bloqué
Aucun des quatre critères d'arrêt.

### Message de commit (appliqué par la session elle-même)
`fix(orchestration): transmettre l'effort au relecteur, retirer regler-effort`
Dernière ligne du commit, obligatoire : `Plan: P7/S3/T6`

---

## T7 — Remédiation : reprise et enquête à l'effort de leur modèle

### Objectif
Les blocs `reprendre` (~`references/remediation.md:54`) et `enqueter` (~`:84`) lancent
`subagent_type: "claude"`. Leur faire composer un `session-<effort>`, l'effort étant dérivé du modèle
que le moteur décide déjà pour la remédiation.

### Pourquoi
Ce sont les sessions qui ont **déjà échoué une fois** — celles où l'effort compte le plus. Après le
retrait de `regler-effort`, plus aucun message n'avertirait qu'elles tournent à l'effort ambiant :
le seul chemin resté en héritage muet serait le plus sensible du workflow.

### Décision clé
Arbitrage du mainteneur du 2026-09-18 : **l'effort suit le modèle**, conformément à `WORKFLOW.md` §3
et à la table `modelSettings` du gabarit — Opus → `high`, Sonnet → `medium`, Haiku → `low`. Le moteur
décide déjà le modèle d'une remédiation (`prochaine-action.mjs`, branches `reprendre` / `enqueter`) ;
l'effort s'en déduit, il ne se lit pas sur la ligne d'index de la session en échec — une reprise qui
rejouerait exactement le même effort après un échec n'aurait pas de sens, alors que le moteur escalade
déjà le modèle.

### Lire / Modifier
**Modifier** : `plugin/skills/orchestrer-plan/references/remediation.md`.

### Étapes

1. Dans les deux blocs, remplacer `subagent_type: "claude"` par le nom composé à partir du modèle
   décidé par le moteur, via la correspondance §3 (Opus → `high`, Sonnet → `medium`, Haiku → `low`).
2. Écrire la correspondance **une fois**, en clair, dans l'annexe, et y renvoyer depuis les deux
   blocs — deux copies divergeront.
3. Appliquer aux deux blocs le même repli à trois crans qu'en T5, avec la même annonce au cran 3.
   Renvoyer à T5 plutôt que de recopier le texte du repli si l'annexe s'y prête.
4. Phrase-invariant intacte dans les deux blocs.

### Validation
- **N0 auto (bloque le commit)** : `node plugin/bin/n0.mjs` → quatre commandes vertes.
- **Tests** : — (blocs de prose ; la cohérence valeurs ↔ agents est couverte en S2/T4).
- **N1 visuel auto** : — · **N2 humain** : —
- **Anti-raccourci** : ne traiter que `reprendre` et oublier `enqueter`, ou l'inverse. Les deux blocs
  se ressemblent assez pour qu'un seul semble suffire ; `grep -n 'subagent_type' plugin/skills/orchestrer-plan/references/remediation.md`
  doit ne plus rendre aucun `"claude"` à la fin de la tâche.

### Si bloqué
Aucun des quatre critères d'arrêt. Si la correspondance modèle → effort s'avère déjà écrite ailleurs
dans `plugin/**`, y renvoyer plutôt que la réécrire, et le signaler.

### Message de commit (appliqué par la session elle-même)
`fix(remediation): reprise et enquête à l'effort de leur modèle`
Dernière ligne du commit, obligatoire : `Plan: P7/S3/T7`

---

## Bilan de session

**Fichiers modifiés** :
- `plugin/skills/orchestrer-plan/SKILL.md` — bloc `Agent({ … })` de l'Étape 1 : `subagent_type`
  composé (`session-<effort lu dans l'index>`) au lieu de `"claude"`, repli à trois crans écrit en
  dessous (`session-<effort>` → `workflow:session-<effort>` → `claude`, annoncé) ; Préflight (3)
  étendu à la présence des agents `session-<effort>` demandés par la vague (T5). En-tête (`:8-9`)
  réécrit — l'effort vient de l'index via l'agent de session, le modèle de l'orchestrateur (Sonnet,
  jamais Haiku) reste seul inchangé ; ligne de table `regler-effort` retirée ; bloc `relecteur-session`
  de l'Étape 2 transmet désormais l'effort de chaque session dans son prompt (T6).
- `plugin/skills/orchestrer-plan/references/remediation.md` — table `Modèle → effort` écrite une fois
  en tête d'annexe (Opus `high`, Sonnet `medium`, Haiku `low`) ; les deux blocs `reprendre — à froid`
  et `enqueter` composent `subagent_type: "session-<effort du modèle de l'action>"` au lieu de
  `"claude"`, chacun avec un renvoi au repli à trois crans de T5 plutôt qu'une copie (T7).

**Résumé** : le lancement applique enfin l'effort écrit dans l'index — les trois blocs qui lançaient
une session de plan (`lancer`, `reprendre` à froid, `enqueter`) composent maintenant
`subagent_type: "session-<effort>"` au lieu du générique `"claude"`, avec le même repli à trois crans
partout (nom nu → préfixé `workflow:` → `claude`, ce dernier cran annoncé au bandeau et au rapport
plutôt que silencieux). L'action `regler-effort` est retirée de la skill (en-tête et ligne de table) ;
l'exemption `low` du relecteur (C7) reçoit enfin l'effort qu'elle attendait, transmis par le bloc de
l'Étape 2.

**N0** : `node plugin/bin/n0.mjs` → quatre commandes vertes (`tester-hooks`, `tester-scripts`,
`tester-renvois`, `publier-dry-run`), rejoué après chaque tâche (T5, T6, T7).

**N1** : — (pas d'UI touchée).

**N2 à faire** : — (aucun N2 humain déclaré par ce `S<k>.md`).

**Écarts au plan** : aucun — les trois tâches ont été exécutées telles qu'écrites, sans correctif
localisé ni amendement. Version/`CHANGELOG.md`/publication restent hors zone : S5 s'en charge
(`plans/P7/index.md`).

**Prochaine action** : S4 (« Les textes disent ce que le code fait », `plans/P7/S4.md`) — elle décrit
ce que S2 et S3 viennent de faire dans `WORKFLOW.md`, `/nouveau-plan` et les gabarits.

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle : non » du bandeau).

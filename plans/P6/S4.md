# P6 · S4 — La norme : `WORKFLOW.md` réécrit, `EXECUTANT.md`, `CLAUDE-BASE.md`

> **Modèle : Sonnet · effort : high · Vague : 4 (parallèle : non) — validation-humaine**
> **Environnement : indifférent**
> **Latitude : la formulation et l'ordre des paragraphes ; jamais le fond d'une règle.**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Design fixé — décision d'entrée, C3 à C7.
> Tu t'arrêtes sur un choix que tu n'as pas reçu : `EXECUTANT.md`.

- Date : 2026-09-17 · Branche : —

## Lire (commun à la session)
`plugin/EXECUTANT.md`, puis :
- `docs/decisions/2026-09-17-autonomie-par-defaut-etat-scripte-push-par-session.md` — en entier.
- `docs/analyses/2026-09-17-sondes-p6.md` — constats A et C.
- `plugin/WORKFLOW.md` — en entier (c'est l'objet de la session).
- `plugin/CLAUDE-BASE.md`, `plugin/hooks/plafonds.json`.
- `plugin/bin/n0.mjs`, `plugin/bin/prochaine-action.mjs` (écrit par S2 puis S10) — en-têtes USAGE seulement.

## Hors périmètre
Les skills et agents (vague 5). Les numéros de section `§1` à `§9c` **ne changent pas** : des
dizaines de renvois y pointent, et `tests/tester-renvois.mjs` les vérifie.

---

## T8 — `WORKFLOW.md` : la norme seule, sous 300 lignes

### Objectif
Même numérotation, nouvelles règles, plus d'historique : chaque « pourquoi » daté devient un renvoi
d'une ligne vers son `docs/decisions/`.

### Pourquoi
`EXECUTANT.md` y renvoie six fois : un exécutant consciencieux charge 709 lignes dont ~40 % racontent
des incidents. La norme doit se lire sans payer l'histoire.

### Décision clé
Ce qui **change de fond**, section par section :

| § | Changement |
| --- | --- |
| §1 | « Quand solliciter un humain » : les quatre critères d'arrêt de C5 en liste fermée ; régime ouvert orchestrable (C6) |
| §3 | selon la sonde A : agents `session-<effort>` **ou** effort annoncé une fois en tête d'orchestration ; l'orchestrateur est **Sonnet**, dit tel quel |
| §4a | `.revue.md` et `.echec.md` commités ; le repère `Revues:` disparaît ; `[x]!` reste |
| §4b | règle de push C3, en remplacement de « push groupé » ; séquentiel par défaut, parallèle sur option déclarée ; messages de commit d'une vague verrouillée dans l'`index.md` |
| §5 | huit agents (retrait de `verificateur-n0`) ; N0 = `node .claude/workflow/bin/n0.mjs`, au premier plan comme toute commande ; revue par vague (C7) |
| §5b | l'état d'orchestration vient de `prochaine-action.mjs` ; retirer « l'orchestrateur tourne sur Haiku » |
| §7 | table des hooks à jour (gate de push, contrôle de version) ; plafond de `S<k>.echec.md` rapatrié dans `plafonds.json` |
| §8 | anti-patterns réalignés (retirer ceux que la nouvelle norme contredit : « lire hors liste », « reporter le push ») |
| §9a | budget en **hypothèses** ; amendement de session ; `Auto : oui` écrit par la session ; lecture ouverte |
| §9c | table « ce qui arrive → ce qui suit » : lignes amendement, exploration ouverte, bloquant de revue corrigeable |

Ce qui **ne change pas** : §2, §3b, §6, §9b, le vocabulaire gate / question / contrainte d'outillage.

### Lire / Modifier
`plugin/WORKFLOW.md`, `plugin/hooks/plafonds.json` (ajout du plafond `.echec.md` : 40 lignes — si
`lib.mjs` ne sait pas l'appliquer à un motif de chemin, l'écrire comme donnée et le signaler au bilan).

### Étapes
1. Recenser les renvois entrants : `grep -rn "WORKFLOW.md\` §" plugin/` — chaque ancre citée doit
   survivre avec le même numéro.
2. Réécrire section par section ; un paragraphe « Pourquoi / constat du … » → une ligne
   « Pourquoi : `docs/decisions/<fichier>` » (la décision existe déjà pour chacun ; sinon, le dire
   au bilan plutôt que d'en inventer une).
3. Appliquer la table ci-dessus. Chemin des scripts : `.claude/workflow/bin/` dans un projet
   vendoré, `plugin/bin/` dans le dépôt source — le dire une fois, en §5.
4. `wc -l plugin/WORKFLOW.md` < 300.

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` → OK ; `node plugin/bin/publier.mjs --dry-run` → code 0.
- **Tests** : — (prose ; les renvois sont la gate).
- **N2 humain** : relecture de `WORKFLOW.md` par l'utilisateur — une règle manque-t-elle, une
  formulation trahit-elle la décision ? (c'est la `validation-humaine` de la vague 4).
- **Anti-raccourci** : passer sous 300 lignes en **supprimant une règle** plutôt que son historique.
  Contrôle : chaque ligne des tables « un statut, un endroit », « nature », « ce qui arrive » de
  l'ancien fichier a un équivalent ou une ligne de la table ci-dessus qui la retire.

### Si bloqué
Une règle ancienne contredit la décision d'entrée **sans** être dans la table ci-dessus → c'est un
choix non reçu : la lister au bilan, garder l'ancienne formulation, continuer.

### Message de commit
`refactor(plugin): WORKFLOW.md — la norme seule ; autonomie par défaut, push par session`
`Plan: P6/S4/T8`

---

## T9 — `EXECUTANT.md` et `CLAUDE-BASE.md`

### Objectif
Les deux textes que **toute** session charge disent la nouvelle règle, sans dupliquer `WORKFLOW.md`.

### Étapes
1. `EXECUTANT.md` : « Lire » = point de départ, écriture bornée ; table des natures avec le budget
   en hypothèses et l'amendement ; délégation : retirer `verificateur-n0`, N0 = `n0.mjs` ; « Fin de
   tâche » : commit par tâche, **push en fin de session** sauf `wave.lock` ou sous-agent orchestré
   (l'orchestrateur pousse). Rester ≤ 70 lignes.
2. `CLAUDE-BASE.md` : mêmes retraits (`verificateur-n0`, « ne lit QUE ») ; « Validation » : N0 par
   script ; rester ≤ 90 lignes.

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `grep -rn "verificateur-n0" plugin/WORKFLOW.md
  plugin/EXECUTANT.md plugin/CLAUDE-BASE.md` → vide.
- **Anti-raccourci** : `—`

### Message de commit
`refactor(plugin): EXECUTANT et CLAUDE-BASE alignés sur la norme 0.39`
`Plan: P6/S4/T9`

---

## Bilan de session

- **T8 — fait, N0 vert.** `plugin/WORKFLOW.md` : 710 → 272 lignes, même numérotation `§1` à `§9c`
  (vérifié par `node tests/tester-renvois.mjs` → OK, aucune ancre orpheline). Table du plan appliquée
  section par section : §1 quatre critères d'arrêt en liste fermée + régime ouvert orchestrable
  (C6) ; §3 effort d'agent non vérifiable (sonde A, NON SONDABLE) ⇒ pas d'agents `session-<effort>`,
  effort de vague annoncé une fois, orchestrateur explicitement **Sonnet** (repris aussi en §5b, où
  « l'orchestrateur tourne sur Haiku » a été retiré) ; §4a `.revue.md`/`.echec.md` commités, `[x]!`
  conservé ; §4b règle de push C3 en remplacement du push groupé, séquentiel par défaut/parallèle
  déclaré (C7), messages de commit d'une vague verrouillée dans l'`index.md` ; §5 huit agents
  (`verificateur-n0` retiré), N0 = script `n0.mjs` au premier plan, revue par vague (C7) ; §5b état
  d'orchestration dérivé de `prochaine-action.mjs` (C2) ; §7 table des hooks à jour sur le
  comportement réel post-S3 (gate de push et contrôle de version déjà implémentés dans
  `stop-contexte.mjs`/`sessionstart-contexte.mjs`, lus pour vérifier avant d'écrire) ; §8
  anti-patterns réalignés (fusion des deux anti-patterns de push, retrait du build verbeux de
  l'exemple d'agent mécanique puisque `n0.mjs` se lance en direct) ; §9a budget en hypothèses,
  amendement de session, `Auto : oui` auto-écrit, lecture ouverte ; §9c trois lignes ajoutées
  (amendement, exploration ouverte, bloquant de revue corrigeable). Correction consécutive hors
  table (référence stale, pas un choix de conception) : §9c point 2 du canal de reprise citait encore
  `verificateur-n0` pour juger N0 — remplacé par `n0.mjs`, cohérent avec le retrait de l'agent en §5.
  `plugin/hooks/plafonds.json` : entrée `plans/P<n>/S<k>.echec.md: 40` ajoutée **comme donnée**
  (`_comment_echec` explique pourquoi) — `depassements()` de `lib.mjs` ne compare qu'un chemin fixe
  et ne peut pas encore résoudre ce motif variable ; signalé ici plutôt que corrigé, `lib.mjs` étant
  hors périmètre de cette session. Validé : `node tests/tester-renvois.mjs` → OK,
  `node plugin/bin/publier.mjs --dry-run` → code 0 (avant et après l'édition de `plafonds.json`).
- **T9 — fait, N0 vert.** `plugin/EXECUTANT.md` (69 lignes, ≤70) : lecture ouverte (liste « Lire » =
  point de départ), table des natures étendue avec la ligne `amendement` et le budget en hypothèses,
  table de délégation réduite à trois agents + `n0.mjs` (script, jamais un agent), fin de tâche =
  commit par tâche puis **push en fin de session**, exemptions `wave.lock`/sous-agent orchestré.
  `plugin/CLAUDE-BASE.md` (89 lignes, ≤90) : ligne N0 auto pointe désormais sur le script `n0.mjs`
  au premier plan. Ni l'un ni l'autre ne contenait déjà `verificateur-n0` ou « ne lit QUE » au sens
  littéral avant cette session (recherché, absent) : le retrait attendu portait donc surtout sur la
  table de délégation d'`EXECUTANT.md`, faite ci-dessus. Validé :
  `node tests/tester-renvois.mjs` → OK ;
  `grep -rn "verificateur-n0" plugin/WORKFLOW.md plugin/EXECUTANT.md plugin/CLAUDE-BASE.md` → vide.
- **Aucun « Si bloqué »** : aucune règle de l'ancien `WORKFLOW.md` ne contredisait la décision
  d'entrée en dehors de la table de la tâche.
- **Latitude tenue** : formulation et ordre des paragraphes réarrangés pour tenir sous les plafonds
  (300/70/90 lignes) ; aucun fond de règle supprimé au-delà de ce que la table du plan demande —
  compression systématique de la prose « pourquoi » en renvois `docs/decisions/`, tables resserrées.
- Deux commits séparés (`Plan: P6/S4/T8`, `Plan: P6/S4/T9`), aucun push (règle C3 §4b appliquée en
  fin de session via `/fin-de-tache`).

## Fin de session
Dérouler `/fin-de-tache` (mode solo).

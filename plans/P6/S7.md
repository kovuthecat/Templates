# P6 · S7 — Cadrage : `/nouveau-plan`, `/cadrer`, `/maj-workflow`, `verificateur-plan`

> **Modèle : Sonnet · effort : medium · Vague : 5 (parallèle : oui, avec S5 et S6)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Design fixé — C4, C5, C6, C7.
> Tu t'arrêtes sur un choix que tu n'as pas reçu : `EXECUTANT.md`.

- Date : 2026-09-17 · Branche : —

## Lire (commun à la session)
`plugin/EXECUTANT.md`, puis :
- `docs/decisions/2026-09-17-autonomie-par-defaut-etat-scripte-push-par-session.md` — C4 à C7.
- `plugin/WORKFLOW.md` — §1, §4b, §9a (version réécrite par S4).
- `plugin/skills/nouveau-plan/SKILL.md` et ses deux `references/`, `plugin/skills/cadrer/SKILL.md`,
  `plugin/skills/maj-workflow/SKILL.md`, `plugin/agents/verificateur-plan.md` — en entier.
- `plugin/bin/prochaine-action.mjs` — le parseur d'index : **tout champ ajouté au squelette doit
  rester lisible par lui**.

## Hors périmètre
`plugin/skills/orchestrer-plan/` (S5), `plugin/skills/fin-de-tache/` (S6).

---

## T15 — `/nouveau-plan` et ses squelettes

### Objectif
Un plan neuf porte l'autonomie par défaut, sa version de workflow, un découpage par zone de contexte.

### Étapes
1. **Étape 0** : contrôle de version (C4) en premier geste ; déclencheurs d'exploration de C6 (dès
   la première prémisse comportementale non sondable).
2. **Étape 2** : regrouper par **lectures partagées** ; retirer « changement de modèle ou d'effort
   ⇒ séparer » et « toute tâche `high` seule » comme règles absolues (elles deviennent des indices) ;
   **séquentiel par défaut**, une vague « parallélisable » se justifie en une ligne (gain d'horloge
   attendu) ; type de session `exploration` (C6).
3. `squelette-session.md` : `Latitude` — défaut écrit « moyens libres ; objectif, contrats publics
   et zone du plan fixes », le cadreur **restreint** par exception ; « Lire » = point de départ ;
   bandeau `Régime : fixé | ouvert` (ouvert : branche, budget, réponse positive **et** négative) ;
   « Si bloqué » réduit aux quatre critères d'arrêt ; N0 = `n0.mjs`.
4. `squelette-index.md` : ligne `Workflow : v<x>` ; colonne ou sous-liste **Message de commit par
   tâche** pour les vagues parallèles (C7) — forme au choix, **à condition que
   `prochaine-action.mjs` parse toujours l'index** (rejouer ses tests).
5. Étape 6 : inchangée (le plan se pousse déjà).

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `node tests/tester-scripts.mjs` (le parseur lit le
  nouveau squelette — ajouter une fixture tirée du squelette modifié).
- **Anti-raccourci** : modifier le squelette sans fixture correspondante laisse les tests verts sur
  l'**ancien** format.

### Si bloqué
Le parseur ne lit pas la forme retenue pour les messages de commit → correctif localisé dans
`prochaine-action.mjs` (commit séparé), sinon choisir la forme qu'il lit déjà.

### Message de commit
`feat(skill): nouveau-plan — latitude par défaut, zones de contexte, session d'exploration`
`Plan: P6/S7/T15`

---

## T16 — `/cadrer` : sonder, et sortir plus tôt par la preuve

### Étapes
1. Étape 2 : la **sonde jetable** est autorisée (script dans le scratch, ou `n0.mjs`), rien ne
   s'écrit dans le dépôt ; le frontmatter `disallowed-tools: Bash, PowerShell` est retiré — il ne
   valait que pour un tour (décision, « Vérifié »), et il interdit précisément la sonde.
2. « Protocole de preuve » : signal d'entrée élargi (C6) ; la preuve est **orchestrable** (session
   `exploration`, sous-agent, branche poussée) — retirer « au premier plan, avec un humain qui la lit ».

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs`.
- **Anti-raccourci** : `—`

### Message de commit
`feat(skill): cadrer peut sonder ; preuve orchestrable`
`Plan: P6/S7/T16`

---

## T17 — `/maj-workflow` et `verificateur-plan`

### Étapes
1. `/maj-workflow` : « Quand » = les frontières de C4 ; sans `DÉRIVE` → dérouler sans question et
   rapporter ; avec → question à options ; `correctifCritiqueDepuis` ; commit **et push**.
2. `verificateur-plan.md` : contrôles en plus — `Workflow : v` présent ; `Latitude` restreinte
   sans raison écrite ; vague parallèle sans justification ni messages de commit dans l'index ;
   session `exploration` sans branche, budget ou réponse négative.

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `node plugin/bin/publier.mjs --dry-run`.
- **Anti-raccourci** : `—`

### Message de commit
`feat(plugin): maj-workflow aux frontières de plan ; verificateur-plan étendu`
`Plan: P6/S7/T17`

---

## Bilan de session
<à compléter>

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle » du bandeau).

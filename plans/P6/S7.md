# P6 · S7 — Cadrage : `/nouveau-plan`, `/cadrer`, `/maj-workflow`, `verificateur-plan`

> **Modèle : Sonnet · effort : medium · Vague : 5 (parallèle : oui, avec S5 et S6)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Design fixé — C4, C5, C6, C7.
> Tu t'arrêtes sur un choix que tu n'as pas reçu : `EXECUTANT.md`.

- Date : 2026-09-17 · Branche : —

## Lire (commun à la session)
`plugin/EXECUTANT.md`, puis :
- `docs/decisions/2026-09-17-autonomie-par-defaut-etat-scripte-push-par-session.md` — C4 à C7.
- `plugin/WORKFLOW.md` — §1, §4b, §9a (version réécrite par S4).
- `plugin/skills/nouveau-plan/SKILL.md` et ses deux `references/`, `plugin/skills/cadrer/SKILL.md`,
  `plugin/skills/maj-workflow/SKILL.md`, `plugin/agents/verificateur-plan.md` — en entier.
- `plugin/bin/prochaine-action.mjs` — le parseur d'index : **tout champ ajouté au squelette doit
  rester lisible par lui**.

## Hors périmètre
`plugin/skills/orchestrer-plan/` (S5), `plugin/skills/fin-de-tache/` (S6).

---

## T15 — `/nouveau-plan` et ses squelettes

### Objectif
Un plan neuf porte l'autonomie par défaut, sa version de workflow, un découpage par zone de contexte.

### Étapes
1. **Étape 0** : contrôle de version (C4) en premier geste ; déclencheurs d'exploration de C6 (dès
   la première prémisse comportementale non sondable).
2. **Étape 2** : regrouper par **lectures partagées** ; retirer « changement de modèle ou d'effort
   ⇒ séparer » et « toute tâche `high` seule » comme règles absolues (elles deviennent des indices) ;
   **séquentiel par défaut**, une vague « parallélisable » se justifie en une ligne (gain d'horloge
   attendu) ; type de session `exploration` (C6).
3. `squelette-session.md` : `Latitude` — défaut écrit « moyens libres ; objectif, contrats publics
   et zone du plan fixes », le cadreur **restreint** par exception ; « Lire » = point de départ ;
   bandeau `Régime : fixé | ouvert` (ouvert : branche, budget, réponse positive **et** négative) ;
   « Si bloqué » réduit aux quatre critères d'arrêt ; N0 = `n0.mjs`.
4. `squelette-index.md` : ligne `Workflow : v<x>` ; colonne ou sous-liste **Message de commit par
   tâche** pour les vagues parallèles (C7) — forme au choix, **à condition que
   `prochaine-action.mjs` parse toujours l'index** (rejouer ses tests).
5. Étape 6 : inchangée (le plan se pousse déjà).

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `node tests/tester-scripts.mjs` (le parseur lit le
  nouveau squelette — ajouter une fixture tirée du squelette modifié).
- **Anti-raccourci** : modifier le squelette sans fixture correspondante laisse les tests verts sur
  l'**ancien** format.

### Si bloqué
Le parseur ne lit pas la forme retenue pour les messages de commit → correctif localisé dans
`prochaine-action.mjs` (commit séparé), sinon choisir la forme qu'il lit déjà.

### Message de commit
`feat(skill): nouveau-plan — latitude par défaut, zones de contexte, session d'exploration`
`Plan: P6/S7/T15`

---

## T16 — `/cadrer` : sonder, et sortir plus tôt par la preuve

### Étapes
1. Étape 2 : la **sonde jetable** est autorisée (script dans le scratch, ou `n0.mjs`), rien ne
   s'écrit dans le dépôt ; le frontmatter `disallowed-tools: Bash, PowerShell` est retiré — il ne
   valait que pour un tour (décision, « Vérifié »), et il interdit précisément la sonde.
2. « Protocole de preuve » : signal d'entrée élargi (C6) ; la preuve est **orchestrable** (session
   `exploration`, sous-agent, branche poussée) — retirer « au premier plan, avec un humain qui la lit ».

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs`.
- **Anti-raccourci** : `—`

### Message de commit
`feat(skill): cadrer peut sonder ; preuve orchestrable`
`Plan: P6/S7/T16`

---

## T17 — `/maj-workflow` et `verificateur-plan`

### Étapes
1. `/maj-workflow` : « Quand » = les frontières de C4 ; sans `DÉRIVE` → dérouler sans question et
   rapporter ; avec → question à options ; `correctifCritiqueDepuis` ; commit **et push**.
2. `verificateur-plan.md` : contrôles en plus — `Workflow : v` présent ; `Latitude` restreinte
   sans raison écrite ; vague parallèle sans justification ni messages de commit dans l'index ;
   session `exploration` sans branche, budget ou réponse négative.

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `node plugin/bin/publier.mjs --dry-run`.
- **Anti-raccourci** : `—`

### Message de commit
`feat(plugin): maj-workflow aux frontières de plan ; verificateur-plan étendu`
`Plan: P6/S7/T17`

---

## Bilan de session

**Fichiers modifiés** :
- `plugin/skills/nouveau-plan/SKILL.md` (T15) — Étape 0 : contrôle de version C4 en premier geste,
  déclencheur d'exploration C6 avancé à la première prémisse comportementale non sondable. Étape 2 :
  regroupement par lectures partagées d'abord, `high`/`xhigh` et changement de modèle/effort
  redevenus des indices (pas des règles absolues de séparation), séquentiel par défaut avec
  justification d'une ligne pour toute vague parallèle, session de type `exploration` ajoutée.
  Correctif localisé mineur en passant : « les quatre contrôles » → « les dix contrôles » (Étape 4b),
  pour rester cohérent avec T17.
- `plugin/skills/nouveau-plan/references/squelette-index.md` (T15) — ligne `Workflow : v<x>` ajoutée ;
  colonne `Message de commit` ajoutée **après** `Statut` (C7) — position choisie précisément pour ne
  pas décaler `cellules[8]` dans `prochaine-action.mjs`, qui lit les colonnes par position fixe.
- `plugin/skills/nouveau-plan/references/squelette-session.md` (T15) — bandeau `Régime : fixé |
  ouvert` ajouté ; `Latitude` par défaut réécrite (« moyens libres ; objectif, contrats publics et
  zone du plan fixes »), restriction desormais justifiée en une ligne ; « Lire » requalifié en point
  de départ (lecture ouverte, C5) ; « Si bloqué » réduit aux quatre critères d'arrêt de
  `WORKFLOW.md` §1 ; N0 auto pointe explicitement `n0.mjs` (C1).
- `plugin/skills/cadrer/SKILL.md` (T16) — frontmatter : `disallowed-tools: Bash, PowerShell` retiré
  (ne valait que pour le tour d'invocation et bloquait la sonde). Étape 2 : sonde jetable autorisée
  (script du scratch ou `n0.mjs`, aucune écriture dans le dépôt). Protocole de preuve : signal
  d'entrée élargi aux quatre déclencheurs de C6 ; la preuve est désormais orchestrable (session
  `exploration`, sous-agent, branche poussée) au lieu de « premier plan, humain qui lit ».
- `plugin/skills/maj-workflow/SKILL.md` (T17) — réécrite : déclenchement aux frontières de C4
  (`/nouveau-plan` Étape 0, `/orchestrer-plan` avant sa 1ʳᵉ vague, `/nouveau-projet`,
  `/migrer-projet`) plutôt que périodique ; sans `DÉRIVE` → synchronise sans poser de question,
  rapporte après ; avec `DÉRIVE` → question à options (Remonter/Écraser) au lieu d'un arbitrage
  silencieux ; `correctifCritiqueDepuis` documenté ; fin de skill : commit **et push** (C3).
- `plugin/agents/verificateur-plan.md` (T17) — quatre contrôles ajoutés (7 à 10) : `Workflow : v`
  absent, `Latitude` restreinte sans raison, vague parallèle sans justification ni messages de
  commit, session `exploration` sans branche/budget/réponse négative. En-têtes et description
  frontmatter mis à jour (« six » → « dix » contrôles).
- `tests/fixtures/plans/squelette/index.md` (T15, anti-raccourci) — copie mise à jour du nouveau
  squelette-index.md (ligne `Workflow` + colonne `Message de commit`), pour que la fixture ne fige
  pas l'ancien format.
- `tests/tester-scripts.mjs` (T15, anti-raccourci) — le cas « fixture du squelette » vérifie
  maintenant `json.workflow === '<x>'` et que `statutBrut` de S1 reste `[ ]` (colonne ajoutée après
  Statut, sans décaler sa lecture par position).

**Résumé** : les trois tâches de la session (T15 `/nouveau-plan`, T16 `/cadrer`, T17 `/maj-workflow`
et `verificateur-plan`) sont câblées sur les contrats C4 à C7 de
`docs/decisions/2026-09-17-autonomie-par-defaut-etat-scripte-push-par-session.md`, sans toucher à
`/orchestrer-plan` ni `/fin-de-tache` (hors périmètre, S5/S6).

**N0 lancé** (`verificateur-n0`, premier plan) :
- `node tests/tester-renvois.mjs` → PASS (relancé après le correctif ci-dessous → toujours PASS)
- `node tests/tester-scripts.mjs` → PASS (idem)
- `node plugin/bin/publier.mjs --dry-run` → PASS (inclut `tester-hooks.mjs` et `tester-renvois.mjs`)

**Observation, sans action de ma part** : un premier passage de `tester-renvois.mjs` (avant le
travail sur T16/T17) a rendu un FAIL isolé sur
`plugin/skills/fin-de-tache/references/bloc-de-relance.md` (assertion 3, « aucun appelant »).
Diagnostic avant conclusion (`WORKFLOW.md` §9a) : `git status` montrait `fin-de-tache/SKILL.md`
modifié et `fin-de-tache/references/` non suivi — travail en cours de S6, session sœur de cette
vague verrouillée, dans un arbre de travail partagé (pas de worktree). Lecture du fichier telle
qu'il existait à cet instant : la citation `references/bloc-de-relance.md` y était déjà présente ;
le FAIL était une lecture prise pendant l'écriture concurrente de S6, pas un défaut. Repassé quelques
minutes après (ci-dessus) : PASS. Aucun fichier de `fin-de-tache/` ni `orchestrer-plan/` touché —
hors périmètre de cette session.

**N1 visuel** : `—` (aucune UI touchée).

**N2 humain** : `—` (aucune ligne N2 dans les trois tâches).

**Écarts au plan** : aucun — pas de `Latitude` déclarée au bandeau de cette session, donc aucune
rubrique à ouvrir (mesure B3).

**Correctif localisé** : aucun hors périmètre. Le candidat de la § Observation ci-dessus s'est révélé
être un état transitoire d'une session sœur, pas un défaut.

**Revue de session (`relecteur-session`, premier plan)** : `plans/P6/S7.revue.md` — 1 bloquant
corrigé sur-le-champ, revue non mise à jour. Incohérence de vocabulaire introduite par cette session
même : `squelette-session.md`, `nouveau-plan/SKILL.md` (Étape 2) et `verificateur-plan.md`
(contrôle 10) exigeaient une branche jetable « nommée » pour une session `exploration`, alors que
C6 et `cadrer/SKILL.md` (édité par cette même session, T16) exigent une branche « poussée » —
l'exigence réelle pour qu'une preuve soit orchestrable (un sous-agent, une branche non poussée n'est
vue par personne). Corrigé dans les trois fichiers (« nommée et poussée », jamais `main`), N0 rejoué
(`tester-renvois.mjs`, `tester-scripts.mjs`) → PASS. Le `.revue.md` reste tel que déposé par l'agent
(`Bloquant : 1`) : c'est ce bilan qui porte la correction, comme `plans/P6/S4.md` avant elle.

**Session sous `.claude/wave.lock`** (vague 5, parallèle avec S5 et S6) : aucun commit, aucun push
effectué — refusés par `pretooluse-git.mjs` tant que le verrou tient. Le diff des trois tâches reste
dans l'arbre de travail courant, avec les messages de commit déjà écrits sous chaque `T<n>`
ci-dessus ; l'orchestrateur committera tâche par tâche à la levée du verrou, en fin de vague
(`plugin/skills/fin-de-tache/references/vague-parallele.md`).

**Prochaine action** : attendre la collecte de la vague 5 (S5, S6, S7) par `/orchestrer-plan`, qui
lève `.claude/wave.lock`, committe les trois tâches ci-dessus avec leurs messages, puis déclenche la
revue de vague (`relecteur-session`) avant la vague suivante de `plans/P6/index.md`.

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle » du bandeau).

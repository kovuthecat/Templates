# P6 · S9 — Clôture : version, CHANGELOG, publication taguée

> **Modèle : Haiku · effort : low · Vague : 7 (parallèle : non)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Tu t'arrêtes sur un choix que tu n'as pas reçu : `EXECUTANT.md`.

- Date : 2026-09-17 · Branche : —

## Lire (commun à la session)
`plugin/EXECUTANT.md`, puis :
- `CHANGELOG.md` — les trois dernières entrées (forme).
- `plans/P6/index.md` — objectif et lignes « en clair » : matière de l'entrée de CHANGELOG.
- `plugin/skills/fin-de-tache/references/fin-de-plan.md` — la procédure de clôture à dérouler.

## Hors périmètre
Tout le reste de `plugin/`.

---

## T21 — Version 0.39.0 et CHANGELOG

### Étapes
1. `plugin/.claude-plugin/plugin.json` : `"version": "0.39.0"`.
2. `CHANGELOG.md` : entrée 0.39.0 — six puces, celles de « Ce que ça change » de la décision
   d'entrée ; une ligne « Migration : `plugin/MIGRATION.md` ».

### Validation
- **N0 auto** : `node tests/tester-hooks.mjs && node tests/tester-scripts.mjs && node tests/tester-renvois.mjs` ;
  `node plugin/bin/publier.mjs --dry-run` → annonce `tag v0.39.0`.
- **Anti-raccourci** : `—`

### Message de commit
`chore(plugin): 0.39.0 — autonomie par défaut, état scripté, push par session`
`Plan: P6/S9/T21`

---

## T22 — Clôture du plan et publication

### Étapes
1. Dérouler la fin de plan (tri des revues, statuts, aucun `[x]!` restant).
2. `git pull --rebase`, `git push`.
3. `node plugin/bin/publier.mjs` ; vérifier `git ls-remote --tags https://github.com/kovuthecat/claude-workflow`
   → contient `v0.39.0`.

### Validation
- **N0 auto** : le `ls-remote` du point 3.
- **Anti-raccourci** : publier **avant** le push du dépôt source — `publier.mjs` refuse un arbre en
  retard, pas un arbre en avance.

### Si bloqué
Un `[x]!` dont le bloquant n'est ni corrigé ni versé : c'est un choix (corriger avant de publier,
ou publier avec le défaut connu) → question à l'utilisateur.

### Message de commit
`chore(plans): clôture P6`
`Plan: P6/S9/T22`

---

## Bilan de session
<à compléter>

## Fin de session
Dérouler `/fin-de-tache` (mode solo).

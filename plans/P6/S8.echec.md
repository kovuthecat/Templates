# S8 — échec du 2026-09-17

Nature : environnement
Tentatives : reprise=0 enquete=0
Blocage : activer un bac à sable shell sur ce poste (Windows Sandbox/WSL, admin + redémarrage) —
geste humain, hors portée d'une session.
Auto : non

## Tâche visée
T19, étape « N0 auto : `claude plugin eval` sur les trois suites » (T18 fait et commité, T20 pas
démarré).

## Où ça a cassé
`claude plugin eval --allow-tools Bash --case "executant-n0-premier-plan" plugin` → « sandbox
required but unavailable : ... Windows sandbox is not active (feature gate off) ... refusing to
start without a working sandbox. » Sans `--allow-tools`, Bash refusé à chaque appel : `max_turns`
épuisé sans rien accomplir. Incident (remédiation détaillée) :
`docs/workflow/incidents/2026-09-17-sandbox-windows-absent-claude-plugin-eval.md`.

## Ce qu'il faudrait pour que ça passe
Depuis un poste avec bac à sable actif : `claude plugin eval --trust-plugin --no-publish
--allow-tools Bash Write Edit --case "<nom>" plugin`, une fois par suite, la 3ᵉ
(`orchestrateur-suit-le-script`) deux fois (`--model haiku`, `--model sonnet`), puis écrire
`docs/analyses/2026-09-<jj>-evals-p6.md`.

## État laissé derrière
- Fichiers modifiés non commités : aucun.
- Les trois suites d'éval sont écrites et commitées (WIP), jamais exécutées avec succès ; le doc
  d'analyse reste à écrire, T20 n'a pas démarré.
- **Faut-il annuler quelque chose avant de reprendre ?** Non.

## Déjà écarté
- Sans `--allow-tools` : Bash refusé à chaque appel, aucune donnée utile (0,47 $ perdus) — ne pas répéter.
- Réglage `sandbox.failIfUnavailable` : absent des settings ; `claude config get` n'est pas une
  vraie sous-commande de ce CLI (a lancé une session complète pour rien).
- WSL en contournement rapide : non installé, même famille de changement système — non tranché ici.

## Hypothèse en cours
Aucun poste connu de ce dépôt n'a de bac à sable actif pour `claude plugin eval` — à confirmer sur
le prochain poste qui tente cette étape.

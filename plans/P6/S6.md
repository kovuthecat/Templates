# P6 · S6 — `/fin-de-tache` scindée, `/reprendre-echec`, revue par vague, retrait de `verificateur-n0`

> **Modèle : Sonnet · effort : medium · Vague : 5 (parallèle : oui, avec S5 et S7)**
> **Environnement : indifférent**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Design fixé — C1, C3, C5, C7.
> Tu t'arrêtes sur un choix que tu n'as pas reçu : `EXECUTANT.md`.

- Date : 2026-09-17 · Branche : —

## Lire (commun à la session)
`plugin/EXECUTANT.md`, puis :
- `docs/decisions/2026-09-17-autonomie-par-defaut-etat-scripte-push-par-session.md` — C1, C3, C5, C7.
- `plugin/WORKFLOW.md` — §4a, §4b, §5, §9a (version réécrite par S4).
- `plugin/skills/fin-de-tache/SKILL.md`, `plugin/skills/reprendre-echec/SKILL.md`,
  `plugin/agents/relecteur-session.md` — en entier.

## Hors périmètre
`plugin/skills/orchestrer-plan/` (S5), `plugin/skills/nouveau-plan/` (S7).

---

## T12 — `/fin-de-tache` : un cœur et trois annexes

### Objectif
`SKILL.md` ≤ 70 lignes (« après chaque tâche » + fin de session), le reste en `references/` ouvertes
seulement quand le cas se présente.

### Pourquoi
267 lignes chargées à chaque session, dont la fin de plan, le mode parallèle et le point propre au
dépôt source — que neuf sessions sur dix n'utilisent pas.

### Lire / Modifier
`plugin/skills/fin-de-tache/SKILL.md` ; créer `references/fin-de-plan.md`,
`references/bloc-de-relance.md`, `references/vague-parallele.md`.

### Étapes
1. Cœur : N0 par `node .claude/workflow/bin/n0.mjs` ; N1 ; bilan ; commit ; incident ; statut ;
   **push** (C3 : `pull --rebase` puis `push`, sauf `wave.lock` ou sous-agent orchestré ; N0 rouge
   à l'interruption → branche `wip/P<n>-S<k>`). Plus de lancement de revue par la session.
2. `bloc-de-relance.md` : gabarit actuel + les deux lignes `Commit :` / `Branche :` et le premier
   geste `git merge-base --is-ancestor` côté session suivante. Reste le **domicile** de la règle :
   vérifier que les renvois des autres skills (`cadrer`, `orchestrer-plan`) pointent le nouveau chemin.
3. `fin-de-plan.md` : tri des revues **commitées** (versement dans `TASKS.md`, suppression dans le
   même commit, plus de repère `Revues:`) ; le point 8b (dépôt source) y descend.
4. `vague-parallele.md` : le mode sous verrou, tel quel.

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `wc -l SKILL.md` ≤ 70 ;
  `grep -rn "verificateur-n0\|Revues:" plugin/skills/fin-de-tache/` → vide.
- **Anti-raccourci** : une annexe jamais citée depuis le cœur n'est jamais lue — chaque
  `references/*.md` a sa ligne « ouvrir quand … » dans `SKILL.md`.

### Message de commit
`refactor(skill): fin-de-tache — cœur de 70 lignes, push en fin de session`
`Plan: P6/S6/T12`

---

## T13 — `/reprendre-echec` : hypothèses, `Auto` par la session, amendement

### Étapes
1. Gabarit `.echec.md` : `Auto :` peut être écrite par la session en échec (mêmes critères) ;
   le fichier est **commité et poussé**.
2. « Avant d'écrire » : budget en hypothèses distinctes (C5), « Déjà écarté » rempli au fil de l'eau.
3. Étape 3 : l'amendement de session (C5) passe **avant** l'aiguillage vers `/nouveau-plan` extension.
4. N0 par `n0.mjs`. Retirer ce que `prochaine-action.mjs` porte désormais (modèle de reprise).

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs`.
- **Anti-raccourci** : `—`

### Message de commit
`refactor(skill): reprendre-echec — budget en hypothèses, amendement, Auto par la session`
`Plan: P6/S6/T13`

---

## T14 — Revue par vague ; suppression de `verificateur-n0`

### Étapes
1. `relecteur-session.md` : périmètre = les sessions d'une **vague** (`git log --grep "P<n>/S<k>/"`
   pour chacune), un `.revue.md` **par session** quand même (le statut `[x]!` reste par session),
   sessions `low` sautées ; correctifs localisés et amendements relus en premier.
2. Supprimer `plugin/agents/verificateur-n0.md` ; remplacer ses citations dans
   `plugin/skills/verif-visuelle/SKILL.md` et partout où `grep -rln "verificateur-n0" plugin/`
   en trouve **hors** des zones de S5, S7 et S8 (celles-là s'en chargent).

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` (l'assertion « skill qui cite un agent absent »
  doit rester verte) ; `node plugin/bin/publier.mjs --dry-run`.
- **Anti-raccourci** : le test de renvois passe tant qu'**un** fichier cite encore l'agent
  supprimé dans une zone qu'il n'inspecte pas — lister au bilan les citations restantes et leur session.

### Si bloqué
Une citation de `verificateur-n0` dans une zone d'une session parallèle (S5, S7) : ne pas y
toucher, le noter au bilan — S8/T20 fait la passe finale.

### Message de commit
`refactor(plugin): revue par vague ; agent verificateur-n0 retiré`
`Plan: P6/S6/T14`

---

## Bilan de session
<à compléter>

## Fin de session
Dérouler `/fin-de-tache` (mode selon « parallèle » du bandeau).

# P6 · S8 — Périphérie : gabarits, migration, README, évals

> **Modèle : Sonnet · effort : medium · Vague : 6 (parallèle : non)**
> **Environnement : indifférent** (les évals demandent Claude Code ≥ 2.1.269 — vérifier en premier geste)
> Exécutant : UNIQUEMENT les tâches ci-dessous. Tu t'arrêtes sur un choix que tu n'as pas reçu : `EXECUTANT.md`.

- Date : 2026-09-17 · Branche : —

## Lire (commun à la session)
`plugin/EXECUTANT.md`, puis :
- `docs/decisions/2026-09-17-autonomie-par-defaut-etat-scripte-push-par-session.md` — C1, C3, C4, C7.
- `plugin/templates/project-settings.json`, `plugin/MIGRATION.md`, `plugin/README.md`.
- `plugin/skills/nouveau-projet/SKILL.md`, `plugin/skills/migrer-projet/SKILL.md` — les points qui
  posent settings, `.gitignore` et commandes.
- `plugin/evals/orchestrer-plan-lancer-vague/` — forme d'une éval existante (prompt + graders).
- Les bilans de S5, S6 et S7 — citations de `verificateur-n0` laissées pour cette session.

## Hors périmètre
Le fond des skills réécrites en vague 5.

---

## T18 — Gabarits, amorçage, migration

### Étapes
1. `project-settings.json` : `permissions.allow` pour `node .claude/workflow/bin/n0.mjs` et
   `prochaine-action.mjs`, `git push`, `git pull --rebase` (une session pousse désormais).
2. `/nouveau-projet`, `/migrer-projet` : créer `.claude/n0.json` depuis les commandes cadrées ;
   `.gitignore` : ajouter `.claude/n0/` ; **ne pas** ignorer `*.revue.md` ni `*.echec.md`.
3. `MIGRATION.md` : « vers 0.39.0 » — créer `n0.json`, committer les `.revue.md` en attente,
   pousser, plans en cours (finis sur leur version, sauf correctif critique). **Le dire en tête** :
   un projet vendoré ≤ 0.38.1 exécute l'ancien hook `SessionStart`, sans contrôle de version — la
   première `/maj-workflow` reste manuelle, c'est elle qui arme le contrôle.
4. `README.md` : huit agents (ou onze si la sonde A a créé `session-*`), scripts `bin/`, tags.

### Validation
- **N0 auto** : `node tests/tester-renvois.mjs` ; `node plugin/bin/publier.mjs --dry-run` ;
  `node -e "JSON.parse(require('fs').readFileSync('plugin/templates/project-settings.json','utf8'))"`.
- **Anti-raccourci** : `—`

### Message de commit
`feat(plugin): gabarits et migration 0.39 — n0.json, permissions de push`
`Plan: P6/S8/T18`

---

## T19 — Trois évals

### Objectif
Rejouer les deux pannes mesurées, et comparer Haiku à Sonnet en orchestrateur.

### Pourquoi
38 versions et 16 publications correctives en 22 jours : l'écrit se teste avant de se publier. Et
la bascule de l'orchestrateur vers Haiku est conditionnée à cette mesure (décision, « Écarté »).

### Étapes
1. `plugin/evals/executant-n0-premier-plan/` : une session d'exécution rend-elle sa ligne
   `VERDICT:` **après** N0, sans rien laisser en arrière-plan ?
2. `plugin/evals/premisse-mesuree-amendement/` : prémisse fausse avec mesure, objectif inchangé →
   la session amende et continue (grader : pas de `.echec.md`, un commit `Amendement :`).
3. `plugin/evals/orchestrateur-suit-le-script/` : sur une fixture avec un `FAIL` exécution puis une
   prémisse, la suite des gestes est-elle celle que `prochaine-action.mjs` rend ? À lancer **deux
   fois**, Haiku et Sonnet.
4. `docs/analyses/2026-09-<jj>-evals-p6.md` : tableau des résultats, et la recommandation Haiku/Sonnet.

### Validation
- **N0 auto** : `claude plugin eval` sur les trois suites → rapport produit (un rouge est un
  **résultat** à consigner, pas un échec de session).
- **Anti-raccourci** : un grader qui ne vérifie que la présence d'une ligne `VERDICT:` passe sur un
  verdict rendu avant N0 — il doit vérifier l'**ordre**.

### Si bloqué
`claude --version` < 2.1.269 → `FAIL` environnement, remédiation nommée (mise à jour), rien d'écrit.

### Message de commit
`test(evals): N0 au premier plan, amendement, orchestrateur Haiku vs Sonnet`
`Plan: P6/S8/T19`

---

## T20 — Passe finale de cohérence

### Étapes
1. `grep -rn "verificateur-n0\|Revues:\|push est groupé\|tourne sur Haiku\|ne lit QUE" plugin/` → vide,
   ou chaque occurrence justifiée au bilan.
2. `node tests/tester-hooks.mjs && node tests/tester-scripts.mjs && node tests/tester-renvois.mjs`.
3. `wc -l` : `WORKFLOW.md` < 300, `fin-de-tache/SKILL.md` ≤ 70, `orchestrer-plan/SKILL.md` < 180.

### Validation
- **N0 auto** : les trois commandes du point 2, vertes.
- **Anti-raccourci** : `—`

### Message de commit
`chore(plugin): passe de cohérence 0.39`
`Plan: P6/S8/T20`

---

## Bilan de session

**Périmètre livré : T18 seul.** T19 et T20 sont **abandonnés pour la publication 0.39.0**
(décision utilisateur du 2026-09-17) — pas un correctif, un renoncement assumé.

- **T18** — fait et commité (`7041835`), N0 revérifié à la clôture de session : vert.
- **T19** (trois évals) — abandonné. Cause : aucun bac à sable Windows disponible sur ce poste pour
  `claude plugin eval` (feature gate Windows Sandbox off, WSL non installé), qui refuse tout cas
  utilisant Bash/PowerShell. Trace : `docs/workflow/incidents/2026-09-17-sandbox-windows-absent-claude-plugin-eval.md`.
  Rapport d'échec de session correspondant (`S8.echec.md`) supprimé — l'échec est résolu par
  décision, pas par correction.
- **T20** (passe finale de cohérence) — abandonné, n'a jamais démarré ; dépendait de T19.
- **Suites d'évals WIP** — **conservées**, pas supprimées : `plugin/evals/executant-n0-premier-plan/`,
  `plugin/evals/premisse-mesuree-amendement/`, `plugin/evals/orchestrateur-suit-le-script/` sont
  écrites (prompts + graders) mais **jamais exécutées avec succès** — aucun run n'a dépassé le refus
  de bac à sable. Elles ne valident donc rien à ce jour et ne doivent pas être citées comme preuve
  d'un comportement testé tant qu'un poste avec bac à sable actif (Windows Sandbox, WSL, ou
  Linux/macOS) ne les a pas rejouées.
- `docs/analyses/2026-09-<jj>-evals-p6.md` (comparaison Haiku/Sonnet en orchestrateur) — **non
  écrit**, dépendait des résultats de T19. La bascule de l'orchestrateur vers Haiku reste donc non
  mesurée ; la décision d'entrée du plan la gardait « Écartée » faute de mesure, ce qui ne change
  pas avec cet abandon.
- Pas de correctif localisé nécessaire, pas de dépendance ajoutée.

## Fin de session
Dérouler `/fin-de-tache` (mode solo).

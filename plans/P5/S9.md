# P5 · S9 — Clôture : version, CHANGELOG, publication   (rédigé par Opus)

> **Modèle : Haiku · effort : low · Vague : 5 (parallèle : non)**
> **Environnement : poste du mainteneur (publication vers le miroir public : `git push --force`)**
> **Lancement : `claude --model claude-haiku-4-5` · effort : défaut du modèle (`low`), rien à régler**
> **Latitude : aucune.**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `plugin/EXECUTANT.md`.

- Date : 2026-09-15 · Branche : main

## Lire (commun à la session)

`plugin/EXECUTANT.md` (toujours en premier), puis :

- `plans/P5/index.md` — table des sessions et vague 5.
- `CHANGELOG.md` — les 5 dernières entrées, pour le **format** d'une ligne.
- `plugin/.claude-plugin/plugin.json` — le champ `version`.
- `plugin/bin/publier.mjs` — l. 11-35 (`USAGE`, conditions d'entrée : tests, renvois, synchro
  source).
- `plans/P4/S6.md` — en entier : la clôture précédente, même déroulé.

## Hors périmètre

- **Aucune modification de `plugin/`** au-delà du numéro de version. Un défaut trouvé ici est un
  `FAIL` qui nomme la session responsable, pas une correction.
- La synchronisation des projets vendorés (`/maj-workflow` dans chaque projet) : geste du
  mainteneur, après cette session.
- Les statuts `[x]` des sessions S1-S8 : posés par chacune à sa clôture (ou par l'orchestrateur) ;
  ici on **vérifie** qu'ils sont là, on ne les pose pas à leur place.

---

## T28 — Version et CHANGELOG

### Objectif
`plugin.json` passe à `0.37.0` ; une ligne datée dans `CHANGELOG.md` qui résume P5.

### Pourquoi
Le CHANGELOG est ce que `MIGRATION.md` et `/maj-workflow` lisent pour mesurer le retard d'un projet
vendoré. Sans ligne, la version bouge sans que personne sache ce qu'elle apporte.

### Décision clé
Une ligne, format existant, contenu : « **Réflexion outillée** (plugin `workflow` 0.37.0, P5) —
`allowed-tools` corrigé (`disallowed-tools`, un tour) ; gabarits sans stack ; `/nouveau-projet`
contraintes avant stack ; annexes `cadrer/references/` (grille, six fiches et `NONE`, rechercher
l'existant) ; hypothèses typées et `Risques du plan` ; ligne `Mesure :` du rapport d'échec ; agents
`critique-plan` (Opus, plan architectural, avant approbation) et `analyste-flux` (Sonnet) ;
`tester-renvois` à six assertions ; trois évals de déclenchement (≥ 2.1.269). Décision
`2026-09-15-reflexion-outillee-critique-et-flux.md`. » Si S8 s'est arrêtée sur la contrainte de
version, la ligne le dit : « évals écrites, non lancées : Claude Code < 2.1.269 ».

### Lire / Modifier
- Modifier : `plugin/.claude-plugin/plugin.json`, `CHANGELOG.md`.

### Étapes
1. `"version": "0.37.0"`.
2. La ligne, en tête de liste si le fichier est antéchronologique, sinon à sa place datée.

### Validation
- **N0 auto (bloque le commit)** : `node -e "console.log(require('./plugin/.claude-plugin/plugin.json').version)"` → `0.37.0` ; `grep -c '0.37.0' CHANGELOG.md` → `1`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : —

### Si bloqué
`marketplace.json` porte aussi une version à aligner → l'aligner et le dire ; c'est mécanique.

### Message de commit
`chore(plugin): 0.37.0 — reflexion outillee, critique avant approbation, analyste de flux`
Dernière ligne : `Plan: P5/S9/T28`

---

## T29 — Publication et index

### Objectif
`publier.mjs` passe (tests, renvois, synchro source, scan de chemins), le miroir public reçoit
`0.37.0`, l'index P5 est cohérent.

### Pourquoi
Une version qui n'est pas publiée n'atteint aucun projet. Et l'index est la seule source du statut.

### Décision clé
Déroulé de P4/S6, à l'identique : `--dry-run` d'abord, lecture de ce qu'il affiche, puis
publication. Aucun `--force` ajouté à la main : c'est le script qui pousse, avec ses garde-fous.

### Lire / Modifier
- Modifier : `plans/P5/index.md` (statut S9, et vérification des autres).

### Étapes
1. `node plugin/bin/publier.mjs --dry-run` → payload propre, version `0.37.0`, aucun chemin
   personnel.
2. Vérifier l'index : S1-S8 en `[x]` ou `[x]!` avec date ; sinon `FAIL`, la session manquante nommée.
3. `node plugin/bin/publier.mjs` (publication réelle).
4. Cocher S9 dans l'index, commit, push sur `main`.

### Validation
- **N0 auto (bloque le commit)** : la publication rend `0` et affiche la version poussée ; `grep -c '\[x\]' plans/P5/index.md` → `9`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : le mainteneur lance `/maj-workflow` dans un projet vendoré et constate que
  `.claude/skills/cadrer/references/` arrive avec trois fichiers.
- **Anti-raccourci** : cocher S9 avant que la publication ait rendu `0`.

### Si bloqué
`verifierSynchroSource` refuse (dépôt local en retard sur `origin/main`) → `git pull` **n'est pas
un geste de cette session** : `FAIL`, nature `environnement`, avec le message du script.

### Message de commit
`chore(P5): cloture — publication 0.37.0, statuts`
Dernière ligne : `Plan: P5/S9/T29`

---

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : non).

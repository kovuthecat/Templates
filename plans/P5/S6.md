# P5 · S6 — (c)(d)(e)(f) `/nouveau-plan` : grille, existant, hypothèses typées, critique, flux   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 3 (parallèle : oui, avec S7)**
> **Environnement : indifférent**
> **Lancement : `claude --model claude-sonnet-5` · effort : défaut du modèle (`medium`), rien à régler**
> **Latitude : aucune — chaque insertion a son texte ou son contrat ci-dessous.**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Tu t'arrêtes sur un choix que tu n'as pas reçu, pas parce que
> quelque chose a cassé : `plugin/EXECUTANT.md`.

- Date : 2026-09-15 · Branche : main

## Lire (commun à la session)

`plugin/EXECUTANT.md` (toujours en premier), puis :

- `docs/decisions/2026-09-15-reflexion-outillee-critique-et-flux.md` — sections **(c) « Points
  d'appel »**, **(d)** première puce, **(e) « Déclenchement » et « Routage »**, **(f) « Appelants »**.
- `plugin/skills/nouveau-plan/SKILL.md` — en entier (240 lignes) : c'est le fichier de la session.
- `plugin/skills/nouveau-plan/references/squelette-index.md` — l. 1-12 (état après S2).
- `plugin/skills/cadrer/references/preparation.md` et `rechercher-existant.md` — en-tête et bloc
  de forme seulement (état après S4) : ce que `/nouveau-plan` **cite**, pas ce qu'il recopie.
- `plugin/agents/critique-plan.md` — frontmatter et « Ce que tu reçois » / « Ce que tu rends »
  (état après S5).
- `plugin/skills/orchestrer-plan/references/remediation.md` — l. 44-52 : le bloc `Agent({` du
  vérificateur de prémisse, **modèle du bloc à écrire en T20** (la ligne de renvoi en première ligne
  du prompt, au mot près).

## Hors périmètre

- Les agents (S5), les annexes (S4), le côté échec (S7 : `verificateur-premisse`, `remediation.md`,
  `reprendre-echec`, `WORKFLOW.md` §9c). Cette session ne touche que `/nouveau-plan` et son
  squelette d'index.
- `verificateur-plan` et l'Étape 4b : inchangés, sauf l'exemple de T17.
- Aucune étape obligatoire ajoutée à la voie courte : chaque insertion est **conditionnelle**
  (inconnue technique, plan architectural, hypothèse comportementale) ou une **relecture** de ce que
  la décision a déjà établi.

---

## T17 — L'exemple de dépendance, et la frontière direction / détails

### Objectif
Deux corrections de texte : l'exemple de l'Étape 4b dit « vague antérieure » ; l'en-tête distingue
direction structurante approuvée et détails techniques ouverts.

### Pourquoi
D4 : l'exemple actuel (« un fichier créé par une session antérieure de la même vague ») normalise
ce que le contrôle 4 de `verificateur-plan` refuse. R1b : la formule « approche déjà décidée »
peut interdire au planificateur une comparaison technique pourtant nécessaire.

### Décision clé
Décision (a) et (c). Étape 4b : « un fichier créé par une session d'une **vague antérieure**, par
exemple ». En-tête, après « **Le QUOI et le POURQUOI doivent déjà être tranchés.** … » ajouter :
« Ce qui est tranché, c'est la **direction structurante** — objectif, frontière, choix approuvés par
l'utilisateur : elle ne se rediscute pas ici. Les **détails techniques encore ouverts** — quel
composant, quelle bibliothèque, quel ordre de migration — se comparent (Étape 1, point 4bis). Une
comparaison qui contredit une décision structurante n'y touche pas : elle écrit la preuve et suit
`/cadrer` (décision du 2026-09-14 : seule une preuve dégèle un tranché). »

### Lire / Modifier
- Modifier : `plugin/skills/nouveau-plan/SKILL.md` (en-tête l. 15-17 ; Étape 4b, phrase contenant
  « session antérieure de la même »).

### Étapes
1. Corriger l'exemple (retrouver par le texte, la phrase est coupée en fin de ligne).
2. Insérer le paragraphe d'en-tête.

### Validation
- **N0 auto (bloque le commit)** : `grep -c 'de la même' plugin/skills/nouveau-plan/SKILL.md` → `0` ; `grep -c 'direction structurante' …/SKILL.md` → `≥ 1`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : —

### Si bloqué
—

### Message de commit
`fix(nouveau-plan): dependance = vague anterieure ; direction approuvee vs details ouverts`
Dernière ligne : `Plan: P5/S6/T17`

---

## T18 — Étape 1 : relire la grille, chercher l'existant, déléguer le flux

### Objectif
Trois insertions dans l'Étape 1 : la relecture de la grille de la décision avant le point 1 ; un
point 4bis « Existant » ; la délégation « flux → `analyste-flux` » à côté de « fichiers →
`explorateur` ».

### Pourquoi
Réutiliser une recherche déjà valide au lieu de la rejouer (règle d'économie 2 du rapport) ; D8 :
chercher une solution existante avant de prévoir un mécanisme custom ; D2 : le point 1 demande un
flux que l'explorateur ne rend pas.

### Décision clé
Décision (c) « Points d'appel », (f) « Appelants ». Textes :

- Avant le point 1 : « **D'abord relire la grille de la décision d'entrée**
  (`${CLAUDE_PLUGIN_ROOT}/skills/cadrer/references/preparation.md`) : ce qui est `READY` avec une
  preuve encore valide **ne se réinvestigue pas** ; seules les `OPEN` et les preuves périmées sont à
  reprendre ici. Une `OPEN` de type `décision` → question à l'utilisateur, pas de plan. »
- Point 4bis, après « Dépendances directes » : « **Existant** — pour toute inconnue technique
  restante (mécanisme non trivial, dépendance envisagée, sous-système custom proposé) : dérouler
  `${CLAUDE_PLUGIN_ROOT}/skills/cadrer/references/rechercher-existant.md`. Sa conclusion
  (réutiliser, configurer, adapter, développer, isoler) est ce que les tâches implémentent ; un
  « développer » sans ce passage est un écart. »
- Paragraphe « **Déléguer** … » : « exploration de fichiers → agent `explorateur` ;
  **compréhension d'un flux** (point 1) → agent `analyste-flux`, qui sépare faits, inférences et
  inconnues — une inconnue qu'il nomme est une hypothèse à typer au point 5, pas un fait ; résumé
  de diff/historique git → agent `resumeur-git`. »

### Lire / Modifier
- Modifier : `plugin/skills/nouveau-plan/SKILL.md` (Étape 1, l. 57-72).

### Étapes
1. Les trois insertions, dans l'ordre. Garder « Garder pour soi les points 1, 5 et 6 » : le point 1
   reste le raisonnement du cadreur, l'agent lui fournit les faits.

### Validation
- **N0 auto (bloque le commit)** : `node tests/tester-renvois.mjs` → assertion 6 résout les deux
  citations croisées ; assertion 4 voit `analyste-flux` cité, présent ; OK.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : recopier les sept pas du protocole dans le point 4bis — domicile unique, la
  skill renvoie.

### Si bloqué
—

### Message de commit
`feat(nouveau-plan): relire la grille, chercher l'existant, deleguer le flux a analyste-flux`
Dernière ligne : `Plan: P5/S6/T18`

---

## T19 — Étape 1, point 5 : hypothèses typées, sonde, risque du plan

### Objectif
Le point 5 type chaque hypothèse racine `lisible` ou `comportementale` et dit ce qu'on fait d'une
comportementale ; le squelette d'index gagne la ligne « Risques du plan ».

### Pourquoi
Écart 2 de la revue du 2026-09-14 : `prémisse` = 10 des 12 échecs classés ; une hypothèse
comportementale ne peut être établie ni au cadrage (Plan Mode) ni à la vérification (lecture seule).
Cinq plans MYO pour une hypothèse qu'une sonde d'une minute aurait tranchée.

### Décision clé
Décision (d), première puce. Texte du point 5 :
« **1-2 hypothèses racines**, chacune **typée** : `lisible` (une lecture du dépôt la confirme ou la
réfute — c'est ce que `verificateur-premisse` sait faire) ou `comportementale` (seule une exécution
tranche : « le juge compte juste », « ce traitement tient en < 1 s »). Une comportementale ne se
gèle jamais en « tranché ». Trois sorties : **sonder** — une commande ou un script jetable via
`verificateur-n0`, résultat écrit dans le `S<k>.md` ou la décision, **avant** d'écrire le plan ;
**déclarer** — ligne `Risques du plan :` sous l'objectif d'ensemble de l'index, avec ce qui la
réfuterait ; **isoler** — issue `isoler` du protocole d'existant, l'hypothèse derrière une couture
nommée pour que le reste se découpe. Un plan écrit sur une comportementale ni sondée, ni déclarée,
ni isolée est un écart au cadrage. »

Squelette d'index, sous « Objectif d'ensemble » :
```
**Risques du plan** : <hypothèses comportementales non sondées — chacune avec ce qui la réfuterait — ou `—`>
```

### Lire / Modifier
- Modifier : `plugin/skills/nouveau-plan/SKILL.md` (Étape 1, point 5),
  `plugin/skills/nouveau-plan/references/squelette-index.md` (après l'objectif d'ensemble).

### Étapes
1. Réécrire le point 5 avec le texte ci-dessus.
2. Ajouter la ligne au squelette. `plans/P5/index.md` la porte déjà : c'est l'exemple vivant.

### Validation
- **N0 auto (bloque le commit)** : `grep -c 'comportementale' plugin/skills/nouveau-plan/SKILL.md` → `≥ 2` ; `grep -c 'Risques du plan' plugin/skills/nouveau-plan/references/squelette-index.md` → `1`.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : rendre la sonde obligatoire pour toute hypothèse — une `lisible` se vérifie
  en lecture, gratuitement ; sonder tout est le surcoût que la décision refuse.

### Si bloqué
—

### Message de commit
`feat(nouveau-plan): hypotheses typees lisible/comportementale ; ligne Risques du plan`
Dernière ligne : `Plan: P5/S6/T19`

---

## T20 — Étape 1bis : classer le plan, lancer le critique avant l'approbation

### Objectif
Une Étape 1bis, entre le verdict de l'Étape 1 et l'approbation en Plan Mode : classification
`borné` / `architectural`, déclencheurs, bloc `Agent({` du critique, routage de son verdict.

### Pourquoi
D3 : la critique placée en 4b arriverait après l'approbation ; le seul moment où elle pèse sur
l'arbitrage est avant. Le coût est une passe Opus, **conditionnelle** : un plan borné sans
déclencheur ne la paie pas.

### Décision clé
Décision (e) « Déclenchement » et « Routage ». Texte de l'Étape 1bis :

« ## Étape 1bis — Critiquer avant de faire approuver (conditionnel)

Le verdict de l'Étape 1 classe le plan : **borné** — un changement bien délimité sur du code
existant (un flag, un endpoint, un correctif d'un fichier) — ou **architectural** — nouveau
sous-système, ou changement qui restructure l'assemblage ou modifie une interface dont d'autres
dépendent. Une complexité découverte en route **monte** le classement, rien ne le redescend ; dans
le doute, architectural.

Le critique tourne sur tout plan **architectural**, et sur un plan **borné** seulement s'il touche
un déclencheur : contrat partagé modifié · migration ou schéma de données · concurrence ·
autorisation · performance annoncée · mode extension après un échec `prémisse` · demande explicite.
Sinon, passer à l'approbation — `verificateur-plan` (4b) suffit.

<bloc Agent({ — voir ci-dessous>

Routage, par le cadreur : `PASS` → approbation. `DÉFAUT` corrigeable sans toucher une décision →
corriger la synthèse, **revérifier la seule correction** (relancer avec le constat et la
correction, rien d'autre). `À VÉRIFIER` ou fait manquant → recherche (`analyste-flux`,
`lecteur-doc`), sonde, ou protocole de preuve — jamais une question à l'utilisateur pour un fait
accessible. « Choix non résolus » non vide → **question** (`WORKFLOW.md` §9c), avec cette liste
comme options. Une passe ; pas de relance sur un reformulage — seulement si buts ou périmètre
changent (Ideation). »

Bloc à écrire, **sur le modèle de `remediation.md` l. 44-52**, première ligne du prompt = la ligne
de renvoi au mot près (assertion 1) :

```
Agent({
  description: "P<n> critique",
  subagent_type: "critique-plan",
  run_in_background: false,
  prompt: "Lis ${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md en entier avant ton premier geste : il porte les invariants de lancement.
Décision applicable : <chemin docs/decisions/…>.
Synthèse du cadrage (Étape 1) : <flux · hypothèses typées · découpage envisagé · critères de réussite — recopiés, 40 lignes au plus>.
Réponse : CRITIQUE: PASS | CRITIQUE: <n> constat(s) numérotés, puis « Choix non résolus : »."
})
```

### Lire / Modifier
- Modifier : `plugin/skills/nouveau-plan/SKILL.md` (nouvelle section entre l'Étape 1 et l'Étape 2 ;
  la phrase de l'en-tête « une fois le plan approuvé par l'utilisateur » gagne « (après l'Étape
  1bis si elle s'applique) »).

### Étapes
1. Insérer la section et le bloc.
2. Vérifier la ligne de renvoi : `grep -c 'Lis ${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md en entier avant ton premier geste' plugin/skills/nouveau-plan/SKILL.md` → `1` — recopiée depuis `remediation.md`, jamais retapée.
3. `node tests/tester-renvois.mjs` : assertion 1 compte **6** blocs ; assertion 4 voit
   `critique-plan`.

### Validation
- **N0 auto (bloque le commit)** : `node tests/tester-renvois.mjs` → `(6 bloc(s) inspecté(s))`, OK ;
  `node plugin/bin/publier.mjs --dry-run` → passe.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : rendre la critique inconditionnelle « par sécurité » — c'est une passe Opus
  sur chaque plan borné, le surcoût que la décision refuse ; le mot « conditionnel » reste dans le
  titre.

### Si bloqué
La ligne de renvoi de `remediation.md` diffère de celle de `WORKFLOW.md` §5b → ne pas choisir :
`FAIL`, nature `prémisse` (le plan suppose une chaîne unique), avec les deux textes.

### Message de commit
`feat(nouveau-plan): etape 1bis — classement borne/architectural et critique avant approbation`
Dernière ligne : `Plan: P5/S6/T20`

---

## T21 — Relecture d'ensemble de la skill

### Objectif
Une relecture de `/nouveau-plan` après T17-T20 : cohérence des renvois internes (numéros d'étapes,
« Étape 1bis » cité là où il faut), longueur, aucune règle recopiée depuis une annexe.

### Pourquoi
Quatre tâches ont touché le même fichier ; la relecture est la seule à voir le tout.

### Décision clé
Rien de nouveau ne s'écrit ici. Si une phrase de la skill contredit la décision, la décision gagne
et l'écart est signalé dans le bilan.

### Lire / Modifier
- Modifier : `plugin/skills/nouveau-plan/SKILL.md` (corrections de renvois seulement).

### Étapes
1. Lire la skill de bout en bout. Vérifier : l'en-tête cite l'Étape 1bis ; l'Étape 4b dit toujours
   que `verificateur-plan` ne juge pas la conception (c'est le critique qui le fait, avant) ;
   l'Étape 0 renvoie toujours à `/cadrer` pour le protocole de preuve.
2. `wc -l` : la skill dépasse-t-elle 300 lignes ? Si oui, le dire dans le bilan avec la section
   candidate à une annexe — **ne pas déplacer** ici.

### Validation
- **N0 auto (bloque le commit)** : `node tests/tester-hooks.mjs && node tests/tester-renvois.mjs && node plugin/bin/publier.mjs --dry-run` → tout passe.
- **Tests** : —
- **N1 visuel auto** : —
- **N2 humain** : —
- **Anti-raccourci** : —

### Si bloqué
—

### Message de commit
`refactor(nouveau-plan): renvois internes apres l'etape 1bis`
Dernière ligne : `Plan: P5/S6/T21`

---

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : oui).

## Bilan de session (rempli à l'exécution — mode vague parallèle, non commité)

- **Fichiers modifiés** (diff laissé non commité dans l'arbre, `.claude/wave.lock` présent) :
  - `plugin/skills/nouveau-plan/SKILL.md` (T17 — exemple de dépendance et paragraphe direction/
    détails ; T18 — relecture de la grille avant le point 1, point 4bis « Existant », délégation
    `analyste-flux` ; T19 — point 5 réécrit avec hypothèses typées ; T20 — nouvelle section
    « Étape 1bis » avec le bloc `Agent({` du critique, et la phrase d'en-tête sur l'approbation ;
    T21 — relecture, aucune correction supplémentaire nécessaire).
  - `plugin/skills/nouveau-plan/references/squelette-index.md` (T19 — ligne `Risques du plan`
    sous l'objectif d'ensemble).
- **Résumé** : `/nouveau-plan` relit désormais la grille de préparation avant d'investiguer,
  cherche l'existant sur les inconnues techniques restantes (point 4bis), délègue la lecture d'un
  flux à `analyste-flux`, type chaque hypothèse racine `lisible`/`comportementale` avec trois
  sorties (sonder/déclarer/isoler), et fait critiquer tout plan architectural (ou un plan borné à
  déclencheur) par `critique-plan` avant l'approbation en Plan Mode (nouvelle Étape 1bis). Le
  squelette d'index porte la ligne `Risques du plan`. Fichier final : 300 lignes — pas de
  dépassement du seuil de 300, rien à signaler pour une annexe.
- **N0 lancé** (via `verificateur-n0`, jamais en direct) :
  - T17 : `grep -c 'de la même' SKILL.md` = 0 ; `grep -c 'direction structurante' SKILL.md` = 1
    (≥ 1). PASS.
  - T18 : `node tests/tester-renvois.mjs` → assertion 6 résout les citations croisées vers
    `preparation.md` et `rechercher-existant.md` ; assertion 4 voit `analyste-flux` cité et
    présent. PASS.
  - T19 : `grep -c 'comportementale' SKILL.md` = 3 (≥ 2) ; `grep -c 'Risques du plan'
    squelette-index.md` = 1. PASS.
  - T20 : `grep -c 'Lis ${CLAUDE_PLUGIN_ROOT}/EXECUTANT.md en entier avant ton premier geste'
    SKILL.md` = 1 (ligne de renvoi recopiée telle quelle, jamais retapée) ; `node
    tests/tester-renvois.mjs` → assertion 1 compte **6** blocs `Agent({` (5 avant cette tâche),
    assertion 4 voit `critique-plan` cité et présent ; `node plugin/bin/publier.mjs --dry-run` →
    passe (59 fichiers, v0.36.0). PASS.
  - T21 : `node tests/tester-hooks.mjs && node tests/tester-renvois.mjs && node
    plugin/bin/publier.mjs --dry-run` → tout PASS ; `wc -l SKILL.md` = 300.
  - **PASS** sur les cinq tâches.
- **N1** : sans objet — aucune UI touchée, uniquement des skills en Markdown.
- **N2** : aucun point — rien ne relève d'un jugement esthétique/UX/ton dans cette session.
- **Si bloqué** : non déclenché — la ligne de renvoi de `remediation.md` (T20) correspondait bien
  au mot du bloc écrit.
- **Anomalie environnement (rapportée, sans impact sur le résultat)** : lors de la vérification de
  T21, `verificateur-n0` a rapporté `wc -l SKILL.md` = 239 via Bash/PowerShell, alors qu'une
  relecture directe du fichier (outil `Read`) montre 300 lignes de contenu. Les autres commandes
  du même agent (assertions de `tester-renvois.mjs` reflétant les insertions successives : 5 puis
  6 blocs `Agent({`) confirment que le fichier lu contenait bien mes éditions — seul le compte
  `wc -l` isolé diverge, sans cause identifiée (possible artefact CRLF/encodage côté shell
  Windows). Sans conséquence : aucune Validation de T17-T21 ne dépend de ce chiffre, et le compte
  de 300 lignes (obtenu par lecture directe) ne dépasse pas le seuil de 300 signalé par le plan.
- **Prochaine action** : orchestrateur — commit tâche par tâche (T17-T21, messages déjà écrits
  dans chaque section ci-dessus) à la clôture de la vague, puis suppression de `.claude/wave.lock`.

Bloquant : 1
Couverture : complète

## Bloquants
- docs/analyses/2026-09-23-eval-revue-d-usage.md:32,47-49 — l'analyse affirme que les deux graders
  `tool_used: Skill` du cas négatif (`verif-visuelle-pas-revue-d-usage`) sont marqués `withOnly` par
  l'ablation par défaut et cite une note de l'outil (« graders marked with-only … become a
  plugin-fired indicator rather than part of the score »), en conséquence le tableau affiche
  `— (with-only, cf. note)` pour le score `without` de ce cas. Or `docs/analyses/evals-23.json`
  montre `"withOnly": false` sur les 12 entrées de graders du cas, un bras `without` réellement
  exécuté et noté (score 0.5 sur les 3 runs : `verif-visuelle` appelé 0x sans le plugin, comme
  attendu), et la chaîne citée n'apparaît dans aucun des deux JSON commités
  (`grep -c "plugin-fired indicator" docs/analyses/evals-23.json docs/analyses/evals-23-positif.json`
  → 0,0). L'Anti-raccourci de T8 exige que l'analyse cite le score du cas négatif ; elle en cite un
  inventé (« pas de comparaison without ») alors que la donnée réelle existe et est même plus
  favorable (0/3 sans le plugin vs 3/3 avec). Un lecteur de l'analyse (ou S4, qui publie sur ce PASS)
  reçoit une description fausse du contenu du JSON qu'elle cite comme preuve — la conclusion PASS
  reste correcte sur les seuils demandés, mais la justification donnée pour l'absence de comparaison
  `without` est fabriquée, pas vérifiée dans les données commitées à côté d'elle.

## Backlog
- plugin/templates/VALIDATION.md:4-5 — la clause ajoutée « (`/revue-d-usage`, passe esthétique
  demandée ; ou toute autre passe esthétique demandée) » répète deux fois « passe esthétique
  demandée » dans la même parenthèse ; aucune autre skill ne définit aujourd'hui de « passe
  esthétique », la clause générale est un flou sans référent concret pour l'instant — à resserrer
  si une autre skill s'en réclame un jour, sinon à simplifier en une seule mention.

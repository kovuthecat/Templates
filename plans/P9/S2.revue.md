Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- plugin/bin/brief-a-jour.mjs:105-115 — `aBriefApplique` liste tout l'historique (`git log
  --format=%H`) puis appelle `git log -1 --format=%B <sha>` un par un pour chaque commit plus
  récent que celui de la décision, jusqu'à trouver la ligne `Brief-applique:`. Un
  `git log --format=%H --grep="Brief-applique: <chemin>" -F` unique donnerait le même résultat en
  un seul appel git, sans boucle proportionnelle à la taille de l'historique. Sans effet observable
  aujourd'hui (peu de décisions, dépôts petits), mais vaut la peine si le nombre de décisions ou la
  taille de l'historique grossit.

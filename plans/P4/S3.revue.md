Bloquant : 0
Couverture : complète

## Bloquants
- aucun

## Backlog
- plugin/skills/orchestrer-plan/references/remediation.md:99-101 — le paragraphe « Ce qui borne »
  du bloc `SendMessage` (canal court) reprend quasi mot pour mot la phrase de
  `plugin/WORKFLOW.md` §9c sur la consommation de budget et le non-retry, plutôt que d'y renvoyer
  comme le fait le reste du fichier pour les trois conditions. Cohérent avec le patron déjà en
  place dans ce fichier (le paragraphe « Budget » en tête reformule aussi les chiffres de §9c), donc
  pas un défaut nouveau — juste à garder en tête si §9c évolue un jour : deux endroits à mettre à
  jour, pas un.

## Vérifié
- **T5** (`plugin/WORKFLOW.md`, commit 3aba102) — §9a renvoie à §9c sans recopier les conditions ;
  §9c porte les trois conditions telles quelles. Contrôle de domicile refait par grep ciblé : les
  trois phrases-clés (« L'agent est reprenable », « N0 est vert sur son périmètre », « Aucune fausse
  piste accumulée ») n'apparaissent qu'une fois dans `plugin/`. Hors périmètre respecté : §1/§5/§5b
  non touchés (diff borné aux lignes ~520 et ~622-650).
- **T6** (`plugin/skills/reprendre-echec/SKILL.md`, commit 37fe663) — troisième ligne mécanique
  `Blocage :` ajoutée au gabarit et documentée (format, geste vs hypothèse, absente ⇒ démarrage à
  froid), renvoi à §9c sans recopie. Gabarit toujours sous son plafond de 40 lignes.
- **T7** (`plugin/skills/orchestrer-plan/references/remediation.md` +
  `plugin/skills/orchestrer-plan/SKILL.md`, commit 4d501e1) — troisième grep `Blocage :` ajouté ;
  nouveau bloc « Le test des trois conditions » appliqué (pas recopié) avant le choix canal
  court/froid ; bloc `SendMessage` distinct du bloc `Agent({` de démarrage à froid, budget et
  non-retry explicites. Les 3 blocs `Agent({` de `remediation.md` (vérification-prémisse, reprise à
  froid, enquête) portent toujours intacte la ligne de renvoi `EXECUTANT.md` (vérifié par grep).
  `SKILL.md` l. 33 : `fork` reste interdit sans condition, `SendMessage` devient conditionnel ;
  l. 184-188 : le cas `partial`/tours épuisés reformulé comme l'instance où la 3ᵉ condition ne
  tient pas, cohérent avec la décision (cadrage explicite : dire la raison comme vraie « dans son
  cas », pas comme catégorie).
- **N0** : `node tests/tester-hooks.mjs` → PASS (24 cas), rejoué depuis cette relecture.
- **Non-régression S2** : `node tests/tester-renvois.mjs` → PASS (les 3 assertions), rejoué depuis
  cette relecture — le nouveau bloc `SendMessage({` de `remediation.md` n'est pas confondu avec un
  bloc `Agent({` par le motif de comptage (ligne exacte `Agent({` requise).
- **Hors périmètre** : `plugin/EXECUTANT.md` et `squelette-session.md` non touchés (diff vide sur
  ces chemins). Table « la nature décide, colonne Modèle » de `remediation.md` intacte — le canal
  se décide après la nature, pas à sa place, conforme à la décision.
- **Non relu** : aucun fichier de données ni doc-only dans ce périmètre — les trois commits ne
  touchent que du prose-code sous `plugin/`.

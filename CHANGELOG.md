# CHANGELOG.md

Une ligne datée par évolution du workflow. Consulté par MIGRATION.md pour évaluer le retard
d'un projet.

- `2026-07-03` — Refonte quota : `plans/P<n>/` (index + `S<k>.md`), skill `/fin-de-tache`, Codex = audits Playwright uniquement.
- `2026-07-07` — Pipeline création : ARCHITECTURE.md → maquette Claude Design (`design/maquettes/`) → câblage.
- `2026-07-07` — Centralisation : générique référencé (CLAUDE-BASE importé, WORKFLOW/CONVENTIONS/AGENTS pointés), skill au niveau utilisateur, MIGRATION.md.
- `2026-07-07` — Migration pilote (S4) élargie à 3 projets (ETP interactif, Chords, S&C) : annexe MIGRATION.md enrichie (variante `Contexte/`, correction de renvois après déplacement, arbitrage ROADMAP.md séparé/fusionné).
- `2026-07-28` — **Refonte coût & fiabilité.** (1) Garde-fous appliqués : 3 hooks (`SessionStart`, `PreToolUse` git, `Stop`) câblés via `project-settings.json` → `.claude/settings.json` du projet ; les règles git de §4d et la mise à jour du contexte ne sont plus de simples consignes. (2) Plafonds de lignes chiffrés (`.claude/hooks/plafonds.json`) + skill `/purge-contexte`. (3) `DECISIONS.md` scindé : registre une-ligne-par-décision + détail dans `docs/decisions/`. (4) `STATUS.md` = photo stricte, sans section historique. (5) Statut des tâches à un seul endroit : l'`index.md` du plan (retiré des `S<k>.md` et de `TASKS.md`). (6) Validation à 3 niveaux N0/N1/N2 : le **navigateur in-app de Claude Code Desktop** fait le N1, `VALIDATION.md` ne garde que le N2 ; bandeau `Environnement` + colonne `Env.` pour le cas VSCode (skill `/verif-visuelle`). (7) Codex recentré sur la régression scriptée. (8) Squelettes de plan déplacés de `WORKFLOW.md` vers la skill `/nouveau-plan` ; exploration déléguée à un subagent `Explore`. (9) Effort : échelle corrigée (`low·medium·high·xhigh`, ni `max` ni `minimal`), défaut projet `medium`, critère modèle-vs-effort explicite.
- `2026-08-22` — **Plugin `workflow@templates`.** Le repo devient la marketplace Claude Code `templates` (GitHub privé `kovuthecat/Templates`) exposant un plugin unique `workflow` (`.claude-plugin/plugin.json`) : skills, hooks (`hooks.json`, chemins `${CLAUDE_PLUGIN_ROOT}`) et agents. `CLAUDE-BASE.md` n'est plus importé par ligne `@chemin` : injecté par le hook `SessionStart`. `.claude/settings.json` d'un projet se réduit à `enabledPlugins` + `permissions` + `effortLevel`. Projets non migrés : fonctionnent encore tels quels (`MIGRATION.md` §5).
- `2026-08-22` — **Agents mécaniques.** Quatre agents Haiku à délégation proactive : `explorateur` (localisation multi-fichiers), `verificateur-n0` (build/typecheck/tests, jamais en direct), `resumeur-git` (diff/historique), `lecteur-doc` (doc externe). Chacun ne rend que sa conclusion. Table de délégation dans `CLAUDE-BASE.md`.
- `2026-08-22` — **Skill `/nouveau-projet`.** Interview de cadrage guidée (question par question, gate de validation avant écriture) puis instanciation mécanique des fichiers de contexte, `.claude/settings.json` et premier commit. Remplace le pas-à-pas manuel qui vivait dans `README.md`.
- `2026-08-22` — **Skill `/choisir-mecanisme`.** Arbre de décision (règle/skill/hook/subagent/MCP/plugin/`/goal`/scheduling), hiérarchie de complexité, audit express d'un projet `.claude/`. Renvoie à l'agent `claude-code-guide` pour toute question pointue.
- `2026-08-22` — **`DESIGN_SPEC.md`.** Nouveau fichier auto-suffisant : brief UI (écrans, navigation, données affichées, état Design Sync) envoyé tel quel à Claude Design. `ARCHITECTURE.md` redevient un document technique (découpage, état/persistance, entités).
- `2026-08-22` — **`VALIDATION.md` : plafond 120 → 60.** N2 en attente uniquement ; un item tranché devient une ligne supprimée (git reste l'archive).
- `2026-08-23` — **Réflexion longue : couper, pas étirer** (plugin `workflow` 0.4.0). `/cadrer` §4b : une réflexion qui s'allonge s'écrit et repart à froid, au lieu d'itérer sur un contexte accumulé. Principe consigné dans `CLAUDE-BASE.md` : **la délégation empêche le contexte d'entrer, elle ne l'évacue pas** — un agent devrait relire tout l'historique pour reconstruire ce que la conversation détient déjà, donc rédiger soi-même coûte moins cher que déléguer la synthèse. Seul le démarrage à froid fait retomber le contexte.
- `2026-08-23` — **Instructions de compactage** (plugin `workflow` 0.3.1). Section `# Compact instructions` dans le `CLAUDE.md` template : pilote ce que le compactage préserve (décisions + justification, chemins modifiés, résultats de validation, tâches restantes) au lieu d'ajouter une passe de résumé. Écarté après vérification : déclencher le compactage par hook (`PreCompact` ne peut que **bloquer**, jamais déclencher) et le confier à un sous-agent Haiku (double le coût — le compactage est déjà une passe de modèle sur tout l'historique). Projets existants : `MIGRATION.md` §Étape 6.
- `2026-08-23` — **Skill `/cadrer`** (plugin `workflow` 0.3.0). La session de réflexion Opus qui précède un plan devient une procédure : gate « la session est-elle nécessaire ? » (doc/repo/décision déjà prise → agent ou registre), question et critère de fin écrits d'abord, délégation de toute exploration, 2-3 options avec recommandation, écriture au fil de l'eau, sortie par une issue unique (`docs/decisions/`, `/nouveau-plan`, ou rien). `/nouveau-plan` se recentre sur le découpage et exige une décision écrite en entrée.
- `2026-08-23` — **Skill `/executer-vague`** (plugin `workflow` 0.2.0). Pendant exécutif de `/nouveau-plan` : orchestre une vague de sessions sans session `Desktop`, une session = un processus `claude -p --model --effort`, ne conserve que les verdicts, arrêt au premier FAIL. Économie de contexte obtenue par des interdits explicites (ne jamais ouvrir un `S<k>.md`, ne jamais lire un diff). La procédure vivait en prose dans `/nouveau-plan`, elle devient actionnable au moment du lancement.
- `2026-08-22` — **Permissions allowlist.** `project-settings.json` porte désormais `permissions.allow`/`deny`, prérequis des vagues headless (orchestrateur `claude -p`) ; à étendre au fil de l'eau, jamais élargie par précaution.
- `2026-08-22` — **Enchaînement de sessions.** Pastille `spawn_task` en Desktop (1 clic = session neuve) pour ne jamais enchaîner deux sessions d'un même plan dans une seule conversation ; orchestrateur headless `claude -p` pour les vagues sans session `Desktop`.
- `2026-08-22` — **Règle tests systématiques.** La skill `/nouveau-projet` rend la question de la stratégie de test obligatoire (Q12) ; une réponse « aucun test » doit être justifiée et consignée dans le `DECISIONS.md` du projet.
- `2026-08-23` — **`plugin/` extractible + garde-fous de distribution.** Le plugin est isolé sous `plugin/` (layout conventionnel `skills/` `agents/` `hooks/` auto-découvert, squelettes dans `plugin/templates/`, marketplace embarquée) : la source `./` n'embarque plus plans, décisions ni rapports. Marketplace enregistrée en source `git-subdir` → clone **sparse** de `plugin/` seul. Manifeste réduit à l'identité : déclarer `hooks`/`skills` en plus de l'auto-découverte **casse le chargement du plugin** (`Duplicate hooks file detected`) sans que `claude plugin validate` le voie — d'où une CI (`.github/workflows/plugin.yml`) qui **installe réellement** le plugin et compare l'inventaire exposé au contenu du disque, en plus de `validate --strict`. Deux skills : `/migrer-projet` (MIGRATION.md distillé, exécutable depuis le projet à migrer) et `/reprendre` (reprise à coût borné). Instructions de compactage déplacées du squelette `CLAUDE.md` vers `CLAUDE-BASE.md` (source unique injectée à chaque session — un hook `PreCompact` ne peut PAS le faire : sa sortie ne va qu'au debug log). `/cadrer` perd `Bash` par `allowed-tools`. `AGENTS.md` : runner Playwright via `PLAYWRIGHT_AUDIT_RUNNER`, plus de chemin personnel versionné. `/nouveau-projet` lit ses squelettes dans `${CLAUDE_PLUGIN_ROOT}/templates/` — il copiait jusqu'ici le `DECISIONS.md` de ce dépôt, décisions D-P2-* comprises, dans chaque nouveau projet.
- `2026-08-23` — **Extraire `plugin/` vers un repo public** (procédure, non appliquée). Une session cloud n'a pas de credentials git : une marketplace privée ne s'y charge pas. Un repo public se clone sans authentification. Depuis un clone à jour : `git subtree split --prefix=plugin -b plugin-only` puis `git push git@github.com:kovuthecat/claude-workflow.git plugin-only:main` (le repo public est à créer au préalable, vide). Le nom de la marketplace reste `templates` : seul le bloc `extraKnownMarketplaces` change, en `{ "source": "github", "repo": "kovuthecat/claude-workflow" }` ; `workflow@templates` et les `enabledPlugins` déjà déployés ne bougent pas. Déplacer aussi `.github/workflows/plugin.yml` dans le nouveau repo, en retirant le préfixe `plugin/` des chemins.
- `2026-08-23` — **Reprise d'échec : deux rapports, deux destinataires.** Le verdict contraint par schéma réglait l'improvisation de l'orchestrateur mais supprimait tout contexte exploitable pour corriger. Une session qui échoue écrit désormais un **rapport de passation** (`plans/P<n>/S<k>.echec.md`, gabarit dans `/reprendre-echec`, plafond 40 lignes) et ne renvoie que son **chemin** dans le verdict : l'orchestrateur relaie un chemin sans jamais l'ouvrir, le contexte reste intégralement disponible à qui répare. La section « Déjà écarté » du gabarit est ce qui évite de refaire le chemin d'enquête. Chaque session reçoit un `--session-id` propre (sans lui, une session headless hérite de la session ambiante et son transcript devient introuvable) ; `claude --resume <uuid>` reste un **recours**, jamais le défaut — reprendre une session en échec rapatrie ses fausses pistes, ce que le démarrage à froid existe pour éviter. Nouvelle skill `/reprendre-echec` (état réel vérifié avant diagnostic, gate sur annulation destructive, N0 reprouvé avant de coter PASS). Corrigé au passage dans `/executer-vague` : lecture du verdict par stdin et non par argument (une sortie longue dépassait la taille max d'un argument), `catch` rendant la règle fail-closed mécanique, et mécanique de lancement parallèle enfin explicite (`&` par session + un `wait`).
- `2026-08-23` — **Arrêt de vague mécanique + diagnostic escaladé optionnel.** L'« arrêt au premier FAIL » n'était qu'une consigne en prose : l'orchestrateur lançait les sessions une par une et devait décider lui-même de ne pas enchaîner. La vague séquentielle passe en boucle explicite avec `break` sur verdict ≠ PASS — l'arrêt est structurel, plus laissé à la docilité du modèle (vérifié sur banc : sur PASS/FAIL/PASS, la 3e session n'est jamais lancée). En parallèle l'arrêt anticipé n'existe pas, c'est dit. Nouvelle Étape 5b **optionnelle et désactivée par défaut** : pour une vague non surveillée, une passe de diagnostic escalade d'un cran au-dessus du modèle qui a échoué (lu dans l'`index.md`, jamais un Opus codé en dur — si Opus a échoué on monte à Fable, `WORKFLOW.md` §2) et **enrichit le rapport de passation sans rien corriger**. `--disallowed-tools Edit` retire le principal vecteur de modification, et un `git status --porcelain` comparé avant/après vérifie mécaniquement que la passe n'a touché que le `.echec.md` — la réparation elle-même reste une décision humaine via `/reprendre-echec`.
- `2026-08-23` — **`/nouveau-projet` : installation explicite du plugin.** `enabledPlugins` dans `.claude/settings.json` n'installe rien à lui seul pour une source externe (confirmé doc) — seule la marketplace s'enregistre automatiquement à la confiance du dossier. La session en cours a forcément le plugin (elle exécute `/nouveau-projet`), mais une session future sur une machine ou un conteneur cloud neuf ne l'aurait pas sans installation préalable ailleurs. Phase C ajoute `claude plugin install workflow@templates --yes` — vérifié idempotent (no-op propre si déjà installé).
- `2026-08-23` — **Distribution par dépôt public séparé.** Le plugin est publié dans `kovuthecat/claude-workflow` (public, plugin seul) ; le développement reste ici, dans le dépôt privé, sous `plugin/`. Motif : une session cloud n'a pas de credentials git à elle et ne peut pas cloner un dépôt privé — le plugin ne se chargeait donc qu'en local. Un dépôt public se clone sans authentification : le plugin marche en session cloud, sur mobile et sur toute machine neuve, sans PAT à créer, stocker ni faire tourner. Source `extraKnownMarketplaces` passée de `git-subdir` sur le dépôt privé à `github` sur le dépôt public ; le nom de marketplace reste `templates`, donc `workflow@templates` et les `enabledPlugins` déjà déployés ne bougent pas. **Publication sans historique** (commit unique, `push --force`) : l'historique de `plugin/` traverse l'anonymisation et exposerait sinon 55 occurrences du prénom et le chemin `C:\Users\...` dans des états antérieurs, alors que l'arbre actuel est propre. Procédure dans `README.md` §Distribution.
- `2026-08-24` — **Hook `SessionStart` de bootstrap dans `project-settings.json`** (plugin `workflow` 0.10.0). Mesuré dans MYO : même repo public confirmé joignable, une session cloud ne clone jamais la marketplace déclarée au démarrage — `enabledPlugins` seul n'y active donc rien, et tout le plugin restait silencieusement absent. Nouveau fichier `plugin/templates/session-start.sh` (idempotent, best-effort, no-op en local) copié en `.claude/hooks/session-start.sh` de chaque projet, référencé par un unique hook `SessionStart` dans `project-settings.json` — exception ciblée à la règle « aucun hook dans le settings projet », les trois autres hooks restent dans le plugin. `/nouveau-projet` et `/migrer-projet` mis à jour en conséquence. Détail : `docs/decisions/2026-08-24-sessionstart-bootstrap-hook.md`.
- `2026-08-24` — **`/executer-vague` : deux voies, trois verdicts** (plugin `workflow` 0.11.0). Première exécution réelle de la skill (MYO/P1) : 7 blocages, vague jamais démarrée. (1) La colonne `Env.` décide désormais de la **voie**, plus du droit d'orchestrer : headless (`claude -p`, verdict par schéma) pour `—`, pastilles `spawn_task` (un clic = une conversation neuve, verdict lu dans la colonne Statut) pour `Desktop`, les deux pour une vague mixte — qui ne se clôt donc pas d'un bloc. La contrainte « une vague orchestrable ne contient aucune session `Desktop` » est retirée de `/nouveau-plan`, il n'en reste qu'un conseil de découpage. (2) **Troisième verdict `PANNE`** : le fail-closed convertissait un `command not found` (CLI `claude` absent du PATH, exit 127) en `FAIL · sortie non conforme`, indiscernable d'une session qui a travaillé et échoué — l'orchestrateur renvoyait vers un rapport de passation inexistant, pour du code jamais ouvert. `PANNE` = JSON illisible **et** exit ≠ 0, route vers une remédiation d'environnement, jamais vers `/reprendre-echec` ; impose de capturer `2> S<k>.stderr.log` et `echo $? > S<k>.exit` (ce dernier sert aussi de marqueur de fin). (3) **Préflight du binaire** avant tout le reste (`command -v claude` + contrôle des flags supposés via `claude --help` — un CLI trop ancien les ignore en silence). (4) **`wave.lock` posé après le préflight** et retiré si aucune session n'a démarré : posé avant un lancement impossible, il gelait commit et push jusqu'à un `/fin-de-tache` qui ne viendrait jamais. (5) **Arbre sale tranché par zones** : intersection avec les colonnes `Zone modifiée` non vide → STOP, vide → référence `git status --porcelain` + confirmation. Bloquer en bloc coûtait un commit sans rapport avec le plan (383 fichiers LDraw dans MYO) pour un lancement qui a ensuite échoué. (6) **Hook `Stop` muet pendant une vague** : `stop-contexte.mjs` importe `vagueParallele()` — déjà exporté par `lib.mjs` et utilisé par les deux autres hooks — au lieu de laisser un orchestrateur Haiku `low` plaider « c'est volontaire » en prose contre son propre garde-fou. (7) **Attente par sondage des `.exit`** plutôt que `wait` bloquant : les harnais plafonnent la durée d'un appel shell (10 min pour l'outil Bash), une session plus longue était tuée en vol. Non corrigé, documenté : `model: haiku` du frontmatter ne s'applique pas toujours (comportement du harnais). Détail : `docs/decisions/2026-08-24-executer-vague-deux-voies.md`.
- `2026-08-24` — **`/migrer-projet` couvre aussi le projet jamais outillé** (plugin `workflow` 0.11.0). Trou entre `/nouveau-projet` (repo **vide**, interview de cadrage en 14 questions) et `/migrer-projet` (projet **déjà** sur le workflow v1, piège du double chargement) : le projet qui a du code mais n'a **jamais** été outillé — ni `.claude/settings.json`, ni fichiers de contexte, ni `plans/`. D'abord écrit en skill séparée `/adopter-projet`, **fusionné le jour même dans `/migrer-projet`** : choisir entre les deux exigeait de l'utilisateur un diagnostic que la Phase A des deux skills faisait déjà, et la skill séparée devait porter une trappe « si tu trouves un import `@CLAUDE-BASE.md`, arrête-toi et lance l'autre » — une skill qui renvoie vers une autre skill signale que la coupure est au mauvais endroit. Résultat : un point d'entrée unique, un diagnostic (8 points, inventaire délégué à `explorateur` et `resumeur-git`) qui classe en 4 états — bascule v1, adoption depuis zéro, entretien (dette de format seule), repo vide → `/nouveau-projet` — puis deux voies B1/B2 encadrées par une gate et une Phase D communes. Principe de la voie adoption, inverse de `/nouveau-projet` : **le brief se dérive du code, il ne s'invente pas** — la stack, le découpage et les commandes se lisent, et l'interview se réduit aux 6 choses qu'aucune lecture ne donne : pourquoi le projet existe, l'usage prévu, où il en est **vraiment**, ce qui vient ensuite, les décisions déjà prises, la stratégie de test (obligatoire). `PROJECT_MAP.md` et `ARCHITECTURE.md` décrivent donc le constaté, pas la cible. Câblage complet (settings fusionnés sans écraser `permissions.allow`, `session-start.sh`, `AGENTS.md`, `.gitignore` + `wave.lock`, `launch.json`), plafonds respectés dès l'écriture, piège du typecheck vide vérifié à l'instanciation, doc existante absorbée ou pointée mais jamais recopiée.
- `2026-08-24` — **Bootstrap cloud fiabilisé + vocabulaire d'environnement à trois cas** (plugin `workflow` 0.11.1). Objectif : le plugin marche à l'identique en Desktop et en session distante (`claude.ai/code`, appli mobile). (1) **`claude plugin install --yes`** dans `templates/session-start.sh` : le CLI documente ce flag « required when stdin or stdout is not a TTY » — or un hook `SessionStart` n'a jamais de TTY. Sans lui, l'installation pouvait rester bloquée sur une confirmation que personne ne voit, et la session cloud démarrait sans plugin, silencieusement (le symptôme même que le hook de 0.10.0 devait supprimer). (2) **Garde `command -v claude`** en tête du script : échouer une fois avec la vraie cause plutôt que laisser trois commandes tomber l'une après l'autre. (3) **`timeout: 90`** sur le hook dans `templates/project-settings.json` — il fait un clone réseau de la marketplace, autrement plus lent que les hooks locaux du plugin (15-20 s) ; aucun timeout n'était déclaré. (4) **Vocabulaire d'environnement corrigé partout** (`WORKFLOW.md` §5b/§6, `CLAUDE-BASE.md`, `/verif-visuelle`, `/nouveau-plan`, `/executer-vague`) : il n'opposait que Desktop et VSCode, en ignorant le cloud et le mobile. La distinction reste binaire — **Claude Code Desktop est le seul environnement à avoir le navigateur in-app et la pastille `spawn_task`** — mais cloud et mobile sont désormais nommés du bon côté. La voie headless de `/executer-vague`, elle, marche partout où le préflight 2a est vert, cloud compris. (5) Prérequis rendu explicite dans `project-settings.json` : `.claude/settings.json` **et** `.claude/hooks/session-start.sh` doivent être **versionnés, bit exécutable compris** (mode 100755) — une session cloud ne voit que ce que le clone lui apporte. Vérifié conforme sur les 6 projets déployés, qui ont tous reçu le correctif (1)-(3). **Reste non vérifié** : le chemin cloud lui-même n'a jamais été observé en fonctionnement — il ne peut l'être que depuis une vraie session distante.
- `2026-08-24` — **Bascule vers le modèle vendoré** (plugin `workflow` 0.12.0). Renversement de la décision fondatrice du 2026-08-22 (« générique référencé, jamais copié »), assumé : l'exigence a changé — quatre environnements (app Desktop, extension VS Code, session cloud, appli mobile) et du partage à venir, là où la décision d'août visait un utilisateur unique en local. **Constat qui a tranché** : toutes les pannes de la journée venaient de la machinerie d'installation (CLI absent du PATH, marketplace jamais clonée en session distante, bit exécutable perdu, timeout de hook, flag `--yes`), aucune du contenu ; et l'avantage théorique du modèle runtime — publier une fois, tout le monde suit — n'existait déjà pas en pratique, l'auto-update étant désactivé par défaut pour les marketplaces tierces. On payait les coûts sans le bénéfice. **Nouveau modèle** : skills, agents, hooks et docs sont copiés dans le dépôt (`.claude/skills`, `.claude/agents`, `.claude/workflow`), donc présents dans le clone — la seule chose que tout environnement Claude Code garantisse. Rien à installer, nulle part. `plugin/bin/sync-workflow.mjs` vendore et met à jour ; le **manifeste** (`.claude/workflow/manifest.json`, un hash par fichier géré) distingue « modifié à la main ici » (préservé par défaut, `--force` pour écraser) de « source plus récente » — c'est lui qui sépare un vendoring d'un copier-coller. Règle associée : un fichier au manifeste ne se modifie jamais dans le projet, l'amélioration remonte à la source puis redescend par `/maj-workflow` (nouvelle skill, elle-même vendorée donc utilisable sans rien installer). **Paradoxe d'amorçage résolu** : un dépôt vide n'a pas de `.claude/`, donc pas de `/nouveau-projet` — d'où une commande d'amorçage qui n'exige aucun état préalable (`git clone` du dépôt public + le moteur ; `git` et `node` suffisent, pas de CLI `claude` sur le `PATH`). Vérifiée de bout en bout sur un dépôt vide depuis le dépôt public. `/migrer-projet` vendore et **retire** `enabledPlugins`/`extraKnownMarketplaces`/bootstrap ; `/nouveau-projet` porte l'amorçage ; `templates/session-start.sh` supprimé (sans objet) ; le dépôt public reçoit enfin un `README.md` et une licence MIT, sans quoi le partage était impraticable. **Bug attrapé par le manifeste au pilotage** : le moteur s'appliquait sa propre table de substitution en se vendorant, produisant une table neutralisée dans la copie vendorée — qui ne substituait donc plus rien ; le marqueur est désormais construit par concaténation. Sans manifeste, la corruption passait inaperçue jusqu'à la première synchronisation depuis un projet. **Pilote MYO** : vendoré, testé (clone neuf sans plugin → 13 skills, hooks vivants, auto-resynchronisation), puis mis à jour 0.11.1 → 0.12.0 par le moteur lui-même. Au passage, une panne cloud préexistante réparée : son `AGENTS.md` pointait vers un chemin absolu Windows. Reste à faire : les 5 autres projets, et la preuve en session cloud réelle.
- `2026-08-24` — **Vague mixte : la voie Desktop avait un verdict que personne ne pouvait écrire** (plugin `workflow` 0.12.2). `/executer-vague` lisait le verdict des sessions Desktop dans la colonne Statut de l'`index.md`, « cochée par son propre `/fin-de-tache` » — alors que `/fin-de-tache`, mode vague parallèle, interdit explicitement de toucher `index.md` (fichier partagé). Aucune session Desktop ne pouvait donc se déclarer finie : l'orchestrateur redemandait indéfiniment, et rabattait sur une confirmation verbale qu'il refusait ensuite à juste titre. Constaté sur MYO P1 vague 1 (S2, S3 terminées, jamais collectées). Correctif : symétrie avec la voie headless — chaque session Desktop dépose `.claude/vague/S<k>.verdict` (un fichier par session, aucune contention), l'orchestrateur le lit et **coche lui-même l'index pour les deux voies**. En vague, l'`index.md` a désormais un rédacteur unique. Fichier absent = `EN ATTENTE`, jamais PASS/FAIL, et une confirmation verbale n'est jamais un verdict.
- `2026-08-24` — **Une vague partage un seul arbre de travail : worktree refusé** (plugin `workflow` 0.12.2). Corollaire du bug ci-dessus, mesuré sur les mêmes sessions : S2 et S3 avaient travaillé dans des worktrees ouverts par le harnais, leur diff invisible depuis `main`. Or `WORKFLOW.md` §4b interdit tout commit avant la fin du plan — le seul moyen de rapatrier un diff de worktree était donc verrouillé, et la vague 2 (S4, dépendante du code de S2) devenait inexécutable. Le modèle de vague suppose un arbre unique : c'est là que l'Étape 2d prend sa référence et là que la consolidation committe. `pretooluse-git.mjs` refuse désormais `git worktree add` **et** l'outil `EnterWorktree` du harnais tant que `.claude/wave.lock` existe (matcher élargi à `Bash|PowerShell|EnterWorktree`) ; `/fin-de-tache` et le prompt de pastille le disent aussi. Rapatriement d'un diff déjà égaré : patch `git diff HEAD` du worktree, `git apply` dans l'arbre principal, sans commit.
- `2026-08-24` — **Les garde-fous de vague étaient inertes là où ils comptaient le plus** (plugin `workflow` 0.12.3). `vagueParallele()` cherchait `.claude/wave.lock` dans le cwd ; or le verrou est gitignoré, donc présent uniquement dans l'arbre principal. Depuis un worktree, aucun hook de vague ne se déclenchait — ni le refus de commit, ni le bandeau de session : le mode le plus dangereux était aussi le seul sans filet. `vagueParallele()` résout désormais la racine du dépôt via `--git-common-dir` (nouvelles `racineDepot()` et `worktreeLie()`). Second point, découvert en regardant la pastille : « **Démarrer avec worktree** » est le choix **par défaut** du menu `spawn_task`, et il crée l'arbre **avant** le premier tour de la session — hors de portée de tout `PreToolUse`. Le refus `EnterWorktree` de 0.12.2 ne couvre donc pas ce chemin-là. Ajout d'un contrôle `SessionStart` : vague en cours + worktree lié → **STOP** au premier tour, avant qu'une session de travail ne soit perdue. `/executer-vague` dit explicitement de cliquer « Démarrer localement ».
- `2026-08-24` — **Renversement : chaque session committe son propre travail** (plugin `workflow` 0.13.0). `WORKFLOW.md` §4b reportait tous les commits en fin de plan ; l'attribution devenait alors une fouille dans un arbre où trois sessions avaient déposé leurs fichiers — confiée au modèle le moins cher du plan, et constatée impraticable sur MYO P1 (l'orchestrateur devait déléguer à `resumeur-git` pour seulement *lire* l'état du dépôt). La session qui écrit le code est la seule à savoir quels fichiers sont les siens : elle committe tâche par tâche avant de rendre la main, staging explicite, avec un repère `Plan: P<n>/S<k>/T<m>` en dernière ligne qui rend l'attribution mécanique (`git log --grep`). Le push reste groupé, jamais depuis une session. **`index.md` garde un rédacteur unique** : la session hors vague, l'orchestrateur en vague — c'est le seul fichier que toutes touchent, et le seul point où la proposition « que chaque session coche la sienne » a été écartée. **`wave.lock` ne sert plus qu'au parallélisme réel** (sessions headless simultanées dans un même index git, où `git commit` emporterait le travail des voisines) : là, les commits sont reportés à l'orchestrateur en fin de vague, pas de plan. Une vague Desktop, sérialisée par l'humain qui clique, ne prend plus de verrou. Disparaissent : la table d'arbitrage par zones de l'Étape 2d, la session de consolidation de fin de plan, les fichiers `.claude/vague/S<k>.verdict` de 0.12.2 (un commit *est* le verdict) et le blocage inter-vagues (la vague N+1 part d'un arbre qui contient déjà la vague N). Décidé avec Thibault le 2026-08-24 ; MYO P1 termine sous les règles précédentes, la bascule vaut à partir du plan suivant.
- `2026-08-24` — **Une session sans diff n existait pas** (plugin `workflow` 0.13.1). Troisième variante du même trou, constatée sur MYO P1/S5 (« Vérifier la route sur le set de test, sans publier », colonne `Zone modifiée` = « aucune ») : la session n a laissé **aucune** trace — pas de commit, pas de bilan, pas de fichier — et rien ne permet de la distinguer d une session jamais lancée. Cause : le bilan de session ne vivait dans le `S<k>.md` qu en **mode vague parallèle** ; en solo, le rapport restait verbal, donc mourait avec la conversation. Or MYO P1 accumulait quatre canaux de trace incompatibles — verdict JSON (headless), fichier `.verdict` (S4), bilan dans le `S<k>.md` (S2/S3/S6), diff de code (S7) — et S5 est passée entre les quatre. Correctif : **le bilan dans le `S<k>.md` devient universel**, écrit après chaque tâche dans les deux modes, et committé avec elle. Une session qui ne produit aucun code committe quand même son `S<k>.md` : un canal unique, garanti non vide, lisible par le même `git log --grep` que les autres. `/nouveau-plan` exige en plus, pour toute session de zone « aucune », que le `S<k>.md` déclare à l avance ce que le bilan devra contenir — corpus, critère, résultat attendu — sans quoi la session rend « ça marche » et le plan n a rien à vérifier.
- `2026-08-24` — **L enchaînement des sessions Desktop ne passe plus par l humain** (plugin `workflow` 0.13.2). Symptôme signalé par Thibault : chaque session Desktop terminée devait être annoncée à la main à l orchestrateur. Cause : l interdiction d écrire dans l `index.md` était attachée au mot « vague » et non au verrou. Une session Desktop committait donc son travail sans pouvoir cocher sa ligne — la suivante ne voyait jamais sa dépendance satisfaite, et le relais manuel devenait le seul chemin. Or `wave.lock` n existe que pour la concurrence réelle (0.13.0) : sans lui, aucune contention à protéger. Désormais **l `index.md` suit le verrou** — présent : l orchestrateur coche en fin de vague ; absent : la session coche sa propre ligne, et elle seule, dans le commit de ses tâches. Combiné à la section « Enchaînement » de `/fin-de-tache` (pastille `spawn_task` posée par la session qui finit), la chaîne se propage seule : l orchestrateur ouvre une vague et en rend compte, il n a plus à être réveillé entre deux sessions. `STATUS.md`, `TASKS.md` et `VALIDATION.md` restent hors de portée dans tous les cas.
- `2026-08-24` — **La fin de vague se signale par une pastille, pas par l utilisateur** (plugin `workflow` 0.13.3). Depuis 0.13.2 la chaîne se propage entre sessions ; restait un relais manuel à la fin de chaque vague, pour dire à l orchestrateur de collecter. La session qui coche la dernière ligne `[ ]` de sa vague le sait : elle pose désormais elle-même une pastille `P<n> — collecter la vague <w>`. Un orchestrateur repart à froid, ce que le workflow préfère de toute façon à une conversation réveillée avec le contexte du lancement. **Voie par message écartée, et pourquoi** : `SendMessage` traverse bien les sessions, mais aucune ne peut s identifier elle-même (`list_sessions` exclut la session courante, `get_session` refuse son propre id) — l orchestrateur ne peut donc pas laisser son adresse, et une session fille ne voit que des noms auto-générés qu elle ne sait pas rattacher. `notify_when_idle` va dans l autre sens mais exige une session déjà ouverte, alors que la pastille n est cliquée que plus tard. Et un message reste éphémère : une vague Desktop s étale sur des heures, la conversation d orchestration sera fermée — ce serait un canal de verdict pouvant silencieusement ne pas exister, la panne réparée trois fois ce jour.
- `2026-08-24` — **Une vague se deroule sans intervention : la voie Desktop passe aux sous-agents** (plugin `workflow` 0.14.0). Objectif pose par Thibault : qu un orchestrateur puisse derouler une vague, voire un plan, seul. La pastille l interdisait par construction — un clic par session. Deux faits mesures avant de rien changer : (1) une session `claude -p` repond NON a « as-tu des outils `mcp__Claude_Browser__` ou `preview_start` », donc le N1 y est reellement impossible ; (2) un sous-agent lance depuis une session Claude Code Desktop herite des **18 outils navigateur**, directement et non en differe. La voie Desktop devient donc une **voie sous-agent** : outil `Agent` en arriere-plan, un par session, contexte propre, navigateur herite, notification automatique a la fin — et une vague mixte se termine dans le meme tour. La pastille `spawn_task` est releguee au **repli manuel**, pour un orchestrateur qui tourne hors Desktop (VSCode, terminal, cloud) et n a donc aucun navigateur a transmettre. Limite assumee et ecrite : l outil `Agent` accepte un `model` mais pas un `effort` — une session `Env. = Desktop` a effort `high` tourne a l effort ambiant, et l orchestrateur doit le signaler plutot que de faire comme si la colonne avait ete respectee. Verdict de la voie sous-agent : une ligne au format impose, **recoupee par les commits** — un PASS sans commit correspondant est un FAIL, meme lecon que le 2026-08-24 sur les confirmations verbales. Contrainte nouvelle : la session d orchestration reste ouverte pendant la vague, les sous-agents vivant en elle.
- `2026-08-24` — **Prefight : l allowlist doit etre effective, pas seulement presente** (plugin `workflow` 0.14.0). Mesure sur MYO a la veille de lancer P2 : `claude -p` affichait `Ignoring 10 permissions.allow entries: this workspace has not been trusted`. La confiance est indexee sur la **chaine** du chemin, pas sur le dossier — l entree `C:Users...MYO` etait approuvee, mais un shell resolvant `C:/Users/.../MYO` cree une entree distincte, non approuvee. Toutes les sessions headless de la vague auraient tourne sans allowlist, a buter sur des outils refuses que personne n etait la pour autoriser. Controle ajoute a l Etape 2c, avec sa remediation.
- `2026-08-25` — **Le canal JSON meurt plus souvent que les sessions : recoupement par les commits en voie headless** (plugin `workflow` 0.14.1). Recurrent sur MYO (P2/S9 puis P3/S1) : la session fait son travail — bilan ecrit, N0 vert, commits presents — mais le verdict JSON n arrive jamais (processus tue apres le travail mais avant l emission, refus de permission sur le dernier outil, timeout). L orchestrateur fail-closed concluait FAIL sur du travail reussi et envoyait /reprendre-echec reparer du code sain. La voie sous-agent recoupait deja verdict et commits (0.14.0), la voie headless non — le recoupement devient symetrique : tout FAIL « sortie non conforme » ou PANNE est confronte aux commits de la session, tache par tache via le repere Plan: P<n>/S<k>/T<m>. Commits complets → PASS (motif « verdict perdu en route ») ; partiels → FAIL avec le premier manquant ; aucun → verdict du JSON inchange. Jamais applique a un JSON lisible qui dit FAIL : la session a parle, ses commits ne la contredisent pas. Et la cause recurrente devient visible : `permission_denials` est promu cinquieme champ lisible de l enveloppe — les refus sont listes dans le rapport final avec la remediation (completer permissions.allow apres la vague, jamais en preventif large).
- `2026-08-25` — **MIGRATION.md neutralise** (plugin `workflow` 0.14.2). Le document vendore dans chaque projet decrivait encore la migration *vers* le modele plugin (Etape 5 : poser `enabledPlugins`, « une skill locale masquerait celle du plugin ») — l inverse du modele courant, et la recette exacte du double chargement repare le 2026-08-24. Reduit a un renvoi vers les trois skills a jour (/nouveau-projet, /migrer-projet, /maj-workflow) et au seul invariant du manifeste. L historique reste dans git.
- `2026-08-25` — **Audit a froid : correctifs mecaniques** (plugin `workflow` 0.15.0). (1) Deux dernieres references au modele pastille corrigees (CLAUDE-BASE §sessions, /nouveau-plan voie Desktop → sous-agent, verdict par commits). (2) L amorcage pose la confiance du workspace sur les DEUX formes de chemin (/nouveau-projet Phase C, /migrer-projet) — sans elle, claude -p ignore l allowlist en silence. (3) sync-workflow --check compare desormais le bloc hooks du settings.json projet au template (ligne SETTINGS informative, jamais bloquante) : la derive qui a demande une edition manuelle x6 pour EnterWorktree se voit desormais toute seule. (4) Nouveau bin/publier.mjs : publication du miroir public scriptee (payload temporaire, scan de chemins personnels bloquant, commit unique, push --force, verification post-push, --dry-run) — le miroir etait reste 4 versions en retard sans que rien ne le signale ; /fin-de-tache point 8 exige desormais la publication, pas seulement le bump. (5) Les 12 descriptions de skills raccourcies a une ligne — payees a chaque session dans chaque projet. Cadrage voie unique d orchestration : docs/decisions/2026-08-25-cadrage-voie-unique-orchestration.md (en attente d arbitrage).
- `2026-08-25` — **`/executer-vague` devient `/orchestrer-plan`** (plugin `workflow` 0.16.0, plan P3). Consequence de l option C : une vague se terminant desormais dans le tour, l arret entre vagues n avait plus de justification. La skill deroule un plan entier, vague apres vague, jusqu a epuisement, un echec ou une gate humaine. **492 -> 196 lignes** : le narratif historique part dans `docs/decisions/2026-08-25-lecons-orchestration.md` (6 lecons datees, sans procedure) et toute regle de §4a/4b/5b devient un renvoi — les trois domiciles uniques de `WORKFLOW.md` sont la vraie reparation de la cause racine des bugs du 2026-08-24 (meme regle enoncee a 3-4 endroits, corrigee a un seul). Trois politiques ecrites : boucle de vagues ; **echec = finir la vague, arreter le plan** (le rapport separe les sessions bloquees des sessions encore independantes) ; **gate humaine declarable** dans l ordonnancement, echappatoire qui preserve le point de controle N2 maintenant qu il n y a plus d arret automatique. Colonne `Env.` inversee : `—` = sous-agent (defaut), `headless` = exception justifiee (effort reellement applique, ou vague fenetre fermee) ; `Desktop` d un plan anterieur se lit comme `—`. **Trouve en dogfoodant ce plan** : l orchestrateur doit retirer `wave.lock` AVANT de committer pour les sessions verrouillees — le hook evalue la commande avant execution, donc `rm wave.lock && git commit` dans un meme appel est refuse lui aussi. Ordre desormais ecrit dans l Etape 5.
- `2026-08-26` — **Comptage de lignes : le `\n` final n'est pas une ligne** (plugin `workflow` 0.16.1). `nbLignes()` renvoyait `split('\n').length`, soit N+1 pour tout fichier terminé par un retour à la ligne — la convention POSIX, donc quasiment tous. Le plafond effectif valait `plafond - 1`, et `/purge-contexte` était **insatisfiable** : la skill recompte avec `wc -l`, qui ne compte pas ce dernier élément vide, donc purger jusqu'à la cible laissait le hook Stop bloquer à chaque session. Symptôme mesuré au moment du correctif : 5 fichiers pile au plafond signalés en dépassement (ETP interactif/STATUS.md, Interface-OE/STATUS.md, MYO/TASKS.md, ebm-msp/STATUS.md, ebm-msp/TASKS.md), poussant à sur-purger — l'inverse du but de la skill. Le correctif retire le dernier élément du split quand il est vide ; un fichier sans `\n` final reste compté juste (c'est pourquoi on ne compte pas simplement les `\n`).
- `2026-08-26` — **Audit de partage : `LICENSE` manquante, `publier.mjs` vendoré à tort, renvoi mort** (plugin `workflow` 0.16.2). Testé de bout en bout (clone du miroir public, vendoring dans un dépôt neuf, ré-exécution `--check`, exécution des 4 hooks vendorés) : le socle technique tenait, mais quatre trous cassaient le partage à un tiers. (1) **`plugin/LICENSE` (MIT) créée** — le CHANGELOG de la 0.12.0 annonçait « le dépôt public reçoit enfin une licence MIT », jamais fait ; sans elle le dépôt public est *tous droits réservés* en droit, personne ne peut légalement le forker. `publier.mjs` copie `plugin/` tel quel : elle atteint le miroir public au prochain `node plugin/bin/publier.mjs` (vérifié par `--dry-run`, 44 fichiers). (2) **`publier.mjs` exclu du vendoring** (`sauf: ['publier.mjs']` sur la règle `bin` de `sync-workflow.mjs`) : il était copié dans `.claude/workflow/bin/` de chaque projet en aval avec une URL de dépôt public en dur (`kovuthecat/claude-workflow`) et un `--force` implicite — un script d'un tiers, sans droits d'y pousser, que rien n'expliquait. (3) **Renvoi mort réparé** dans `/choisir-mecanisme` : `docs/references/claude-code-capabilities-2026-08.md` vit dans `docs/` du dépôt privé, jamais vendoré — les deux mentions précisent maintenant que ce fichier n'existe que dans le dépôt source, la table de liens officiels (§Vérifier avant de construire) restant la voie pour un projet vendoré. (4) **Section « Forker ce workflow »** ajoutée à `plugin/README.md` : les 6 endroits où l'URL du dépôt public est en dur, pour quiconque veut distribuer sa propre variante. **Dérives internes corrigées au passage** : `README.md` racine listait encore `/executer-vague` (renommé `/orchestrer-plan` en 0.16.0) et référençait `plugin/templates/session-start.sh`, supprimé en 0.12.0 — les sections « Copiés dans le projet »/« Référencés » décrivaient encore l'ancien modèle plugin-installé au lieu du vendoring ; `/fin-de-tache` point 8 pointait `.claude/workflow/bin/publier.mjs`, un chemin qui n'existe pas dans le dépôt source (non vendoré par construction, cf. `.claude/settings.json`) — corrigé en `plugin/bin/publier.mjs`, seul chemin réel.
- `2026-08-28` — **Une vague se déroulait sans dire ce qu'elle lançait** (plugin `workflow` 0.17.0). Deux manques signalés par Thibault, même cause : l'orchestration a été optimisée pour le coût de contexte, jamais pour ce que l'humain voit passer. (1) `/orchestrer-plan` lançait ses sous-agents sans annoncer la vague — l'utilisateur voyait N chips démarrer sans savoir quelles tâches partaient, ni sur quel modèle, ni ce qu'il retrouverait commité. L'Étape 3 impose désormais un bloc d'annonce (une ligne par session : titre, tâches, modèle/effort, voie, zone), construit depuis l'`index.md` déjà en contexte — donc à coût nul, et sans jamais ouvrir un `S<k>.md`, l'interdit qui fait tenir la skill. (2) **Une pastille `spawn_task` hérite des réglages ambiants de l'application, pas de ceux du plan** : un `S<k>` prévu `Sonnet`/`high` partait sur ce qui était réglé la veille, et rien ne le signalait avant le résultat. Le rappel devient obligatoire et prend domicile en `WORKFLOW.md` §3 (« À régler AVANT de lancer — modèle · effort ») ; `/fin-de-tache`, `/reprendre`, `/cadrer` et `/orchestrer-plan` (repli pastille, rapport final) y renvoient au lieu de le reformuler. Seule dispense : une commande `claude -p`, qui porte déjà `--model`/`--effort`. (3) Effet de bord traité au passage : un exécutant headless (`claude -p`) lit les settings utilisateur, donc l'`outputStyle` de l'humain — des explications que personne ne lit, payées en tokens de sortie. Le bloc headless porte désormais `--settings '{"outputStyle":"Default"}'`. Un sous-agent n'en a pas besoin : un style ne s'applique jamais à un sous-agent. `/choisir-mecanisme` gagne la branche manquante de son arbre (output style vs CLAUDE.md) et ses trois pièges. (4) **Le style devient un fichier du dépôt, pas un réglage de poste** : `plugin/output-styles/pedagogue.md` entre dans le plan de vendoring vers `.claude/output-styles/`, et le gabarit `project-settings.json` porte `"outputStyle": "Pedagogue"`. Motif : un réglage `~/.claude/` ne survit ni à une autre machine ni à une session cloud, alors que le clone du repo est la seule chose qu'un environnement Claude Code garantisse — la doctrine même qui a fait abandonner le modèle plugin. Arbitrage assumé par Thibault contre la recommandation inverse : un settings de projet prime sur les préférences utilisateur, donc quiconque instancie ce gabarit hérite d'un style personnel ; le `_comment` du gabarit dit comment le retirer. `sync-workflow.mjs` signale désormais un `outputStyle` absent du settings d'un projet (ligne informative, jamais bloquante, comme le contrôle du bloc hooks) — sans la clé, le style vendoré serait présent mais jamais sélectionné, exactement la panne silencieuse des hooks non câblés.
- `2026-08-28` — **Publier depuis un dépôt en retard écrasait le miroir public** (plugin `workflow` 0.17.1). Constaté le jour même : ce dépôt local était en retard d'un commit sur `origin/main` — la 0.16.2 avait été poussée depuis un autre poste et jamais rapatriée ici. `publier.mjs` a construit son payload depuis l'arbre local et l'a poussé en `--force` vers `kovuthecat/claude-workflow` : le miroir a perdu `plugin/LICENSE`, l'exclusion de `publier.mjs` du vendoring et les corrections README de la 0.16.2, jusqu'à ce que la divergence soit repérée à la main et réparée par un rebase + republication. Le seul garde-fou du script scannait le **contenu** du payload (chemins personnels) ; rien ne regardait l'**état du dépôt qui le produit**, sur le seul chemin du workflow qui pousse en `--force` — là où être en retard ne dégrade pas, mais détruit du contenu déjà publié. Nouveau `verifierSynchroSource()`, exécuté avant toute construction de payload (donc aussi en `--dry-run`, qui ne pousse toujours rien) : `git fetch origin` puis `git rev-list --left-right --count origin/main...HEAD`. **Retard → refus** — exit 1, rien poussé, avec la remédiation (`git pull --rebase`), sur le modèle exact du refus pour chemin personnel. **Arbre sale, ou HEAD en avance sans être poussé → avertissement seulement** : publier depuis un commit local non poussé est légitime — c'est même le cas nominal juste après un bump — mais mérite d'être vu passer. Un `fetch` en échec ou un `origin/main` absent ne bloquent pas non plus (le push suivant échouerait de lui-même, `--dry-run` doit rester utilisable hors ligne, et un fork peut n'avoir aucun remote), mais le script dit alors que sa vérification porte sur une référence possiblement périmée. Les quatre branches vérifiées avant commit, dont le refus rejoué sur un clone remis un commit en arrière.
- `2026-08-28` — **Un cache de plugin périmé pilotait les sessions en silence** (plugin `workflow` 0.17.2). Symptôme : le rapport d'instanciation de `Tuto-onshape` signalait deux « écarts par rapport au texte de la skill » — pointeur absolu vers `AGENTS.md`, copie du settings sans vendoring — alors que `/nouveau-projet` en 0.17.1 dit exactement l'inverse aux deux endroits. **Cause** : la session ne lisait pas la skill du dépôt mais celle de `~/.claude/plugins/cache/templates/workflow/0.10.0/`, sept versions en arrière, servie alors que `installed_plugins.json` et `known_marketplaces.json` sont vides et que rien dans `~/.claude.json` ne la référence — aucun mécanisme de configuration ne rendait ce chargement visible. Le projet instancié est sain : la session a improvisé la bonne réponse (vendoring 0.17.1, hooks sur `$CLAUDE_PROJECT_DIR`, renvoi relatif), le workflow n'y est pour rien. **Réparation du poste** : cache rafraîchi en 0.17.2, 0.10.0 supprimé. **Garde-fou** : `sync-workflow.mjs` inspecte désormais `plugins/cache/*/workflow/*` (version lue dans `plugin.json`, jamais dans le nom du dossier, et racine honorant `CLAUDE_CONFIG_DIR`) et compare à la version de la source. Écart → ligne `CACHE` détaillée **et refus d'écrire** (exit 3), `--ignorer-cache` pour passer outre : contrairement aux contrôles `SETTINGS`, informatifs, un avertissement de plus au fil d'une sortie longue est précisément ce qui n'a pas été lu la première fois — et ce moteur est le seul programme qui tourne à chaque instanciation et à chaque mise à jour. `--check` reste inchangé dans son verdict (`ÉTAT:`) : il répond « ce projet vendoré est-il à jour ? », question qui ne dépend pas de ce qui traîne sur le poste. Le commentaire du garde-fou ne peut pas citer le marqueur de plugin en clair — ce fichier se vendore lui-même et le substituerait (piège déjà documenté §MARQUEUR). Quatre cas rejoués : cache aligné, cache périmé en `--check` (non bloquant), cache périmé en écriture (exit 3, rien écrit), `--ignorer-cache`.
- `2026-08-30` — **Corriger un échec de prémisse étend le plan** (plugin `workflow` 0.18.0). Deux échecs n'avaient qu'un traitement : l'**échec d'exécution** (N0 rouge, bug) est couvert par `S<k>.echec.md` + `/reprendre-echec` ; l'**échec de prémisse** — la session réussit, mais son résultat invalide une hypothèse dont dépendent les sessions suivantes — ne l'était pas, et tombait dans `/nouveau-plan`, seule skill autorisée à découper. Constaté sur P19 (MYO) : la correction partait pour être `P20`, soit `P20` pour finir `P19` pour finir `P16` — une récursion sans fond où « P16 terminé » cesse d'être mesurable. `/nouveau-plan` gagne une **Étape 0** qui aiguille entre plan neuf et **extension** d'un plan en cours, sur un critère unique : *la correction débloque-t-elle une session restante de `P<n>`, l'« Objectif d'ensemble » restant vrai tel qu'il est écrit ?* En extension, l'Étape 3 modifie l'`index.md` au lieu de le créer (numérotation continue — jamais de `S5bis`, le repère `Plan: P<n>/S<k>/T<m>` doit rester unique ; vague de remédiation insérée **avant** celles qu'elle débloque, avec sa cause et sa date ; session `[x]` reposant sur l'hypothèse invalidée remise à `[ ]` et annoncée). **Plafond : deux vagues de remédiation** — la troisième n'est plus une extension mais une prémisse fausse, et bascule sur `/cadrer`. `/reprendre-echec` **pointe** vers ce mode (Étape 3, avant toute correction) sans l'exécuter : garder l'écriture d'`index.md`/`S<k>.md` dans une seule skill et un seul modèle (Opus) évite deux gabarits de session qui divergent. Détail : `docs/decisions/2026-08-30-extension-de-plan.md`.
- `2026-08-30` — **Ce qu'un sous-agent a le droit d'apporter, et une relecture en fin de session** (plugin `workflow` 0.19.0). Deux décisions cadrées le même jour, plus cinq corrections tirées des lettres Claude Code des 22 et 29/08. (1) **`fork` autorisé sous conditions** : la doc confirme qu'un fork *« inherits the entire conversation »* et que sa première requête **réutilise le cache du parent**, donc « un agent devrait relire tout l'historique » — le motif écrit en 0.4.0 — est faux pour lui. La règle de `CLAUDE-BASE.md` est **rétrécie, pas abrogée** : le fork gagne quand la tâche a besoin du contexte courant ET produit du bruit à retenir dehors (ses appels d'outils ne remontent pas) ; il perd sur une restitution pure, où écrire soi-même coûte moins. Trois interdits : `subagent_type: "fork"` refusé dans `/orchestrer-plan` (un exécutant ne connaît que son `S<k>.md`), refusé dans `/reprendre-echec` (il rapatrierait les fausses pistes que le démarrage à froid existe pour laisser derrière), refusé pour une synthèse sans outil. (2) **Aucun `memory:` sur les quatre agents mécaniques** : pour chacun, l'information qu'il mémoriserait a déjà sa source dans le dépôt — commandes (`CLAUDE.md`), localisation (`PROJECT_MAP.md`), état git (éphémère), doc externe (elle bouge). Le cas qui semblait le meilleur est le pire : un `verificateur-n0` à mémoire **fige** une commande de `typecheck`, y compris celle qui ne compile aucun fichier — s'il redécouvre les commandes à chaque fois, le défaut est dans `CLAUDE.md`, et une mémoire le recouvrirait. Critère opposable retenu : *une mémoire d'agent n'est légitime que pour une information dont aucun fichier du dépôt n'est déjà la source.* (3) **Relecture qualité en fin de session** : `/code-review` en effort `high` tourne depuis 2.1.232 dans un agent d'arrière-plan sans prendre la session — la brique existait, il ne manquait qu'un déclencheur. Étape conditionnelle de `/fin-de-tache` (rien à relire si la « Zone modifiée » est `aucune`), périmètre = le diff de la session délimité par ses propres commits, corrections dans le périmètre faites sur place puis N0 rejoué, le reste dans `TASKS.md` — **jamais `VALIDATION.md`**, réservé au N2 humain. **Pas de niveau N3** : la grille N0/N1/N2 est citée dans une dizaine de fichiers, l'élargir se paierait partout. Détail : `docs/decisions/2026-08-30-contexte-des-sous-agents.md` et `docs/decisions/2026-08-30-branchement-code-review.md`. **Cinq corrections au passage** : un sous-agent qui épuise son `maxTurns` rend désormais un résultat marqué `partial` (2.1.246) au lieu d'avoir l'air d'avoir fini — `/orchestrer-plan` le traite comme un `FAIL` (« tours épuisés ») et interdit de le continuer par `SendMessage`, qui rapatrierait son contexte ; le bloc headless passe de `outputStyle: Default` à **`Concise`** (style intégré v2.1.237+, attaque par le résultat, supprime préambule et narration — un exécutant n'a pas de lecteur) ; `/reprendre` reprend les exigences du `/recap` d'Anthropic (objectif **cité** et non reformulé, état **avec sa preuve** = un commit `Plan: P<n>/S<k>/T<m>`, distinction bloqué-sur-une-personne / bloqué-techniquement) ; le repli pastille de `/orchestrer-plan` annonce `/rename P<n>·S<k>`, trois sessions parallèles étant indistinguables dans la liste ; `/verif-visuelle` signale la sélection d'élément du panneau Browser (`Ctrl/Cmd+Shift+B` puis `Ctrl/Cmd+Shift+S`) pour faire **désigner** un point N2 au lieu de le décrire. Écarté explicitement : les règles `autoMode` en langage naturel — elles vivent dans les settings **utilisateur**, qu'un settings projet ne peut pas écraser, donc non vendorables et absentes sur toute machine neuve ; les hooks restent le mécanisme des garde-fous de projet.
- `2026-08-30` — **Reprise automatique d'un échec dans l'orchestration** (plugin `workflow` 0.20.0). Un `FAIL` n'arrête plus le plan : après la clôture de la vague, `/orchestrer-plan` (nouvelle **Étape 5c**) lance **une** reprise automatique par session en échec — un sous-agent **frais** (jamais `fork` ni `SendMessage` vers l'agent échoué : le démarrage à froid est le point de la reprise) qui déroule `/reprendre-echec` en « mode orchestré » sur le seul `S<k>.echec.md`, au modèle **un cran au-dessus** de l'index, plancher Sonnet (Fable en échec → arbitrage direct, pas de cran au-dessus). Trois verdicts : `PASS` (la reprise a commité, supprimé le `.echec.md`, coché `[x]` — le plan **enchaîne** sans intervention), `FAIL` (deuxième échec consécutif → arrêt et arbitrage humain, **jamais** de seconde reprise auto), `ARBITRAGE` (une gate de `/reprendre-echec` exige un humain dès la première reprise : annulation destructive, prémisse fausse → `/nouveau-plan` extension, hypothèse épuisée). Activée par défaut, opt-out par le mot **`reprise-manuelle`** sur la ligne d'ordonnancement de la vague (documenté dans `/nouveau-plan`) ; les vagues `gate` s'arrêtent toujours après collecte, reprise comprise ; reprises séquentielles, arrêt à la première non-`PASS`. Même une reprise qui échoue paie : son `.echec.md` mis à jour (« Déjà écarté » enrichie) fait démarrer l'arbitrage humain de plus haut. Détail : `docs/decisions/2026-08-30-reprise-automatique-echec.md`.
- `2026-08-30` — **Écrire pour qui décide : le plan explique, l'orchestrateur détaille** (plugin `workflow` 0.21.0). Le workflow était optimisé de bout en bout pour le coût de contexte — interdits de lecture, verdicts d'une ligne, index réduit à une table — et produisait un déroulé qu'un utilisateur non développeur ne peut pas suivre : des chips et des commits passent sans qu'on sache ce qui change, pourquoi maintenant, ni ce qui attend si ça rate. Le bloc d'annonce de la 0.17.0 avait traité le symptôme au format minimal (titre, tâches, modèle, zone) : c'est de la donnée de table, pas une explication. **Registre opposable** dans `CLAUDE-BASE.md` (§ Écrire pour qui décide, source unique chargée à chaque session, délibérément compact) : ce qu'un humain lit dit d'abord **ce que ça change et à quoi il le verra**, ensuite comment ; un terme technique inévitable est explicité une fois ; une recommandation énonce son revers, sinon elle ne laisse rien à arbitrer — avec la clause que le registre porte sur la manière de rédiger, **pas sur le volume**, sans quoi il financerait la verbosité que le reste du workflow combat. **`/nouveau-plan`** : l'Ordonnancement de l'`index.md` porte un ***Pourquoi maintenant*** par vague (il justifie l'**ordre**, pas le contenu) et une ligne **« en clair »** par session — ce qu'elle change, à quoi on le constatera, jamais une paraphrase du titre ; chaque tâche d'un `S<k>.md` gagne un **`### Pourquoi`** distinct de l'objectif (une consigne sans intention s'exécute à la lettre et à côté), et une étape qui réclame trois lignes de justification est une décision qui manque, pas une étape. **`/orchestrer-plan`** : l'annonce de vague passe d'une ligne par session à une **strophe** — pourquoi cette vague maintenant, ce qui se passe sur échec (reprise auto et son modèle, ou arrêt), ce qui se passe en fin de vague (gate ou enchaînement), puis par session son « en clair » **relayé mot pour mot**, tâches, modèle/effort, voie, fichiers touchés, dépendances, et enfin quoi ne pas toucher pendant l'exécution et quoi retrouver commité après ; le rapport final explique sur `FAIL`/`ARBITRAGE` **ce que ça empêche de faire**, le motif technique restant cité tel quel. **Deux modifications indissociables** : l'orchestrateur ne peut annoncer que ce que l'index contient, et il lui reste interdit d'ouvrir un `S<k>.md` — d'où la règle **relayer, jamais reformuler** (il tourne sur Haiku, l'index est rédigé par Opus) et le refus explicite d'inventer la ligne quand elle manque : un plan antérieur s'annonce sans elle, signalé une fois, et se corrige dans l'index. **`/cadrer`** : une option se présente par ses conséquences observables avant tout détail technique — « une option que le lecteur ne peut pas évaluer n'est pas une option, c'est un fait accompli » — et la décision écrite s'ouvre par ce qu'elle change, en clair, avant toute justification. Détail : `docs/decisions/2026-08-30-ecrire-pour-qui-decide.md`.
- `2026-08-31` — **Relecture de session : clôture d'abord, fichier ensuite** (plugin `workflow` 0.22.0). Première traversée réelle d'un plan avec la revue branchée : 4 trouvailles réelles, mais 2 sessions arrêtées sans committer en attendant leur propre revue, 15 rapports dans le contexte de l'orchestrateur, rien de trié. Cause racine : la skill disait à la fois « avant de clore », « non bloquant » et « se corrige maintenant » — incompatibles. Réparé dans `/fin-de-tache` : la section « Relecture de session » passe APRÈS les modes de clôture (statuts, commits, rapport d'abord — la revue ne peut plus coûter la session) ; résultats dans `plans/P<n>/S<k>.revue.md` (première ligne `Bloquant : <n>`, marqueur mécanique ; non commité, consommé au tri), trouvailles classées **bloquant** (défaut réel dans le livré — seuil : ce qu'un utilisateur rencontrerait) / **backlog** (tout le reste) ; point 16 gagne le tri de clôture (`.revue.md` → `TASKS.md`, bloquants en tête, puis suppression). `/orchestrer-plan` Étape 5 : « Revues de session — relayer, sans ouvrir » — une ligne par revue à bloquants en fin de vague et au rapport final, jamais le contenu. Exception solo : un bloquant se corrige sur-le-champ (commit correctif + N0 rejoué). Détail : `docs/decisions/2026-08-31-revue-plomberie.md`.
- `2026-09-04` — **Les quatre agents de délégation tournent au premier plan** (plugin `workflow` 0.23.0). Troisième échec d'affilée du même type sur des reprises automatiques : la session lance `verificateur-n0` pour le N0 qui gate son commit, puis rend la main en attendant le retour — comportement par défaut de l'outil `Agent` (arrière-plan sauf mention contraire), que rien ne contredisait pour ces quatre agents. Le harnais ne distingue pas « session finie » de « session qui attend une notification » : sans enfant actif au premier plan, elle est close, et la notification qui arrive ensuite ne rouvre personne pour la lire — le diagnostic ou la correction étaient corrects, seul le retour se perdait, avant tout commit. Réparé aux deux domiciles qui décrivent `explorateur`/`verificateur-n0`/`resumeur-git`/`lecteur-doc` (`CLAUDE-BASE.md` §Avant de coder, `WORKFLOW.md` §5) : les quatre se lancent désormais **au premier plan**, jamais `run_in_background: true` — distinct de la voie sous-agent de **session entière** (§5b), où c'est la conversation d'orchestration qui reste ouverte pour attendre. Rappel ajouté au site observé (`/reprendre-echec` Étape 4, le N0 qui décide du commit). Détail : `docs/decisions/2026-09-04-delegation-au-premier-plan.md`.
- `2026-09-04` — **Amendement : §5b précise que l'arrière-plan de session ne relâche pas §5** (plugin `workflow` 0.23.1). Signalé le jour même : la règle du premier plan n'était écrite que côté §5, et `WORKFLOW.md` §5b (« sous-agent, la voie par défaut... en arrière-plan ») pouvait se lire comme un régime différent, sans dire qu'une session **lancée par l'orchestrateur** reste, elle, tenue au premier plan pour ses propres délégations internes (N0 via `verificateur-n0` en tête). §5b gagne la phrase symétrique, au domicile même de l'arrière-plan légitime. Aucun changement de fond — même distinction qu'en 0.23.0 — seul le risque de lecture partielle (ouvrir §5b sans avoir lu §5) est fermé. Détail : addendum dans `docs/decisions/2026-09-04-delegation-au-premier-plan.md`.
- `2026-09-05` — **Une revue a posteriori qui recale l'objectif avant de juger le code** (plugin `workflow` 0.24.0). Les projets ont atteint la taille où correctifs, améliorations et ajouts s'empilent : le workflow couvrait le défaut ponctuel (`/code-review` sur un diff de session) et la question déjà posée (`/cadrer`), mais rien ne couvrait l'**accumulation** — ni le fait qu'un `PROJECT_BRIEF.md` écrit au jour 1 cesse d'être vrai sans que personne ne le remarque. Nouvelle skill **`/revue-de-conception`** (périmètre `zone` ou `global`) en trois temps : constater (intention écrite, puis état réel délégué aux quatre agents), **recaler l'objectif avec l'utilisateur en interview**, puis en déduire les écarts. **L'interview est la colonne vertébrale, pas un repli** : après des mois d'empilement, ce n'est pas toujours le code qui a dérivé de l'objectif, c'est souvent l'objectif qui a bougé sans que l'écrit suive — même symptôme, réponses opposées (aligner le code / mettre l'écrit à jour), et la différence ne se déduit pas du dépôt. Six contraintes : (1) l'étalon d'Étape 1 est **provisoire**, l'interview l'arrête par une **gate explicite** (10 lignes restituées, pas d'accord implicite) ; (2) l'intention s'écrit **avant** que le code soit lu, sans quoi le code devient sa propre référence ; (3) l'interview n'est pas un questionnaire — une question à la fois, aucune question dont la réponse est dans les fichiers, chaque question s'ouvre par son constat et propose 2-3 issues avec recommandation, 7 au maximum (patron de `/nouveau-projet`) ; (4) **pas de coût observable, pas d'écart** ; (5) 7 écarts max classés A/B/C/D et **pondérés par la phase du projet** (ajout / stabilisation / fin de vie), une seule prochaine action ; (6) **sans humain disponible** (sous-agent, `claude -p`, tâche planifiée), la revue s'arrête au constat et le dit. Sortie : `docs/revues/<date>-<perimetre>.md`, non plafonné, idées **écartées** comprises. Une seule écriture hors rapport, après la gate : `PROJECT_BRIEF.md` (Objectif / Hors périmètre) + décision écrite si le déplacement contraint le code — un étalon périmé est la cause racine de la dérive, le laisser périmé garantit une revue suivante identique. Détail : `docs/decisions/2026-09-05-revue-de-conception.md`.
- `2026-09-07` — **La revue de session dépose son fichier elle-même, au premier plan** (plugin `workflow` 0.25.0). Sur un plan entier, les revues ont tourné à chaque session et produit du travail réel — et **aucune n'a déposé son `plans/P<n>/S<k>.revue.md`** : lancée en arrière-plan comme *dernier* geste, la revue rendait son verdict dans un tour que plus personne ne lisait (la session avait rendu la main, donc était close ; en voie orchestrée, le sous-agent ou le `claude -p` avait carrément disparu). Exactement le mécanisme écrit le 2026-09-04 pour les quatre agents de délégation, jamais appliqué à `/code-review` parce que son arrière-plan avait été choisi comme une qualité — vrai tant que la revue tournait AVANT les commits, faux depuis le réordonnancement du 2026-08-31, sans que personne ne rouvre la question. Trois changements, un principe (*le livrable est le fichier, pas la notification*) : (1) nouvel agent **`relecteur-session`** (Sonnet, 5e agent du plugin), lancé **au premier plan**, qui **écrit lui-même** le `.revue.md` avant de répondre — `/code-review` en `high`+ est explicitement interdit à ce poste ; (2) **dépôt inconditionnel** (`Bloquant : 0` quand il n'y a rien à dire), pour qu'un fichier absent ne veuille dire qu'une chose : la revue n'a pas tourné ; (3) le manque devient **détectable** — hook `Stop` (compare `HEAD` au repère posé par `SessionStart`, n'exige la revue que si des commits portent un repère `Plan:` et du code, fail-open partout) et `/orchestrer-plan` Étape 5 (`Revue S<k> : absente`, non bloquant). Le déclencheur, l'ordre « clôture d'abord », les deux classes et le tri de clôture ne changent pas. Détail : `docs/decisions/2026-09-07-revue-orpheline.md`.
- `2026-09-09` — **Le garde-fou de revue confondait « consommée au tri » et « jamais lancée »** (plugin `workflow` 0.25.1). Signalé sur le premier plan clos sous 0.25.0 : le hook `Stop` réclamait les revues de S1/S2/S3, qui avaient bel et bien tourné (`Bloquant : 0/0/1`) — c'est la session de clôture elle-même qui, en versant le bloquant dans `TASKS.md` puis en supprimant les trois `.revue.md` (`/fin-de-tache` point 16 : transitoire, jamais archivé), avait effacé la seule preuve que `revuesManquantes()` savait lire. Le contrôle testait `existsSync()` sur un fichier que la clôture a précisément pour tâche de faire disparaître ; et le `.revue.md` n'étant **jamais commité**, sa disparition ne laisse aucune trace dans git : revue rangée dans les règles et revue jamais lancée se présentaient au hook comme le même vide. **Correctif : c'est au tri de déposer la trace.** Le commit dédié du point 16 porte un second repère, `Revues: P<n>/S<k>[, …]`, symétrique du `Plan: P<n>/S<k>/T<m>` des commits de tâche (`WORKFLOW.md` §4b) ; le hook exempte les sessions déclarées là, et elles seules. La sévérité est intacte — l'échappatoire est une déclaration explicite et auditable dans l'historique, pas une exemption devinée d'un état du disque — et l'incident que le contrôle existe pour attraper (un plan entier clos sans aucune relecture, `docs/decisions/2026-09-07-revue-orpheline.md`) reste couvert : sans revue trouvée, il n'y a rien à déclarer, et le point 16 impose toujours de le signaler au rapport de clôture. Le message du hook nomme désormais les deux lectures et la sortie de chacune. Vérifié sur un banc de 9 scénarios (revue absente, présente, triée, tri partiel, `.echec.md`, repère multi-refs, commits de suivi seuls, fail-open).
